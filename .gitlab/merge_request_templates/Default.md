## Description

<!-- Explain your changes and the reasoning behind them. -->

## Related issues

Closes #

## Screenshots

<!-- Attach before/after screenshots for UI changes. -->

## Checklist

- [ ] Changelog fragment added to `changelog.d/` (format: `<slug>.<type>.md`)
- [ ] Tests pass locally (`make test` or scoped `pytest` / `npm test`)
- [ ] Lint passes locally (`make lint`)
- [ ] Type-check passes (`make typecheck`)
- [ ] Documentation updated if applicable (`docs/`)
- [ ] No `any` types introduced in TypeScript
- [ ] No raw SQL — ORM or parameterized queries only
