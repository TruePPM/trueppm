Fix the risk form, ceremony modal and phase-gate panel printing an HTML error body (a proxy 502 or nginx 403) one character per line; they now show the generic fallback message. The risk form also shows the read-only demo refusal as a calm note instead of a red error.
