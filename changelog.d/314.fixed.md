Fix successor cascade not appearing in the Schedule view after a dependency edit (closes #314): `useDependencyMutations` now invalidates the `tasks` query alongside `dependencies` so CPM-shifted dates are visible to the originating client without waiting for the next 2-second poll, and `useProjectWebSocket` now handles `dependency_created` / `dependency_updated` / `dependency_deleted` events so collaborators see new dep edges immediately.
