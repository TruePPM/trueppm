Bumped the locked `fast-uri` resolution to 3.1.7 in the website dependency tree, clearing four HIGH advisories (GHSA-5jgf-p345-68v8, GHSA-f65p-4m7j-42xc, GHSA-fph4-wmhf-6fwf, GHSA-jqff-g426-hqxp — all CVSS 7.5), each fixed in 3.1.6 and unpatched in every prior 3.x release. `fast-uri` is pulled transitively by `ajv`, which declares `^3.0.1`, so this is a lockfile-only change that stays inside the range the manifest already allows. The web and mobile trees do not resolve `fast-uri` and are unaffected.
