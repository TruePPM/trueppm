The hosted read-only demo's landing project no longer reads as a project in trouble (#4050 Part B). After loading the Atlas sample, and only on a deployment with `TRUEPPM_DEMO_READ_ONLY` on, `load_sample_project` now applies a demo-only overlay: the `AT_RISK` health override is cleared, "Migration complete" is confirmed, the dry-run migration is finished, the unscheduled pile is trimmed to two, and a final Monte Carlo run — recorded last, so it outlives every edit — lands P80 a few days ahead of the commitment. One realistic signal is kept deliberately: Performance tuning stays about ten points behind plan on the critical path. `atlas-platform-launch.json` is unchanged, so every other consumer of the fixture still loads it exactly as authored.
