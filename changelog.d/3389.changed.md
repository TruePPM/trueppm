- **`api:test` shards are balanced again**: the pytest-split duration baseline
  had covered 1,758 of the suite's 10,075 tests since June, so the four shards
  landed 260s apart in serial-equivalent time and the slowest one — which gates
  the whole test stage — ran roughly 2.2x its siblings, looking like a hung job.
  The baseline is regenerated; simulated shard spread drops from 260s to 3.4s
  and the gating shard from 521s to 413s.
