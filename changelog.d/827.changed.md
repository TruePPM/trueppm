Helm chart: the API/worker image now defaults to the chart `appVersion` instead of `latest` (reproducible deploys and rollbacks; override `image.tag` for a specific build), and the chart version tracks the release version.
