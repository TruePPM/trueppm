Fixed SonarQube reliability and accessibility findings: schedule canvas layers are now explicitly non-focusable (`tabindex="-1"`) so their `aria-hidden` cannot trap a screen reader, burndown-chart buttons declare `type="button"` to avoid accidental form submits, and a scheduler Monte-Carlo sensitivity guard uses a robust `<= 0.0` norm check.
