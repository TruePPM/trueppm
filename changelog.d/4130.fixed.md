Scheduler: a non-string task `id` or `name` (and project `id`/`name`) is now rejected with `InvalidScheduleInput` instead of leaking a bare `TypeError`/`ValueError` or, for an integer id, silently scheduling in Python while the Rust engine refuses the same document.
