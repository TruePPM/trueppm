Released container images are no longer deleted by the registry cleanup policy. The policy matched every tag and protected none, so its daily sweep removed the published `api` and `web` images for `v0.3.0-alpha.3`. Release tags are now protected, and a nightly CI job (`registry:release-images`) fails red if the latest `v*` release tag is not pullable for both images.
