The hosted read-only demo now opens on a readable schedule instead of five bands of chrome over a sliver of Gantt (#4050 Part A). One 44px demo bar replaces the read-only notice, the sample-project strip, the how-to bar, the suggestions strip and the docked forecast bar — carrying the mode, the edition disclosure and its "What's included" link, a sample-project switcher, and a two-step landing hint. The demo opens in **Read** mode every time, even when the shared browser holds a previous visitor's Author preference, and an Author choice made during a visit lasts for the page session only. The opening framing is Fit, the unscheduled tray starts collapsed, and phases that are already complete open collapsed. A normal install is unchanged.
