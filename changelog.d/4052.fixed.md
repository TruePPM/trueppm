Fix the interactive demo's Board defaulting a Member visitor into the "My tasks" filter with no matching work, landing on an empty board ("No tasks assigned to you in this project yet."). The shared demo account owns no work, so "mine" has no meaning — the filter now defaults off in read-only demo mode. Role-based defaults and any explicitly stored preference are unchanged on a normal install.
