Hide the sample-project bar's "Manage demo data" link for the read-only interactive-demo visitor. It pointed at teardown/reset controls that always refuse under `demo_read_only`, and read as an admin affordance on the first screen a visitor sees.
