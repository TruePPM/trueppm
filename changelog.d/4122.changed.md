Raise the API's `djangorestframework-simplejwt` floor to 5.3.1 and `psycopg` to 3.1.8, the oldest releases that actually load, and add a CI job that installs the built wheel (fresh, locked and lowest-direct) and imports every API module.
