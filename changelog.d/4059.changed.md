Removed the `web:publish:npm` release job: it exited 0 without publishing on every tag because `NPM_TOKEN` was never configured, so `@trueppm/web` was never on npm. `@trueppm/web` is an application bundle rather than an importable library, so it is deliberately not published to npm; the web artifact is the Docker image (#4059).
