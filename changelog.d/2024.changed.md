- **Settings sub-pages now use a consistent page-body indent and bottom spacing.**
  Nine settings sub-pages that deviated from the shared `px-6 pb-8` body wrapper
  (Project Team, Signal & Privacy, Access, Project/Program Integrations, and the
  Workspace Email, Groups, Members, and Roles pages) were normalized, so the content
  no longer shifts horizontally or crowds the page bottom as you scroll between
  sections. The empty-state button on the workspace Integrations redirect now uses
  the shared accessible button component (#2024).
