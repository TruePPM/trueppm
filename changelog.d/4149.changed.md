- **Sample seed project keys**: the bundled sample programs' projects now carry
  explicit keys (e.g. `PLAT`, `SOC2`, `HELIOS`), so their risk and task IDs read
  qualified, such as `PLAT-R-7`, instead of falling back to the unqualified form.
