- **Program backlog UI** (`/programs/:id/backlog`): the program-backlog tab is now a working two-pane workspace (#742) — a sortable, filterable item list with status/type/tag facets and trigram-style title search, an inline detail/edit pane, and the **pull-down** flow that promotes a backlog item into a project's backlog (single target, optimistic with an 8-second undo). Includes a distinct mobile layout (card list + bottom-sheet detail/pull/create/filter), drag-to-reorder for proposed items, empty/no-results/error states, and full keyboard + screen-reader support. The list, search, and mutations are fixture-backed pending the ADR-0069 API (#737/#739); the data-layer hook signatures already match the API contract, so wiring them is a local swap.
