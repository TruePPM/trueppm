Raise the `trueppm-scheduler` floor in `trueppm-api` to `>=0.4.0b4,<0.5` and refresh `uv.lock`, so a locked install can import the app; `release.sh` now moves the floor with each release and a new `lint:api-scheduler-lock` gate fails when the lock trails the scheduler line.
