- **Gantt dependency arrows**: FS arrows now use orthogonal elbow routing (exit right → vertical drop → arrive at target left-edge) instead of cubic Bézier curves. Eliminates the looping, bar-crossing lines that appeared when source and target rows were close together.
