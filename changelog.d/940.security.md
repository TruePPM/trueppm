- **Published image SBOMs**: every released `api` and `web` container image now ships a CycloneDX SBOM (generated by Syft from the exact published image tarball) as a non-expiring release-pipeline artifact, alongside the existing Trivy vulnerability scan that fails a release on fixable HIGH/CRITICAL CVEs. Operators can audit the full dependency inventory of any image they run. Cosign signing and SBOM attestation of the public images/chart follow with GHCR image publishing.
