The Helm demo-mode `helm install` examples in the chart README and the Helm values reference now include the `networkPolicy.ingressControllerSelector` override that 0.4.0-beta.4 requires; copied as written, they previously failed to render.
