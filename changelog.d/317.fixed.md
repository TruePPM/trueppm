**Schedule view — Unscheduled gutter only shows actually-scheduling work** (#317): the gutter now lists only `NOT_STARTED` tasks that are not in a sprint and have no start date. BACKLOG ideas stay on the Board until promoted, and sprint-committed tasks are treated as already scheduled. `IN_PROGRESS` / `REVIEW` / `COMPLETE` tasks without dates surface a `⚠ missing dates` chip on the task list row so data-integrity issues are not silently hidden.
