Gantt timeline header labels are now visible when scrolled past the last task bar. The label for the current period (month, quarter, year) pins to the left edge of the viewport rather than rendering off-screen when the period boundary has scrolled behind the user.
