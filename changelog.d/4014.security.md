Closed programs are now read-only for backlog items, ceremony templates, and program memberships: `IsProgramNotClosed` scopes its `destroy`/`close`/`reopen`/`remove_sample` bypass to `ProgramViewSet` instead of matching the action name, so nested `DELETE`s on a closed program return 403 (#4014).
