Helm: the web pod now restarts when its rendered nginx config changes. Previously a config-only `helm upgrade` — such as turning on `demo.interactive`, or changing `demo.baseUrl` or a share token — updated the ConfigMap but left the running web pod serving the old config until something else restarted it.
