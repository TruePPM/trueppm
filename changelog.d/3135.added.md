- **ADR-0942 — the project rail is three verb bands and one scope band**: the decision
  record for the 0.4 project-navigation taxonomy (`PLAN` / `DELIVER` / `TRACK`, plus a
  ruled-off `WORKSPACE` scope band pinned to the bottom of the rail). It supersedes
  ADR-0128 §A's `PEOPLE` group, retains ADR-0195's methodology-adaptive placement,
  reaffirms ADR-0127's 0px collapsed rail, decouples per-user view hideability from band
  membership, and retires the per-user Schedule-in-Deliver placement preference. **No
  behavior changes with this record** — it is the design gate the implementation
  (#3136) and the preference removal (#3137) are built against.
