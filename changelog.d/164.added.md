- **Resource Timeline — overallocation week range**: resource row headers now display the overallocated ISO week range (e.g. "overallocated · W18" or "W17–W19") alongside the availability line, making it immediately clear which weeks are over-capacity without opening the utilisation heatmap.
- **Resource Timeline — status bar**: a 24 px footer beneath the timeline shows live resource count, total assignment count, over-allocated resource count, and a bar-variant legend (Normal / Partial / Over-allocated / Complete).
- **Resource Timeline — popover overflow fix**: the inline allocation-edit popover now renders above its bar without being clipped by adjacent resource rows; the track container uses `overflow-x: clip` + `overflow-y: visible` so the popover is always fully accessible.
