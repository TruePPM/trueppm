- **Settings layout shift**: navigating between settings sub-pages (Workspace, Program, and Project) no longer jumped the page sideways. The shared settings shell now reserves a stable scrollbar gutter on its content panel and sidebar, so a scrollbar appearing on a taller sub-page (e.g. Program General) can't shift the layout when you switch to a shorter one (e.g. Projects or Integrations).
