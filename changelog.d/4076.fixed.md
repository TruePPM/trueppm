The API Docker image no longer fails to boot with `ModuleNotFoundError: No module named 'requests'`. `requests` was only reaching the image transitively through the OpenTelemetry HTTP exporter; `django-allauth` is now installed with its `socialaccount` extra, which declares it.
