Timescale labels on the Schedule no longer collide or render cut off at coarse zoom (#4050 A5). The quarter cell now carries its fiscal year itself (`FY26 · Q3`), which frees the row below it for months (`Jul`) instead of repeating the year, and every label steps down to a shorter form — then to nothing — as its cell narrows, rather than being clipped mid-word. This applies to every install, not only the demo.
