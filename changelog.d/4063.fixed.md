The deployment guide's SBOM-attestation verification example now targets the per-platform image digest, which is where the multi-arch images carry their attestation, and documents that the `web` image for `0.4.0-beta.4` has no SBOM attestation.
