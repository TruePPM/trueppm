The interactive demo's nginx write fence now answers a refused write with the same JSON `demo_read_only` body as the API middleware instead of a stock HTML 403, so the web app's demo refusal surfaces work at the public edge; `helm test` now checks this through the web Service.
