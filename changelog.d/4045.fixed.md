`ci:pypi-mint-probe` now checks that the PyPI token it mints is scoped to `trueppm-api` instead of printing a fixed "valid" line, so a Trusted Publisher registered on the wrong project fails the probe rather than the real upload.
