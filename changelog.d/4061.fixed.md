Fixed `release.sh` reading only local tags for its duplicate-version guard: it now also refuses a version whose tags exist on `origin` or whose release exists on PyPI, and fails closed when either cannot be read.
