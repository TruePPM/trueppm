- **The task detail drawer's three-point estimate (Optimistic / Most Likely /
  Pessimistic) now batches behind the same Save / Cancel bar as name and
  description, instead of saving each field on blur.** Editing any of the three
  values stages the change, reveals the shared Save/Cancel footer, and marks the
  changed field as unsaved; the PERT expected-value and standard-deviation preview
  updates live as you type. Save persists all edits in a single request and Cancel
  reverts them. Estimates on the full-page task view continue to save immediately,
  and accepting a velocity-calibration suggestion is unchanged (#1985).
