The board's swimlane grouping is now available over the API at `GET /projects/{id}/board/lanes/`. It returns real container ids plus the project node — no synthetic catch-all lane — with the nested phase each card belongs to travelling alongside as a crumb. Previously this grouping existed only in the web app, so an MCP client, an agent, or any integration could not reproduce the board a person was looking at.
