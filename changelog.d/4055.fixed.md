The notifications "You're all caught up" empty state now shows a static check-in-circle instead of a radiating burst that read as a loading spinner, and the Unread + All view says "No unread notifications right now." instead of "mentions".
