New tasks created via the API without an explicit `wbs_path` are automatically assigned the next sequential root-level code (e.g. "1", "2", "3"). The assignment runs inside an atomic block with `SELECT FOR UPDATE` to prevent races under concurrent creates. Child tasks (paths containing ".") are excluded from the count so nested tasks do not shift root numbering.
