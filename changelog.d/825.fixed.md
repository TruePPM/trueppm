scheduler: `Task.free_float` is now computed across all four dependency types (FS/SS/FF/SF) per the PMI definition, rather than finish-to-start successors only. A task whose only successors are SS/FF/SF no longer reports `free_float == total_float` incorrectly.
