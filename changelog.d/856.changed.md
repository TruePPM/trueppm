Team members without admin access now see a focused **Notifications + Profile** settings view instead of the workspace/project admin pages (Methodology, Workflow, Roles, Groups). Gated on a new `can_access_admin_settings` signal from `/auth/me/` (#856, ADR-0118).
