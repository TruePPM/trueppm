/// <reference types="nativewind/types" />
// Augments React Native core component props with NativeWind's `className`.
// Present so feature screens can style with Tailwind classes; the scaffold
// itself styles via the typed src/theme/tokens.ts bridge + StyleSheet.
