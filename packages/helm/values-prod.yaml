# Overrides for production deployment.
# postgresql and valkey are managed externally (RDS, ElastiCache, MemoryDB, or a
# managed Valkey service) and their connection details are injected via
# Kubernetes Secrets / external-secrets.
replicaCount: 2

postgresql:
  enabled: false

valkey:
  enabled: false

ingress:
  enabled: true
