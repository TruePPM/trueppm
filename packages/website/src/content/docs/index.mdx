---
title: TruePPM
description: Most project tools store dates. TruePPM computes them — a real CPM engine with Monte Carlo risk analysis, Apache 2.0, self-hosted, and installable on its own so you can verify the math before you trust the platform.
template: splash
hero:
  tagline: |
    Most project tools store dates. TruePPM computes them — a real Critical Path Method engine with Monte Carlo risk analysis, wrapped in a platform where the board card and the Gantt bar are the same object. The engine installs on its own, so you can check the math before you trust anything else here.
  actions:
    - text: Verify the engine (60s)
      link: /features/scheduler/
      icon: right-arrow
      variant: primary
    - text: Get Started
      link: /getting-started/installation/
      icon: right-arrow
      variant: secondary
    # Hosted read-only demo (#1487, #1775). The try.trueppm.com instance is not
    # live until the 0.4 tag, so the hero routes to the /try-it/ preview page
    # (which explains the status and the verified path today) rather than the
    # placeholder domain, which would 404 pre-tag.
    - text: Try it (0.4 beta)
      link: /getting-started/try-it/
      icon: right-arrow
      variant: minimal
    - text: View on GitLab
      link: https://gitlab.com/trueppm/trueppm
      icon: external
      variant: minimal
---

import { Card, CardGrid, Badge } from "@astrojs/starlight/components";
import { SHIPPED, SHIPPED_TAG, SHIPPED_DATE, FIRST_BETA, UNDERWAY_PRERELEASE_TAG } from "../_release-status.mdx";

{/* Banner derives its version from the release-status SSOT (#807). The
    admonition label is plain directive text and cannot interpolate, so it
    stays in sync with SHIPPED by hand; the body interpolates the constants. */}
:::caution[0.3 shipped (alpha) · pre-GA]
TruePPM {SHIPPED} shipped {SHIPPED_DATE} (as the <code>{SHIPPED_TAG}</code> pre-release) — the scheduling engine, REST API, real-time backend, the full web UI (Schedule, Board, Sprints, Risk, My Work, Programs), the settings/administration platform, program foundations, board and schedule depth, the first import/export migration path, and the agile-team feature set (first-class sprints, dual backlog, epic hierarchy, and the sprint↔schedule bridge) on the v2 interface refresh are all functional. {SHIPPED} is an alpha release; the first beta is planned for {FIRST_BETA}. {FIRST_BETA} arrives as a beta directly — the next tag on the line is <code>{UNDERWAY_PRERELEASE_TAG}</code>, with no alpha step in between (<a href="/overview/roadmap/#how-the-04-line-is-numbered">how the {FIRST_BETA} line is numbered</a>). The product is pre-GA: API contracts may evolve across 0.x point releases. Suitable for evaluation and early-adopter deployments; expect breaking changes before 1.0.
:::

## Start with the engine, not the platform

You should not have to install a platform to find out whether its scheduling is real. TruePPM's engine is a **separate Apache 2.0 Python package** — the same code the platform runs on, versioned in lockstep. No server, no database, no account:

```bash
pip install trueppm-scheduler
```

```python
from datetime import date, timedelta
from trueppm_scheduler import schedule, Project, Task, Dependency

result = schedule(Project(
    id="p-1", name="Verify me", start_date=date(2026, 1, 5),
    tasks=[
        Task(id="design", name="Design", duration=timedelta(days=5)),
        Task(id="build",  name="Build",  duration=timedelta(days=10)),
        Task(id="docs",   name="Docs",   duration=timedelta(days=3)),
    ],
    dependencies=[Dependency(predecessor_id="design", successor_id="build")],
))

print(result.critical_path)                                            # ['design', 'build']
print(next(t for t in result.tasks if t.id == "build").early_finish)   # 2026-01-23
print(next(t for t in result.tasks if t.id == "docs").total_float)     # 12 days, 0:00:00
```

Fifteen working days across two weekends, computed on a Mon–Fri calendar — and `docs`, off the critical path, carries twelve days of slack before the date moves. Check it against MS Project. That is why the engine ships on its own.

All four dependency types, lead/lag on every link, multi-calendar composition, cycle detection with the offending path, and Monte Carlo with per-task sensitivity. [Full engine reference →](/features/scheduler/) · [View on PyPI →](https://pypi.org/project/trueppm-scheduler/)

Want the whole platform, not just the engine? [Install with Docker Compose](/getting-started/installation/) — one command, five services, running in a few minutes — or read [What TruePPM's evaluators check first](/getting-started/evaluation-guide/) for a guided tour. Nothing to install yet? [Try it](/getting-started/try-it/) covers the zero-config paths.

:::tip[Before you evaluate further]
Two pages exist so you can disqualify TruePPM quickly rather than slowly:
[**How TruePPM compares**](/overview/how-it-compares/) names competitors with dated
citations — including the head-to-heads TruePPM loses — and
[**What TruePPM doesn't do yet**](/overview/what-it-does-not-do/) is the maintained
gap list (no resource leveling, one constraint type, no cost/EVM, a ~1,000-task
Schedule-view ceiling).
:::

## Why TruePPM?

Five claims, each one checkable rather than taken on trust — and one page of the ones that go the other way.

<CardGrid>
  <Card title="Monte Carlo, in the free core" icon="random">
    Three-point estimates give you P50/P80/P95 plus a per-task sensitivity ranking — which tasks actually move the finish. Across the commercial field this is a separately licensed product (Acumen Risk, Full Monte, Safran Risk, Primavera Risk Analysis) bolted onto an exported schedule. No other open-source PPM platform ships it at all. [The comparison →](/overview/how-it-compares/)
  </Card>
  <Card title="The engine is separable" icon="puzzle">
    `pip install trueppm-scheduler` — CPM and Monte Carlo, no server, no account, the same code the platform runs on. No other PPM tool, open or commercial, lets you audit its scheduling engine in isolation.
  </Card>
  <Card title="Board card = Gantt bar" icon="rocket">
    Not synced. Not mirrored. The same row. MS Project has no agile surface; Jira has no critical path; OpenProject keeps board and Gantt as adjacent views. Here, a contributor drags a card to Done and the project manager's finish date moves — and closing a sprint reforecasts the schedule from measured velocity. [The full story →](/the-story/)
  </Card>
  <Card title="Nothing that should be free is taxed" icon="open-book">
    Apache 2.0, no seat count, no feature flags, no telemetry. SSO through your own identity provider ships in the OSS core at 0.4 — OpenProject gates OIDC behind Enterprise, Plane behind Commercial. [SSO is not an enterprise feature →](/overview/sso-is-not-enterprise/)
  </Card>
  <Card title="Computed, not guessed" icon="approve-check">
    Every date and forecast is calculated by the engine with a derivation to show for it — which is what makes the schedule safe to hand an AI. An agent can **compute**, **cite**, be **refused**, and **reproduce**. The read-only MCP server lands in the 0.4 beta. [How grounding works →](/overview/computed-not-guessed/)
  </Card>
  <Card title="…and what it doesn't do" icon="warning">
    No resource leveling. One constraint type. No cost or earned value until 0.8. A measured ~1,000-task ceiling in the Schedule view. If you need automatic leveling today, use P6. The full maintained list: [What TruePPM doesn't do yet →](/overview/what-it-does-not-do/)
  </Card>
</CardGrid>

## Who is it for?

| Persona | Methodology | TruePPM gives them... | Guide |
|---------|-------------|----------------------|-------|
| **Project Managers** | Waterfall / Hybrid | CPM schedules, Monte Carlo confidence intervals, critical path, velocity-suggested Gantt durations (automatic reforecast added in 0.3) | [Guide →](/guides/project-managers/) |
| **Scrum Masters** | Agile / Hybrid | Sprint board, WIP limits, velocity tracking, burndown, retrospective-to-backlog automation | [Guide →](/guides/scrum-masters/) |
| **Product Owners** | Agile / Hybrid | Backlog management, velocity-based release forecasting, sprint scope protection | [Guide →](/guides/product-owners/) |
| **Team Members** | Any | Board-first task view, My Work cross-project surface, real-time updates, offline sync | [Guide →](/guides/team-members/) |
| **Resource Managers** | Any | Per-project capacity preflight, allocation tracking; cross-program leveling in Enterprise | [Guide →](/guides/resource-managers/) |
| **PMO Directors** | Any | Program coordination; portfolio health scores, demand intake, governance in Enterprise | [Guide →](/guides/executives/) |
| **Executives** | Any | Monte Carlo confidence intervals per project; portfolio RAG status, board-ready reports in Enterprise | [Guide →](/guides/executives/) |

For everything shipped so far, release by release, see the [roadmap](/overview/roadmap/).

## Next steps

<CardGrid>
  <Card title="Install" icon="setting">
    [Set up a local instance](/getting-started/installation/) with Docker Compose in under 5 minutes.
  </Card>
  <Card title="Try the API" icon="document">
    [Create a project and schedule](/getting-started/quickstart/) — tasks, dependencies, and CPM results via curl.
  </Card>
  <Card title="Find your guide" icon="star">
    [Project managers & waterfall PMs](/guides/project-managers/), [team members & agile teams](/guides/team-members/), [resource managers](/guides/resource-managers/), [executives & PMO directors](/guides/executives/).
  </Card>
  <Card title="Evaluate" icon="magnifier">
    [Architecture overview](/architecture/overview/) and [API reference](/api/reference/) for technical evaluation.
  </Card>
</CardGrid>
