Importing or loading a sample program no longer 500s when the shared catalog already holds two calendars sharing a name, or a duplicate calendar exception (holiday/PTO range) already exists — the seed importer now reuses the existing row instead of raising.
