"""Agents app — the OSS agent-as-actor extension point (ADR-0112).

Phase 0 (#1805) ships the append-only, hash-chained ``AgentAction`` audit
record and its ``record_agent_action`` service. The fuller agent-actor identity
model and capability scopes are Phase 1 (#1063, 0.5).
"""
