# TruePPM Web Frontend — Design Rules

These rules are enforced at review time. Violations block merge.

> ## ⭐ Design System v2 — the golden standard (ADR-0126)
>
> The canonical UI is the **v2 redesign** (`docs/design/v2-golden-standard.md`,
> upstream `design_handoff_trueppm_v2/`). Every new screen, component, fix, and
> feature must conform. Foundation tokens are in `globals.css` + `tailwind.config.ts`;
> conformance is machine-checked by `scripts/check-design-system-v2.sh` (in `make lint`
> / CI). Key invariants beyond the numbered rules below:
> - **Warm-paper canvas:** page backgrounds use `bg-app-canvas` (warm `#F2EEE5`),
>   cards use `bg-neutral-surface` (white) and pop against it. Never a cool-grey or
>   pure-white app canvas.
> - **Sage has two roles (ADR-0126 §3):** `--sage` `#3E8C6D` is the **fill/accent**
>   (with navy or white text); normal-weight **text/border/ring** use
>   `brand-primary` (sage-700, AA). sage-600 is 4.06:1 on white — never body text.
> - **One status vocabulary:** a **dot** = health, a **pill/chip** = state; color is
>   signal only, calm-neutral by default.
> - **Borders over shadows:** `shadow-card`/`shadow-pop` are reserved for
>   popover/drawer/modal/command-palette/toast (see rule 1).
> - **Full Light/Dark/Auto** via the `.dark` token swap on `<html>` — never a dark
>   sidebar on a light app.
> Staged adoption of the remaining surfaces is tracked in epic #1163.

## Layout & Visual

1. **No drop shadows anywhere** — use `border border-neutral-border` for separation instead of `shadow-*`
2. **Sidebar collapse animation** — `transition-[width] duration-200 ease-out` on the sidebar element itself; do not animate `grid-template-columns`
3. **Bottom nav rail replaces view tabs at `< 768px`** — never show both simultaneously

## Accessibility (WCAG 2.1 AA)

4. **Focus rings on all interactive elements**: `focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-1`
   - On a **sage fill** (primary button, `bg-sage-500`): use `focus-visible:ring-navy-700 focus-visible:ring-offset-sage-500` (navy-on-sage 6.8:1).
   - **No dark-mode override needed for `brand-primary` (ADR-0103/0125).** `brand-primary` is mode-aware sage — sage-**700** #316F57 (light, 5.93:1) / sage-400 #66B998 (dark, ≥3:1) — so `ring-brand-primary` passes WCAG 1.4.11 in both modes. (sage-600 #3E8C6D is only 4.06:1 on white — a fill/dot shade, never the foreground/ring token; see rule 143 and ADR-0126 §3.) The old `dark:focus-visible:ring-semantic-on-track` escape hatch (for green #1C6B3A's 2.81:1 dark failure) is **retired** — do not reintroduce it.
   - Never use `outline-none` without a visible replacement
5. **Touch targets** — minimum 44×44px at all breakpoints
6. **Color dots** (8px project color indicators) are always `aria-hidden="true"` — health state must also be conveyed via text or `aria-label`

## Color & Semantic Tokens

7. **Health state color encoding**:
   - On-track → `text-semantic-on-track` / `bg-semantic-on-track`
   - At-risk → `text-semantic-at-risk` / `bg-semantic-at-risk`
   - Critical → `text-semantic-critical` / `bg-semantic-critical`
   - Unknown → `text-neutral-text-disabled`
8. **No custom hex colors in components** — always use Design System tokens from `tailwind.config.ts`. The `design-system-v2` gate (`scripts/check-design-system-v2.sh`) ratchets hex **color literals** — a `#hex` that is quoted (`'#f59e0b'`), in a Tailwind arbitrary value (`bg-[#…]`), or a CSS value (`stroke: #fff`). It does **not** count a bare `#1236`-style issue reference in a comment, so cite issue numbers freely — do **not** rewrite them to "issue N" to appease the gate (that was a workaround for an old false positive, now fixed). If the gate fails, you added a real hardcoded color: tokenize it, don't launder the number.

8a. **`--chrome-*` tokens are for fixed UI furniture** (sidebar, Schedule task-list panel, TopBar) that follows the active theme. `--neutral-*` tokens are for page content surfaces. Use `bg-chrome-surface` on shell chrome, `bg-neutral-surface` on content areas. Both adapt with the `.dark` class on `<html>`. In dark mode `--chrome-surface` equals the legacy `gantt-surface` deep navy (#0F1117). Chrome tokens: `surface`, `surface-raised`, `border`, `text-primary`, `text-secondary`, `row-hover`, `row-active`, `grid`.

8b. **`--sem-*-bg` tokens are for badge pill fills and status cards** — `bg-semantic-critical-bg`, `bg-semantic-at-risk-bg`, `bg-semantic-on-track-bg`, `bg-semantic-warning-bg`. These are pre-computed RGBA values so they cannot be combined with Tailwind's opacity modifier. Always pair with the matching full semantic token for text/border (e.g. `border-semantic-at-risk/80 bg-semantic-at-risk-bg text-semantic-at-risk`). **Never substitute `bg-semantic-{state}/N` (the opacity modifier) for `bg-semantic-{state}-bg`** — `/N` applies Tailwind's alpha to the token's RGB channels and will NOT match the pre-computed dark-mode RGBA in `globals.css`, so a `/10` badge fill silently diverges from the design system in dark mode. The `-bg` tokens are the only correct form for semantic badge/pill fills. (Surfaced by ux-review on #691.)

8c. **`.tppm-mono` applies JetBrains Mono with tabular numerals** — use it on every numeric value: KPIs, percentages, dates, durations, counts, build hashes. It is defined as a Tailwind utility in `globals.css` and maps to `font-family: 'JetBrains Mono'; font-feature-settings: "tnum"`.

8d. **`bg-neutral-overlay` is the scrim token for a modal/slide-out's full-screen backdrop** — never a raw `bg-black/N` literal. Like the `--sem-*-bg` family (rule 8b), it is a pre-computed RGBA custom property (`rgba(0, 0, 0, 0.4)` in both light and dark — the scrim doesn't get a dark-mode-specific treatment) and therefore **cannot be combined with Tailwind's `/N` opacity modifier**; write `bg-neutral-overlay` alone, not `bg-neutral-overlay/40`. Popover/edge scrims that intentionally use a different opacity for a lighter touch (e.g. a mobile drawer's `/30` edge, a cheatsheet's `/20`) are a distinct visual role, not this token — `bg-neutral-overlay` is specifically for the `role="dialog" aria-modal="true"` full backdrop. (Surfaced by ux-review follow-up, issue 575.)

## Code Conventions

9. **No default exports** — all components use named exports
10. **No CSS-in-JS** — Tailwind utility classes only; use `style` prop only for dynamic values (e.g., CSS custom properties, inline widths derived from state)
11. **Stub hooks over mock network** — while real API hooks don't exist, return fixture data from the hook (`src/hooks/`). Components never import from `src/fixtures/` directly.
12. **Responsive breakpoints** (from `tailwind.config.ts`): `xs`=320px, `sm`=375px, `md`=768px, `lg`=1024px, `xl`=1280px, `2xl`=1440px

## Gantt-Specific Rules

13. **Gantt bar label text is `#1A1917`** (`neutral-text-primary`) — the Schedule view uses a light surface. Canvas bar label: `ctx.fillStyle = COLOR.text` from `GanttRenderer.ts`. SVAR CSS var: `--wx-gantt-task-color: #1a1917`.
14. **Gantt bar heights**: normal/critical/complete = 18px; summary = 8px; milestone diamond = 12px; baseline ghost = 6px.
15. **Task list row height**: 28px fixed — required for scroll sync with SVAR's internal row height.
16. **`readonly={true}` on `<Gantt>`** until WASM CPM drag (issue #19) is implemented — prevents partial drag UX.

## Monte Carlo Row Rules

17. **MC row height is 44px** — outside the virtualizer; does not participate in scroll sync (not 28px like task rows).
18. **No always-visible mini-histogram strip in the MC row** — the always-visible surface is chips-only (`P50 {date}` · `P80 {date}` · `P95 {date}`) plus a "Detail ›" hint. Real-world inputs without PERT estimates collapse to a single histogram bar that misleads more than it informs; the chips are the persona-aligned signal. Distribution shape lives only in the hover/focus tooltip (desktop) and the bottom-sheet (mobile).
19. **MC histogram SVG bars in the tooltip use `fill-neutral-text-disabled`** — distribution shape is neutral; semantic colours are reserved for the P50/P80/P95 vertical rule lines inside the tooltip. The same rule applies to the histogram inside `MonteCarloSheet` (mobile) and `MCResultPanel` (TopBar P80 click).
20. **P50 / P80 / P95 date chips are permanently visible** in the `MonteCarloTimeline` row — outlined style (`bg-transparent border border-{semantic}/40 text-{semantic}`), not fill. Hover or keyboard-focus opens the detailed histogram tooltip; chip text is the WCAG 1.4.1 fallback so colour is never the sole signal.
21. **P80 badge uses outlined style** — `bg-transparent border border-semantic-at-risk/40 text-semantic-at-risk`. Not `bg-semantic-at-risk/10` fill. Consistent with rule 39.
22. **MC row (`MonteCarloRow`) is `hidden md:flex`** — suppressed below 768px. The P80 badge in `TopBar` is `hidden md:flex` (desktop only). Mobile surfaces P80 via a chip in `StatusBar` (`md:hidden`) — resolved by issue #33. `MonteCarloLabel` is text-only (σ + "Monte Carlo") — the previous left-side P80 chip was a duplicate of the right-side P80 chip in the timeline and was VoC-flagged as noise.

22a. **MC row uses a browser-native `title`, not a custom popover.** The plain-English explanation (`"8 in 10 simulations finish by {date}"` for real distributions, `"Every simulation finished on {date}. Add PERT estimates …"` when percentiles collapse to one date) is carried by the `title` attribute on the row's static `div`, mirrored to `aria-label` for screen readers. The previous `mouseenter`-triggered popover opened on cursor pass-through and overlapped the unscheduled gutter sitting directly above; native `title` doesn't fire on transient hovers and never positions over adjacent elements. The full histogram lives in `MCResultPanel` (TopBar P80 click) and `MonteCarloSheet` (mobile) — surfaces where the user has explicitly asked for distribution shape.

22b. **MC chips use a colon separator** — `P50: Nov 30`, `P80: Nov 30`, `P95: Nov 30`. Consistent across `MonteCarloTimeline` and `MobileMonteCarloCard`. Never render the label and date with only a space.

## Drag Preview Rules (Issue #19)

23. **Preview bars use `ghost-fill` / `ghost-border` tokens** — slate-500 at 12% / 55% opacity. No ad-hoc rgba() in components; values are defined in `tailwind.config.ts` only. Applied via `style` prop (rule 10 — dynamic values).
24. **Call them "preview bars" in code and comments** — not "ghost bars". Baseline ghost bars (6px, rule 14) are a distinct concept; the naming must not collide.
25. **Critical preview bar: `ghost-border` → `semantic-critical` border only; fill stays `ghost-fill`** — a red fill on a provisional bar is alarming. Border alone signals critical-path flip.
26. **Critical-path flip requires a non-color signal** — a "CP" badge (min 400ms) must accompany the `semantic-critical` border change. Color alone fails WCAG 1.4.1.
27. **Preview overlay is `pointer-events-none aria-hidden="true"`** — never intercepts pointer events; all drag events route through SVAR.
28. **"Esc to cancel" label is mandatory during drag** — render a small `aria-hidden="true"` label adjacent to the dragged bar. Remove on pointerup or Escape.
29. **Offline guard before drop commit** — check `navigator.onLine` before PATCH. If offline: skip success animation, clear preview bars immediately, show toast "You're offline — change not saved."
30. **`aria-live` region is written via DOM ref, not React state** — prevents re-render storms on every pointermove event.
31. **MilestoneDeltaTooltip mounts at GanttView level** — not inside GanttTimeline, to escape `overflow:hidden`.
32. **Cap preview bars at 10** — show "+N more affected" count label for the remainder.
33. **Preview bars animate out only (150ms opacity, `motion-safe` only)** — they appear immediately; animation on entry causes flicker during fast drags.
34. **Keyboard drag alternative is a known WCAG 2.1.1 gap** — tracked in issue #34. Do not claim WCAG 2.1 AA conformance for the Gantt feature until #34 is resolved.

## UI Harmonization Rules (Issue #44)

35. **Sidebar background is `bg-chrome-surface`** — the adaptive chrome token (warm off-white in light mode, deep navy in dark mode via CSS custom properties). `bg-brand-primary` is reserved for interactive elements (buttons, focus rings, active accents) — never large-area backgrounds.

36. **Sidebar section headers** (`PROJECTS`, `ORG`) use `text-xs font-semibold tracking-widest uppercase text-chrome-text-secondary`. Minimum size is `text-xs` (12px) — `text-[10px]` is prohibited; fails WCAG 1.4.3. They are `<h2>` elements with `aria-label` matching the visible text. Hidden when sidebar is collapsed.

37. **Sidebar active-row indicator** is a 2px left border (`border-l-2 border-brand-primary`) in addition to `bg-brand-primary/10`. The border is the primary non-color visual signal; background fill is secondary. Never use border alone without the fill.

38. **ViewTabs active state uses underline** — `border-b-2 border-brand-primary` — not pill/outlined. Pill style is reserved for the secondary Gantt toolbar (rule 42) to maintain a clear visual hierarchy between top-level navigation and sub-view options. If a pill border is ever used for tabs, it must meet WCAG 1.4.11 3:1 against `bg-neutral-surface`; `border-neutral-border` (#D4D2CE) fails at ~1.51:1.

39. **TopBar status badges use outlined style** — `bg-transparent border border-{semantic-color}/40 rounded px-2 py-0.5 text-xs`. Badge labels include the full semantic word: `{n} at risk`, `{n} critical`, `P80: {date}`. Labels must also specify scope (tasks vs. projects) — ambiguous counts are a PMO compliance risk. `aria-label="{n} at risk tasks"` or `"{n} critical tasks"`. At-risk and critical badges are `<button aria-haspopup="menu">` elements — NOT `listbox` (listbox implies selection, not navigation). They open a `role="menu"` popover with `role="menuitem"` task entries.

40. **Schedule view adapts to color scheme** — in light mode the task-list panel and canvas use `bg-neutral-surface` (#FFFFFF); in dark mode they use the dark navy surface (#15223C, ADR-0103). The canvas renderer switches palettes via `setRendererColorMode(isDark)` called from `GanttEngineImpl` before each paint pass. `CanvasGanttTimeline` derives `isDark` from `useThemeStore` and passes it to `useGanttEngine`. `COLOR` (light) and `COLOR_DARK` (dark) palettes in `GanttRenderer.ts` are the canonical color sources; no hex literals in component files.

42. **GanttToolbar view-switcher** (Gantt · WBS · Table) uses `role="group" aria-label="View mode"` with `aria-pressed` on the active item. Action buttons (+ Task · Baseline · Monte Carlo) are plain `type="button"` elements. All toolbar buttons: `border border-neutral-border rounded h-7 px-3 text-xs font-medium`. WBS and Table render as `disabled aria-disabled="true"` until their panels are implemented.

43. **Gantt column layout** — the task list has five resizable columns: **Task** (name + WBS indent), **Dur** (duration in days, e.g. `14d`), **Start** (early start, e.g. `Apr 9`), **Finish** (early finish, e.g. `Apr 21`), **%** (progress, text only — no mini bar). Column widths are persisted in localStorage under `trueppm.gantt.columnWidths.v4`. Default widths: task=220, dur=52, start=74, finish=74, progress=44. Full-height `border-r border-neutral-border/20` dividers appear on Task, Dur, Start, and Finish columns; the header resize-handle indicator is right-aligned within its hit zone to align with the row borders. Dur/Start/Finish/% visibility is persisted in localStorage under `trueppm.gantt.columnVisibility.v1` and toggled via the Columns popover in the toolbar.

44. **StatusBar layout** (issue #201 — overrides the old Gantt-legend footer design): 24px height, `bg-neutral-surface-sunken border-t border-neutral-border`, `hidden md:flex`. Three regions left-to-right: (1) live dot (`bg-semantic-on-track`) + `Live · {N} online` count from `useProjectPresence`; (2) `build {sha}` in `.tppm-mono` from the `__BUILD_SHA__` compile-time constant; (3) spacer + status note `{project.name} · {activeView}` in `.tppm-mono`. Hidden on Login (not inside AppShell) and on viewports < 768px.

45. **StatusBar text size is `text-[11px]`** — this is a deliberate override of the 12px floor (rule 50) for the status bar only. 11px matches the design spec for this single component; do not apply `text-[11px]` elsewhere. The exception is documented here because the design system floor is 12px everywhere else.

46. **Focus rings in the Schedule view** use `focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-1 focus-visible:ring-offset-neutral-surface` — same as the global rule 4 pattern. No special dark-surface override needed.

47. **Monte Carlo row is role-gated** — hidden for the Contributor RBAC role; visible for PM, Resource Manager, PMO Director, and Executive roles. Use a role-check hook (`useCurrentUserRole()`) to gate the `MonteCarloRow` render, not CSS visibility. Contributor-role users must not see P50/P80/P95 terminology without context.

48. **Export / print surfaces force the light theme island — never assume the app is in light mode (issue #1683, corrects the pre-dark-mode assumption).** A PDF/PNG export surface (`SchedulePrintLayout`, `BoardPrintLayout`) is a deliberately LIGHT document (white sheet, navy ink) but is mounted in the live app DOM and rasterized with `html-to-image`, so it inherits `html.dark` when the exporter is in dark mode: the sheet stays a fixed `bg-white` while every CSS-variable token (`text-neutral-text-primary`, `bg-neutral-surface`, `border-neutral-border`, `semantic-*` fills) resolves to its **dark** value, and the rasterizer captures near-white ink on a white page (WCAG 1.4.3 fail — the original bug). The root of any export/print surface MUST carry the **`.theme-light`** class (the token island in `globals.css` — the mode-paired `:root` token blocks extend to `:root, .theme-light`, so the light values are single-sourced and re-asserted on the subtree regardless of the ancestor `.dark`). This supersedes the earlier "the Schedule view already uses a light surface; no override needed" note, which predated the Schedule view adopting real dark theming (rule 40 / ADR-0103). Still ensure `@media print` does not reintroduce dark browser-default backgrounds. When adding a new client-rasterized export surface, apply `theme-light` to its root and assert it in the layout's unit test.

49. **Critical-path red requires a plain-English tooltip** — `title="This task is on the critical path — a delay here delays the project end date"` on every red task row. Color alone (WCAG 1.4.1) and a legend entry are insufficient for first-time users; the tooltip is the accessible fallback.

50. **`text-[10px]` is prohibited** — the design system floor is `text-xs` (12px). Arbitrary size values below 12px bypass the token ladder and introduce WCAG 1.4.3 failures on any surface. Exception: `text-[11px]` is permitted in the global StatusBar only (rule 45 documents the rationale).

## Keyboard Reschedule Rules (Issue #34)

51. **Keyboard instruction strip is mandatory during keyboard reschedule** — render
    `"← → Shift+arrow · d date · Enter confirm · Esc cancel"` in the `PreviewOverlay`
    instruction strip when `isKeyboardMode` is `true`. The mouse-drag strip `"Esc to cancel"`
    must remain unchanged (rule 28). Both are `aria-hidden="true"` — the assertive aria-live
    region (rule 53) carries the accessible equivalent.

52. **Origin ghost bar is required during keyboard reschedule** — show a dashed 2px
    `ghost-border` outline bar at the task's pre-nudge start/finish position so the user
    has a spatial reference point. Uses `OriginBar` in `PreviewOverlay`; only visible when
    `isKeyboardMode` is `true` (SVAR renders its own drag shadow for pointer drags).
    Fill is `transparent`; no CP badge; no label. Bar height follows rule 14 (18px).

53. **Assertive aria-live region is required for keyboard reschedule** — a second
    `aria-live="assertive"` region (`ariaAssertiveRef` in `GanttView`) must announce each
    nudge immediately: `"{N} working day{s} later/earlier"` or `"Back to original start date"`.
    Confirm announces `"Reschedule confirmed."`, cancel announces `"Reschedule cancelled."`,
    mode-entry announces the task name and key legend. The existing polite region
    (`ariaLiveRef`) continues to handle milestone slip messages. Never merge assertive and
    polite into one region — the polite queue delay makes nudge feedback unintelligible.

## Canvas Renderer Rules (feat/gantt-canvas-renderer)

> These rules govern the purpose-built canvas Gantt renderer that replaces
> @svar-ui/react-gantt. SVAR-specific rules (16, 15's "scroll sync with SVAR",
> 27's "route through SVAR") are superseded by the rules below for any code
> inside `src/features/gantt/engine/` or that imports from it.

### Architecture

54. **`GanttEngine` is the sole integration boundary** — `GanttView`, `useDragCpm`,
    `useKeyboardReschedule`, `PreviewOverlay`, and `MonteCarloTimeline` hold a
    `GanttEngine | null` reference and nothing else. No component may import from
    `GanttEngineImpl` or any engine sub-module directly — only through
    `src/features/gantt/engine/index.ts`. Violations break the stable API contract.

55. **`GanttEngine.on()` always returns an unsubscribe function — always call it.**
    Every `engine.on(event, handler)` call must be paired with the returned teardown
    in a `useEffect` cleanup. Do not use `engine.on()` outside of a `useEffect`.
    This fixes the SVAR `intercept()` memory leak (handlers accumulated on remount).

56. **`GanttScaleData` is the canonical coordinate system** — `dateToLeft`,
    `leftToDate`, and `parseUTCDate` from `engine/GanttScaleData.ts` are the only
    permitted coordinate utilities. Do not use SVAR's `scales.diff()`, the old
    `dateFromCanvasLeft`, or any millisecond-approximation math. The new functions
    are DST-safe (UTC-only arithmetic).

57. **`dateToLeft` returns canvas-origin coordinates** — the result is px from the
    canvas x=0 origin, not viewport-relative. Subtract `engine.scrollLeft` when
    positioning DOM overlay elements (e.g. `PreviewOverlay`, `MilestoneDeltaTooltip`).
    Pass canvas-origin coordinates directly to CPM workers and drag event handlers.

58. **`GanttEngineStub` is the only permitted test double for `GanttEngine`** — do not
    hand-roll mock objects with `{ on: vi.fn(), scales: null, ... }`. The stub is a
    typed class that will fail to compile if the interface changes, surfacing test
    staleness immediately.

### Canvas Rendering

59. **Three-layer canvas stack — one responsibility each:**
    - `canvas-bg` (z-index 0): row bands, grid lines, today line, weekend shading. Rarely repaints.
    - `canvas-bars` (z-index 1): task bars, dependency arrows, float bars, baseline ghosts. Dirty-rect per row.
    - `canvas-interaction` (z-index 2): active drag shadow, resize highlight, link-creation preview. Cleared completely between frames.
    Never draw task bar content on `canvas-bg` or interaction chrome on `canvas-bars`.

60. **Dirty-rect invalidation — never full-repaint during drag.** On each drag-move
    only the dragged row and the rows containing CPM preview results are invalidated.
    A 500-task project must repaint ≤ 11 rows per frame during a typical drag.
    Full repaints are only permitted on: zoom change, scroll > 1 viewport width,
    window resize, baseline mode toggle.

61. **Row virtualisation is mandatory — always.** The renderer only paints rows
    whose `top` falls within `[scrollTop - overscan, scrollTop + viewportHeight + overscan]`
    where `overscan = 5 rows`. This must hold from the first commit — never paint
    all rows and optimise later. Phase 1 (≤500 tasks) and Phase 2 (2,000 tasks)
    use the same virtualisation path.

62. **`devicePixelRatio` scaling is applied once at canvas init and on `ResizeObserver`.**
    All logical coordinates (bar positions, hit zones, font sizes) use logical pixels.
    The canvas backing store is scaled by `window.devicePixelRatio`. Never scale
    individual draw calls — scale the context once via `ctx.scale(dpr, dpr)`.

63. **Hit testing uses a spatial index — never per-pixel color mapping.**
    The `HitIndex` array is indexed by `rowIndex` and rebuilt on data change or
    zoom change (O(n), < 1ms for 2,000 tasks). Hit zones per row:
    - Bar body: `[barLeft, barRight]` × `[barTop, barBottom]`
    - Resize handle: `[barRight - 8, barRight + 4]` logical px (expand to 20px on touch)
    - Link-dot: `[barRight + 4, barRight + 16]` × full row height (expand to 44px on touch)

### Interaction

64. **Drag FSM has 5 states: IDLE → HOVER_WAIT → DRAG_STARTED → DRAGGING → DROP/CANCELLED.**
    The 4px movement threshold between HOVER_WAIT and DRAG_STARTED prevents
    accidental drags on clicks and tap-and-hold on iPad. `setPointerCapture` is
    called at DRAG_STARTED to ensure `pointermove` fires outside the canvas.

65. **Snap-to-day is applied inside the renderer before emitting `drag-task-move`.**
    The `left` value in the event payload is always snapped to the nearest UTC
    midnight boundary. Holding Shift suspends snap (free-drag). `useDragCpm` must
    not snap independently — do not double-snap.

66. **Use the Pointer Events API throughout — not Mouse Events or Touch Events.**
    `touch-action: none` on all canvas elements. Pinch-to-zoom is handled via two
    simultaneous active pointers (pointer span delta). This unifies mouse and touch
    without branching.

### Accessibility

67. **`GanttAriaOverlay` is mandatory — canvas is `aria-hidden="true"`.**
    A transparent DOM layer (`position: absolute; inset: 0; pointer-events: none`)
    provides the WCAG 2.1 grid structure over the canvas. Structure:
    `role="grid"` > `role="row"` > `role="gridcell"`. The overlay is virtualised
    to match the canvas render window. Canvas elements have no ARIA attributes.

68. **ARIA grid uses roving tabindex.** The focused `gridcell` has `tabIndex={0}`;
    all others have `tabIndex={-1}`. When the focused row scrolls out of the
    virtualised window, `engine.scrollToDate()` is called before `.focus()`.
    Keyboard navigation (ArrowUp/Down, Enter, Space) is handled in the overlay
    component, not in the canvas event listeners.

69. **`buildTaskAriaLabel(task)` format is canonical:**
    `"{name}, {durationDays} days, starts {start}, finishes {finish}, {CP status}"`
    e.g. `"Design sprint, 10 days, starts Apr 7, finishes Apr 18, on the critical path"`.
    Used as `aria-label` on the focused gridcell. The canvas bar is `aria-hidden`.

70. **`prefers-reduced-motion` is evaluated at engine init and on media query change.**
    When true: disable the today-line pulse, CP-flip animation, preview-bar fade,
    scroll-to-date smooth behavior. Functionality is never disabled — only motion.

### Visual Design (Canvas)

71. **Canvas font is `"12px Inter, system-ui, sans-serif"`** — set once at engine
    init, not per draw call. Must match Tailwind's `font-sans` stack so canvas
    labels are visually identical to the task list text. `ctx.font` is a global
    state — reset it after any draw call that changes it.

72. **Bar label text uses `COLOR.text` (`#1A1917`)** — `neutral-text-primary` on the light canvas surface. Set via `ctx.fillStyle = COLOR.text`. All color values live in the `COLOR` constant in `GanttRenderer.ts`; never use hex literals in draw functions.

73. **Critical path bars use `COLOR.barCritical` (`#B91C1C`)** — `semantic-critical` on light surface. Complete bars use `COLOR.barComplete` (`#3E8C6D` = sage-600, brand on-track per ADR-0103; `#66B998` sage-400 on dark) — `semantic-on-track`. Both values are defined once in `GanttRenderer.ts`; update there only.

74. **Non-working day shading uses `rgba(0,0,0,0.03)`** — a very subtle dark overlay on weekend columns on the light canvas. Applied on `canvas-bg`, not recalculated during drag.

75. **FS dependency arrows use collision-avoiding Manhattan routing with merge junctions** (issue #466,
    ADR-0063). Spec lives in `docs/adr/0063-gantt-dependency-routing-rules.md` and next to the code
    in `GanttRenderer.ts` (header comment for `calculateDependencyPath`). Behavior contract:
    - **Pure Manhattan polyline.** No Bézier, no diagonals. 3 to 7 segments. Every segment is
      strictly horizontal or strictly vertical.
    - **Stubs.** Exit stub from source's right edge ≥ `EXIT_STUB` (5px). Approach stub from the last
      Manhattan waypoint to the arrowhead base ≥ `APPROACH_STUB` (8px). The router targets the
      arrowhead base (`tipX − arrowSize`), NOT the bar/diamond edge, so the visible stroked shaft
      before the arrowhead is always APPROACH_STUB long. Arrowhead never sits at a path corner.
    - **Algorithm — decision tree** (ADR-0063 §"Routing engine"):
      1. Same row: 3 segments — exit stub → H → run-in.
      2. Stacked-sequential (target.x ≤ source.barRight + EXIT_STUB): 5-segment gutter dogleg
         per R12 — exit stub → V to row-gap gutter → H along gutter → V to target row → run-in.
      3. V at exit column blocked by a non-source/non-target bar: 5-segment left-detour —
         exit stub → V to gutter → H west past blocker's left edge → V south past blocker →
         run-in. Used for cases like milestone → child-of-phase where the exit column
         lands inside the containing summary's X range.
      4. Otherwise: collapsed 3-segment canonical L — exit stub → V at exit column straight
         to target row → run-in.
    - **Arrow color is charcoal, always.** `arrowNormal` and `arrowCritical` both resolve to
      `#444441` (light) / `#B8B5AE` (dark). Critical-path state is conveyed by the red BAR fill
      (rule 73), NOT by the arrow. Previous "critical arrow = red" rendered red arrows visually
      merging with red bars where they crossed (issue #466 gap P0-1).
    - **Summary rollups CAN be arrow endpoints.** Waterfall PMs use phase-to-phase dependencies
      as the primary relationship; suppressing them hides the user's working structure. Rollups
      are also obstacles for routing other arrows.
    - **Milestone flank rules.** Incoming arrows enter on the LEFT vertex flank (tip at
      `cx − milestoneHalfDiag`). Outgoing arrows exit from the RIGHT edge (= right vertex of the
      milestone). Entry and exit vertices on a single milestone naturally differ because FS sources
      always exit right and FS targets always enter left.
    - **JUNCTION RULE (codified, do not deviate).** A junction dot renders only where 3 OR MORE
      distinct arrow LINES meet at one (x, y) — TRUE convergences. Counting segments is the wrong
      lens: a T-junction has 3 segments but is visually indistinguishable from any Manhattan
      corner (one line passing through + one branching off), and a dot there reads as noise. The
      rule applies to:
      - **Merge** (convergence): 2+ predecessor lines arrive at one point and a single trunk
        line continues to the target = 3+ distinct lines meeting. Junction at
        `min(maxPredecessorExitX, tipX − arrowSize − APPROACH_STUB)`. Each predecessor terminates
        AT the junction (no arrowhead). A single trunk arrow with the only arrowhead runs east
        to the target.
      - **Split** (divergence) — **NO dot.** When a source has 2+ outgoing arrows that share a V
        column, each "intermediate" turn-off is a T-junction (one V line passing through + one H
        branching off east). Two visible lines, not three — corner, not junction. The deepest
        target's turn-off is the same (V terminating + H branching off). Splits draw no dots.
      - **Cross-arrow intersections** — bridge hop per ADR-0063 Rule 15 Type A. Two independent
        arrows crossing at one (x, y) get a 10-px-wide, 6-px-tall quadratic Bézier arc on the
        "over" segment (horizontal by default). Not a dot. Implemented via the collect-then-draw
        refactor in `drawDependencyArrows`: every Manhattan path is collected first, then
        `detectHops()` walks every pair of paths to record orthogonal interior crossings, then
        `drawPathWithHops()` strokes each path with arcs inserted at hop positions. Bézier
        (SS/FF/SF) arrows skip detection — Bézier-vs-Manhattan crossings are out of scope for v1.
      - **One arrow terminating on another arrow's path** — small T-junction dot per Rule 15
        Type B (spec adopted; implementation deferred). Halo radius 5 + dot radius 4 —
        intentionally one pixel smaller than the merge marker to reinforce the hierarchy.
      - **Junction visual (merge).** Outer halo (radius `MERGE_HALO_RADIUS=6`, `palette.surface`)
        + inner dot (radius `MERGE_DOT_RADIUS=5`, stroke color). Drawn LAST so it sits on top of
        every line endcap. If a junction would land inside a task bar's body, push it to the
        nearest row gutter (it should never sit ON an object). Original spec values were 4/3;
        bumped to 6/5 after canvas testing — 4/3 was visually subordinate to the 2-px arrow
        stroke and easy to miss on dense charts. See ADR-0063 "Junction rule (codified)" for
        rationale and Rule 15.2 for the full size hierarchy.
    - **Selection emphasis.** When the source OR target is in `engine.selectedTaskIds`, the arrow
      uses `palette.selectionRing` stroke at 2.5px. Other arrows hold 2px.
    - **SS / FF / SF unchanged** — cubic Bézier with 40px control-point offsets. Same charcoal
      stroke. Manhattan routing collapses these to U-shapes that cross the source bar; Bézier
      reads cleaner for same-edge links and matches MS Project's convention.
    - **Source connection dot is removed** — the arrow tail is the affordance; the dot added noise.
    - **Ancestors of the arrow's target are transparent obstacles** (ADR-0063 Override 4). A
      summary rollup is a visual aggregation of its children, not a real wall — an arrow into a
      deep descendant descends straight through the rollup's body instead of doing a chart-spanning
      U-detour around the entire summary bar. Source-side descendants stay as walls; only
      target-side ancestors are excluded.
    - **Redundant FS edges to descendants of a summary target are suppressed at render** (ADR-0063
      Override 5). When a source has FS to both summary S AND one or more descendants of S, only
      the summary edge renders. Schedule semantics are unchanged (data still has both edges, CPM
      still uses both); the renderer drops the descendant edges to declutter. Example: milestone
      → phase 4 and milestone → daddy3 (daddy3 ∈ phase 4) — only milestone → phase 4 renders.
    - **Lag annotation and click-to-delete on the arrow** are out of scope for v1.

### Performance

76. **Performance budget (enforced in CI visual regression):**
    - First render of 2,000 tasks: < 200ms
    - Frame budget during drag (500 tasks): < 16ms (60fps)
    - Zoom level change: < 100ms
    - Smooth scroll at 60fps with no dropped frames
    Any PR that regresses these targets must include a profiler screenshot.

77. **`TaskSoA` (Structure of Arrays) is required at 10,000+ tasks (Phase 3).**
    For Phase 1–2 (≤ 2,000 tasks), a plain `Task[]` array is acceptable.
    Do not introduce SoA prematurely — the abstraction cost is not justified
    below 10k tasks.

78. **Empty state on zero tasks** — `GanttEmptyState` component renders when
    `tasks.length === 0`. Uses `bg-neutral-surface` and `role="status"`
    so screen readers are informed. Never render the canvas stack with no tasks.

79. **Engine init failure fallback** — `GanttFallbackTable` renders a plain `<table>`
    when `canvas.getContext('2d')` returns null (e.g. headless test environments or
    very old browsers). Shown instead of the canvas timeline; not a degraded mode —
    all task data is accessible. Check support once at startup, not per frame.

80. **Zoom preserves center date** — when `engine.setZoom()` is called, the engine
    computes the canvas-origin coordinate of the viewport center before zoom and
    calls `container.scrollLeft` to restore it after `scales-change`. The visible
    date range shifts symmetrically around the user's current view midpoint.

81. **Initial viewport: today at 25% from left** — on engine `ready` event, set
    `container.scrollLeft` so today's date lands at 25% of the viewport width from
    the left edge. Provides immediate context without centering (which would hide
    near-term tasks).

82. **"Today" button in toolbar** — a `type="button"` element with the same style
    as ZoomControl buttons (rule 42): `border border-neutral-border rounded h-7 px-3
    text-xs font-medium`. Calls `engine.scrollToDate(todayIso, 'smooth')` (or
    `'instant'` when `prefers-reduced-motion` is active, rule 70). Placed to the
    left of the ZoomControl in the toolbar.

83. **Selection visual** — in the canvas bars layer: a 2px `COLOR.selectionRing` (**navy `#1B2A4A`** light / **reversed `#E9EDF3`** dark) inset stroke ring is drawn after the bar fill using `ctx.save()/restore()` (rule 59, canvas-bars layer only). The ring is navy INK — not sage — so it stays visible on a sage complete bar (distinguishability triad, ADR-0103 D4: complete = sage fill, selected = navy ring, today = sage line). In the task list row: `bg-brand-primary/10 border-l-2 border-brand-primary` (sage) on the selected row. Selection state is read from `engine.selectedTaskIds` (immutable Set) — never duplicated in local component state.

84. **Cursor states on canvas-interaction** — `ixCanvas.style.cursor` is set by
    `GanttEngineImpl._updateCursor()` based on FSM state and hit zone type:
    `grab` over bar body, `col-resize` over resize handle, `crosshair` over
    link-dot, `grabbing` during active drag, `default` otherwise. Never set
    cursor on bg or bars canvas layers.

85. **Resize handle indicator** — when hovering over a resize handle hit zone, a 1px
    vertical line is drawn on canvas-interaction at `barRight - 4` px, spanning the
    full bar height (`BAR_TOP_OFFSET` to `BAR_TOP_OFFSET + BAR_HEIGHT`). Color:
    `COLOR.textSecondary` (`#6B6965`). This meets WCAG 1.4.11 (3:1 against `neutral-surface`). Drawn by `drawResizeIndicator()` in GanttRenderer.ts.

## Risk Register Rules

86. **Risk severity color mapping** — always use these token pairs for severity labels and chips.
    Never use ad-hoc colors. All combinations achieve WCAG 4.5:1 on `neutral-surface` (#FFFFFF).
    Dark mode alternates are required — `bg-brand-accent-light` (#FFF3CD) is white and flashes in dark mode.
    Light → Dark overrides:
    - CRITICAL (20–25): `text-semantic-critical bg-semantic-critical/10` (semantic tokens adapt automatically via CSS vars)
    - HIGH (12–19): `text-brand-accent-dark dark:text-brand-accent bg-brand-accent-light dark:bg-brand-accent/20`
    - MEDIUM (6–11): `text-neutral-text-primary bg-brand-accent-light/50` (neutral tokens adapt via CSS vars)
    - LOW (2–5): `text-neutral-text-secondary bg-neutral-surface-raised` (neutral tokens adapt via CSS vars)
    - MINIMAL (1): `text-neutral-text-secondary bg-neutral-surface-sunken` (neutral tokens adapt via CSS vars)
    The severity chip is read-only in the UI — always computed from `probability × impact`.

87. **`text-neutral-text-disabled` on `bg-neutral-surface-sunken` is prohibited** — this
    combination yields 1.97:1 contrast, failing WCAG 1.4.3 on all text sizes. MINIMAL severity
    labels must use `text-neutral-text-secondary` (#6B6965, 3.12:1 on #EBEBEB) at minimum.
    This prohibition applies everywhere in the app, not only to risk register.
    The disabled token has almost no contrast headroom, so the prohibition extends to **any
    surface darker than `bg-neutral-surface` (#FFFFFF)** — including `-raised`, `-sunken`, and
    any `hover:bg-*` state on a row that contains disabled-token text (the hover state is the
    worst case and the one that must pass AA). Informational text that must stay legible uses
    `text-neutral-text-secondary` as the floor. (Surfaced by ux-review on #590.)

88. **Risk matrix zone tokens live in `tailwind.config.ts` under `colors.risk`** — no hex
    literals inside `RiskMatrix.tsx` or `RiskMatrixCell.tsx`. Tokens reference CSS custom
    properties defined in `globals.css` so dark mode automatically swaps to higher-opacity
    values that remain legible on dark surfaces. Light / dark values:
    ```
    risk.zone-critical: rgba(185,28,28,0.08)   /  rgba(248,113,113,0.28)
    risk.zone-high:     rgba(232,160,32,0.12)  /  rgba(251,146,60,0.22)
    risk.zone-medium:   rgba(232,160,32,0.06)  /  rgba(251,191,36,0.16)
    risk.zone-low:      rgb(245,245,240)        /  rgba(74,222,128,0.22)
    risk.zone-minimal:  rgb(255,255,255)        /  rgba(74,222,128,0.08)
    ```

89. **Risk detail opens as a drawer (desktop) / bottom sheet (mobile) — not a modal** —
    a drawer keeps the risk list visible while a user references WBS numbers to fill in
    linked tasks. A full-screen modal blocks this context. On mobile (< 768px), use a
    bottom sheet (85vh, drag handle, `role="dialog" aria-modal="true"`). On desktop
    (≥ 768px), use a right-side slide-in drawer (480px wide).

90. **Mobile "Add Risk" entry point is a FAB** — `fixed bottom-16 right-4 w-14 h-14 rounded-full
    bg-brand-primary`. Positioned above the BottomNav rail. Uses `border border-brand-primary-dark`
    for elevation affordance (no drop shadow, per rule 1). The FAB opens the bottom sheet.
    Tap count to save a minimal risk must be ≤ 4 taps + typing.

## Resource Utilization View Rules (Issue #22)

91. **Cell display is load % bars — not task bars.** Each cell in the resource grid shows a
    single filled bar representing load as a percentage of capacity. Task bars (mini) are not
    the default; they may only appear in a drill-down tooltip. Color thresholds:
    - Green (`semantic-on-track`): load < 85% of capacity
    - Amber (`semantic-at-risk`): 85% ≤ load ≤ 100%
    - Red (`semantic-critical`): load > 100% (overallocated)
    Never invert this color scheme. Red must mean overallocation without exception.

92. **Capacity baseline is `resource.calendar.hours_per_day` — never a fixed 8h/day global.**
    The API returns actual hours computed from the resource's own calendar. The UI divides
    `day.hours / resource_calendar_hours_per_day` to compute the fill percentage. Part-time
    workers (e.g. 6 h/day) must show 100% at 6 h, not at 8 h. Never hard-code 8 as the
    denominator. If `resource.calendar` is null, fall back to `project.calendar.hours_per_day`,
    then to 8.0 as a last resort — in that priority order.

93. **Default date range is rolling ±4 weeks from today.** On first render, `window_start` =
    Monday of (today − 4 weeks) and `window_end` = Sunday of (today + 4 weeks). A
    **"Fit to project"** button in the toolbar resets the range to
    `[project.start_date, max(task.early_finish)]`. After "Fit to project" is clicked,
    the button label changes to "Reset to today" until the user navigates away.

94. **Permission gate: ResourceView is only rendered for SCHEDULER (role ≥ 2) and above.**
    Team Member (MEMBER, role=1) and Viewer (role=0) must see `PermissionDeniedNotice`
    instead of the grid. Gate via `useCurrentUserRole()` on the client side; the API
    enforces the same gate server-side (HTTP 403). Never render the grid for lower roles
    even if the API call happens to return 200.

95. **409 "schedule not run" state renders `ResourceEmptyState` with a scheduler CTA.**
    When the API returns HTTP 409, show the empty-state component with the message
    "Run the scheduler to see resource utilization" and a "Run Scheduler" button that
    triggers a CPM recalculation. Do not show a generic error toast for 409.

96. **`calendar_differs_from_project` flag triggers a tooltip on the resource name.**
    When `resource.calendar_differs_from_project === true`, render a `ⓘ` icon next to
    the resource name. The `CalendarMismatchTooltip` reads: "This resource uses a different
    calendar than the project. Load is computed from the resource's calendar." Never
    suppress this flag silently.

97. **Column headers are week labels (Mon DD MMM), not individual day labels.** The grid
    groups days into ISO weeks. Each column header shows the Monday of that week formatted
    as `"Mon 2 Mar"`. Individual day cells are 32px wide; the week header spans 7 × 32px.
    Weekends are rendered at 50% opacity — they are never working days in the default
    calendar but are shown for date continuity.

98. **Resource rows are sorted alphabetically by `resource_name`.** The API returns them
    pre-sorted; the frontend must not re-sort. Unassigned task count (`unassigned_task_count`)
    is displayed in the toolbar as `"{N} task(s) without resource assignment"` in
    `text-semantic-at-risk` when N > 0, hidden when N = 0.

99. **Load tooltip on cell hover shows hours + task list.** Hovering any cell with load > 0
    opens `LoadTooltip` containing: total hours for that day, capacity hours, percentage,
    and a bulleted list of task names contributing to the load. The tooltip is positioned
    above the cell and uses `role="tooltip"` with `aria-describedby` wiring. It must
    dismiss on `Escape` and on pointer-leave.

100. **ResourceGrid uses CSS Grid, not canvas.** Unlike the Gantt (rule 59), the resource
     grid is a standard HTML/CSS layout: `display: grid` with `grid-template-columns`
     driven by the date window. Row virtualization is not required for the initial
     implementation (projects have ≤ 50 resources). Do not apply canvas-rendering rules
     (rules 59–85) to the resource grid.

## Board / Kanban View Rules (Issue #21)

101. **Board column header style** — same token as Sidebar section headers (rule 36):
     `text-xs font-semibold tracking-widest uppercase text-neutral-text-secondary`.
     Column count badge: `ml-2 text-neutral-text-disabled`. Column headers are `<h2>`
     elements with `aria-label` matching the visible text (e.g. `aria-label="In Progress, 12 tasks"`).

102. **Board card elevation** — `bg-neutral-surface border border-neutral-border rounded-md p-3`.
     No shadow (rule 1). Drag-lifted state: `ring-2 ring-brand-primary opacity-60 scale-105`
     with `motion-safe:rotate-1`. The original slot shows a dashed placeholder of equal height
     (`border-2 border-dashed border-neutral-border rounded-md`). Never animate the card on entry —
     only the lifted (drag) and snap-back (error) states animate.

102a. **Drag-lifted rows in list/backlog views use ring, not shadow.** A sortable row in the
     dragging state (e.g. the Product Backlog grooming view) uses `ring-2 ring-brand-primary
     opacity-60` — never `shadow-*` (rule 1). This mirrors the Board card drag treatment (rule
     102) so the shadow prohibition is not reintroduced piecemeal on each new sortable list.
     The drag handle is a `min-h-[44px] min-w-[44px]` button (rule 5 touch target) carrying the
     dnd-kit listeners, with an `aria-label` and the rule-4 `focus-visible` ring; the row stays
     clickable. Fixed-column list grids that exceed a phone's width wrap their header + rows in a
     `min-w-max` container inside an `overflow-auto` shell so columns stay aligned under
     horizontal scroll rather than compressing and misaligning.

103. **Board drag-over target** — `bg-brand-primary/5` fill + `border-l-2 border-brand-primary`
     on the column container during an active drag. Applied on `pointerenter` over the column drop
     zone; removed on `pointerleave` and `pointerup`. Never highlight the source column while a
     drag is in progress.

104. **Mobile board uses horizontal snap scroll** — `scroll-snap-type: x mandatory` on the board
     container; each column `scroll-snap-align: start`; column width `85vw`. A dot-strip indicator
     (`role="tablist"` with one `role="tab"` per column, `aria-selected` on the visible column)
     sits below the board and updates on scroll. Never use a column-picker dropdown on mobile — it
     hides spatial context. The mobile FAB creates a task in the currently-visible column's status,
     following the same pattern as the Risk Register FAB (rule 90).

105. **Board keyboard move alternative is mandatory** — drag-and-drop is not keyboard accessible
     (known WCAG 2.1.1 gap, parallel to Gantt rule 34). Every card's `···` overflow menu **must**
     include a "Move to…" item that opens a submenu listing the other columns. Arrow keys
     navigate the submenu; Enter commits. An `aria-live="polite"` region announces the result:
     `"{task name} moved to {column name}"`. This is not optional — it is the only keyboard path
     to change task status from the board.

106. **5-column board model** (issue #178) — the canonical status set is `BACKLOG | NOT_STARTED | IN_PROGRESS | REVIEW | COMPLETE`. `ON_HOLD` is a legacy value kept for data compatibility; it must never appear as a column in new board configs. The `_CANONICAL_STATUSES` frozenset in `serializers.py` is the authoritative list.

107. **Board card readiness states** (issue #179) — every `BoardCard` renders a `ReadinessChip` when `task.readiness` is present. Four states:
     - `idea`: no assignee — dashed-border chip, italic task name, `?` avatar circle, no progress ring, no accent bar
     - `estimated`: has owner — neutral chip with dot prefix
     - `ready`: has owner + predecessor links — brand-primary chip with ⛓ icon
     - `baselined`: in active baseline — neutral chip with 🔒 icon, accent bar uses `semantic-on-track`
     The left accent bar follows readiness, overridden by `isCritical` (→ `semantic-critical`). Absent `readiness` field → no chip rendered (backwards compat with pre-#179 API responses).

## Shell Navigation Rules (Issues #204–#205)

108. **Canonical view tab order is `Overview · Board · Schedule · WBS · Table · Calendar · Team · Risks`** (issue #204, updated per VoC review 2026-04-29). Overview is first — it is the canonical landing/orientation surface (ADR-0030). Board is second — the execution surface. The route segment for the Schedule view is `/schedule`. Never change this order without a design review. `ViewTabs.tsx` is the source of truth. The mobile `BottomNav` mirrors this order and omits Risks (infrequent on mobile). The methodology-gated **Backlog** tab (`product-backlog`, #1096) sits between **Board** and **Sprints**, visible only on Agile/Hybrid projects (gated via `methodologyTabs.ts`, the same mechanism as Sprints — web-rule 154); it links to the existing `/projects/:id/product-backlog` grooming page. It is desktop-only: `BottomNav` deliberately does **not** mirror it (an 8th mobile tab harms phone UX; mobile backlog cards are tracked in #1044).

109. **The health surface is a single all-width status chip + `role="dialog"` popover — NOT a responsive `md:flex` / `md:hidden` split** (issue #205; tablet breakpoint #1562; reshaped to the chip in #1644, ADR-0128 §B progressive disclosure). `HealthCluster.tsx` renders one `data-testid="health-cluster"` chip `<button>` at **every** width (no breakpoint-gated dual render): a health **dot** (`aria-hidden`, rule 6), the worst-state **word** (`On track` / `On watch` / `At risk` — the non-color signal, rule 6; derived from the project-wide `at_risk_count` / `critical_count` on `useShellStats`, independent of the methodology segment set), an optional **neutral** P80 forecast fragment (`hidden sm:inline-flex`; neutral even inside a red/amber chip, rule 172), and a caret. Clicking it toggles a popover (`role="dialog"` `aria-label="Project health"`, `shadow-pop` pop-surface exception to rule 1) whose rows are exactly the methodology's `healthClusterModel` segments — forecast band (P50 + P80 rows, neutral, `Details ›` opens the MC panel, issue #196), at-risk/critical drill groups, sprint/points/velocity — with the ADR-0104 velocity privacy wall honored (rule 168, no number when suppressed). The popover **must** trap focus with `useFocusTrap` (rule 206): focus in on open, Esc closes + restores focus to the chip, outside pointer-down closes. **Do NOT reintroduce the pre-#1644 `hidden md:flex` expanded cluster or the `md:hidden` "Health ▾" phone dropdown** — the all-width single control is deliberate (both to reduce the surface and to avoid the rule-211 `hidden`-toggle double-render, where the same `getByRole` control resolves to two DOM nodes). The chip word colors clear WCAG 1.4.3 in both modes (at-risk `#92400E` / critical `#B91C1C` light, `#FB923C` / `#F87171` dark; on-track word is neutral `text-neutral-text-secondary` with the green carried by the dot).

## Toolbar Responsive Rules (Issue #568)

110. **Every toolbar control is classified as primary or secondary.** This classification is the contract — enforce it in code review, not just implementation. Primary controls are always visible at all widths ≥ 768px. Secondary controls collapse at narrow widths (see rule 111). Canonical classification per view:
     - **Schedule toolbar**: `+ Task`, `+ Milestone`, `BUILD MODE` = primary; `CP only`, `Focus chain`, `Critical path`, `Milestones` toggles = secondary; health summary chip, `Columns`, `Today`, ZoomControl = primary. The **Export** button (`ScheduleExportButton`, #1438) is **secondary with a desk-task carve-out** from rule 111's icon-only-at-`md` tier: a deck-style PDF export is a desktop task, so it renders as a labeled button at `lg` only, folds straight into the `···` Project-actions overflow menu at `md` (as "Export schedule as PDF…", opening the same options dialog), and is **hidden entirely below `md`** — it never renders an icon-only `md` variant. A control may take this two-tier (`lg` label → `md` overflow → hidden) collapse instead of the standard three-tier when its cost or desk-only nature justifies it, but the carve-out must be named here per-control (this is the documented instance).
     - **Board toolbar** (CalmToolbar): Group, Sort, Density, Layout segmented control = primary; `My tasks`, `At-risk`, `Cost` toggles = secondary.
     - **Resource toolbar**: view mode tabs, Prev/Today/Next date nav, `Fit to project` = primary; status filter chips = secondary.
     Adding a new control to any toolbar requires an explicit primary/secondary declaration in the MR description.

111. **Three-tier breakpoint collapse for toolbar controls:**
     - `lg:` (≥ 1024px): all controls visible with full text labels. This is the reference layout — screenshots and design specs target this width.
     - `md:` (768–1023px): secondary controls render icon-only — wrap the label text in `<span className="hidden lg:inline">` so it disappears but the icon and `aria-label` remain. Primary controls keep their short text labels.
     - `< md:` (< 768px): secondary controls disappear from the toolbar entirely and move into the `ToolbarOverflowMenu` (rule 112). Primary controls remain visible with short labels. Never render a standalone secondary control below 768px.
     Use `hidden lg:inline` on label text spans, `hidden md:flex` / `md:hidden` on visibility toggles — the same pattern already enforced on `TopBar` (rule 109).

112. **`ToolbarOverflowMenu` is the shared overflow container for all views** — do not implement a per-view overflow popover. Import from `src/components/toolbar/ToolbarOverflowMenu.tsx`. The trigger is `<button aria-label="More options" aria-haspopup="menu">` rendering `⋯`. The popover is `role="menu"` with one `role="menuitem"` per secondary action or `role="menuitemcheckbox" aria-checked={…}` per secondary toggle. The overflow button is rendered only below `md:` (`md:hidden`); it must not appear at `lg:`. Keyboard: `ArrowDown`/`ArrowUp` navigate items; `Enter`/`Space` activate; `Escape` closes and returns focus to the trigger.

113. **Toolbar root must be `flex flex-nowrap` — wrapping to multiple rows is a bug.** The fixed height is `h-10` for view-level toolbars (Schedule, Board, Resource) and `h-7` for sub-toolbars (GanttToolbar secondary row). If a toolbar wraps, it is missing breakpoint collapse rules (rule 111). `flex-nowrap` is explicit so lint / visual review surfaces the problem immediately rather than silently producing a two-row layout. **Documented carve-out (#1708): the bespoke Grid `Toolbar` (`features/grid/GridView.tsx`) intentionally `flex-wrap`s below `md`** — it predates the rules 110–112 `ToolbarOverflowMenu` pattern and was never migrated to it. Because it wraps on a phone, its root uses `min-h-9 md:h-9` (grow to contain the wrapped rows) rather than a fixed `h-9` that would clamp the height and let the wrapped rows overflow onto the task list (the #1708 overlap bug). It stays `md:flex-nowrap` at desktop. Do **not** "correct" it back to `flex-nowrap` at all widths — that reintroduces the overlap. Migrating it to the rule 111/112 overflow-collapse pattern is the proper long-term fix (tracked in #1710); until then this wrap is deliberate.

114. **`ScheduleToolbarToggle` and any new toggle button accept a `hideLabel` prop.** When `hideLabel={true}`, the text label is omitted from the render; the button still carries `aria-label` and `title` matching the full label text. Used by the `md:` tier (rule 111) to switch secondary toggles to icon-only mode. Do not add a new size variant to the design system for this — the existing `h-7 px-3 text-xs font-medium` sizing is retained in both modes.

## Settings Shell Save Contract (Issue #536)

115. **Every settings page with a save-bar form follows the dirty/save/discard contract.** Two paths:
     - **API is wired**: the page MUST call `useDirtyForm({ values, initialValues, onSave, onReset, apiReady: true })` from `src/features/settings/hooks/useDirtyForm.ts`. The hook publishes the page's dirty state to `useSettingsSaveStore`, which `SettingsShell` reads to render the save bar, the `ConfirmDiscardDialog`, the `beforeunload` listener, and the `Ctrl/Cmd+S` save shortcut. Pages keep their own `useState` per field — `useDirtyForm` does not own field state, only observes a `values` / `initialValues` pair via `JSON.stringify` compare.
     - **API not yet wired (stub page)**: the page MUST wrap its content in `<StubFieldset disabled>` from `src/features/settings/components/StubFieldset.tsx`. The `<fieldset disabled>` disables every form control and the `.settings-stub :disabled` CSS rule in `globals.css` applies the visual treatment. When the page's API ships (typically one of #517–#530), flip from `<StubFieldset>` to `useDirtyForm({ apiReady: true })`.

     Pages with row-level mutations and no save-bar (e.g. `WorkspaceMembersPage`, `WorkspaceGroupsPage`, `ProjectIntegrationsPage`, `ProjectAccessPage`, `ProgramAccessPage`, `ProgramProjectsPage`) and danger-zone pages with typed-confirm flows (`WorkspaceDangerPage`, `ProjectArchivePage`, `ProgramArchivePage`) are exempt — they don't have a save-bar UI surface.

116. **Discard semantics: page owns the snapshot.** The "last-saved snapshot" lives in the page (typically alongside the field `useState` calls). On save success, the page bumps its `initialValues` to the freshly-saved values so `useDirtyForm` re-derives `dirty=false`. On discard, the page resets its field `useState` back to `initialValues` from inside the `onReset` callback. Never store the snapshot inside `useDirtyForm` or `useSettingsSaveStore` — both are stateless aggregators.

117. **Future enterprise sections (ADR-0029 slots) participate in the same contract.** A slot-registered enterprise section MUST NOT render its own save bar; it publishes dirty state via `useDirtyForm` to the shell's save bar, just like a built-in page.

118. **Settings-surface density exception (rules 5, 45, 50).** Pages and components under `features/settings/` use a compact admin density: `text-[11px]` / `text-[10px]` for secondary and label text, and `h-6` / `h-7` controls, are permitted — an explicit override of rules 5, 45, and 50. Rationale: settings is a desktop-admin-first surface, and matching the established Workspace/Project/Program settings density (e.g. `WorkspaceMembersPage`, `ProjectWorkflowPage`) is better UX than per-component rule conformance that would make one page visually inconsistent with its siblings. This exception is **scoped to `features/settings/` only** — it does not apply elsewhere. Two clauses remain non-negotiable inside settings: (a) every interactive control still requires the full rule-4 `focus-visible` ring; (b) a third-party-fidelity preview (e.g. a Slack-message mock) may use the third party's literal palette as product content, exempt from rule 8, but must carry a code comment stating the intent. Modal focus-trap/restore and sub-44px touch targets remain a known codebase-wide a11y gap tracked separately — not introduced by, nor required to be fixed in, settings MRs. The settings density exception floor is `text-[10px]`; `text-[9px]` and below are prohibited everywhere, including settings.

122. **`SettingsShell` scroll containers reserve a stable scrollbar gutter.** Both the sidebar `<nav>` and the content panel `<div>` in `SettingsShell.tsx` carry `[scrollbar-gutter:stable]` alongside `overflow-y-auto`. The shell is the single scroll authority for every settings scope (Workspace/Program/Project) and React Router swaps only the `<Outlet>` content between sub-pages, so without a reserved gutter the scrollbar appears/disappears between a tall sub-page (Program General, Risk policy) and a short one (Projects, Integrations) and shifts the panel ~15px horizontally on platforms with classic scrollbars (#776). Never drop the `[scrollbar-gutter:stable]` utility from these two containers; use `stable` (scrollbar-side only), not `both-edges`, so the left `px-6` content rhythm is preserved. The content container carries `data-testid="settings-content-scroll"`; its gutter is asserted in `SettingsShell.test.tsx` (class present) and `e2e/settings-shell-ux.spec.ts` (computed `scrollbar-gutter === 'stable'`).

123. **Entity shells suppress their working chrome on `/settings` routes.** The shared `SettingsShell` (SCOPE switcher + context selector + nav) is mounted in all three scopes — Workspace (`/settings`, directly under `AppShell`), Project (under `ProjectShell`), and Program (under `ProgramShell`). It must mount at the **same vertical position** in every scope, or clicking the SCOPE switcher relocates the switcher itself and the user loses their place (#776). The constant-height global `TopBar` (its project `ViewTabs` are `h-full` inside it, adding no height) is the only chrome permitted above the shell. Therefore an entity shell that renders its own header/tab chrome above the `<Outlet>` (today: `ProgramShell`'s program header + `Overview/Backlog/Projects/Members` tab strip) **must suppress that chrome on its settings routes** — detect via `useMatch('/programs/:programId/settings/*')` (or the scope's equivalent) and render only a minimal full-height wrapper around `<Outlet>`, mirroring `ProjectShell`. Entity identity in settings is carried by the `SettingsShell` context-selector pill; exit is the fixed-position global left Sidebar — do **not** add a per-scope "back to {entity}" bar, which reintroduces the per-scope height delta this rule removes. Suppressing the program `<h1>` on settings routes is also correct: `SettingsPageTitle` already renders the page `<h1>`, so the previous program-header `<h1>` was a duplicate. New entity shells follow the same contract. Covered by `ProgramShell.test.tsx` and `e2e/program-general-settings.spec.ts`.

124. **The settings context pill is a searchable entity switcher, and its chevron means "switchable".** `SettingsShell` renders `SettingsContextSwitcher` (the program/project identity row) as a `aria-haspopup="listbox"` trigger whenever it receives `contextOptions` with **≥ 2** entries, letting the user jump straight to another program's / project's settings — the option `to` preserves the current sub-page (`…/test/settings/cadence` → `…/test2/settings/cadence`), and selection routes through the shell's `guardedNavigate` so a dirty form still hits `ConfirmDiscardDialog`. With **0–1** options it is a static identity row with **no chevron** — never render the chevron when no switch is possible (the pre-#776 always-on chevron was a dead affordance). The open popover is **always** a `combobox` search input + `role="listbox"` of `role="option"` rows (no scan-then-search gap, no menu-vs-combobox mode split): type-to-filter is case-insensitive substring on name; focus stays in the input with the highlight via `aria-activedescendant`; `aria-selected` marks the current entity (checkmark); arrows/Home/End/Enter navigate; two-stage Escape clears the query then closes; empty result → a `role="status"` "No …s match" row; focus returns to the trigger on close. The per-row health dot is `aria-hidden` (rule 6), so health is also carried in each row's and the trigger's `aria-label` (e.g. `"test2, critical"`). The search box gets its focus ring via `focus-within` (it receives programmatic focus on open, where `focus-visible` is unreliable). Workspace scope passes no `contextOptions` (single workspace) → static pill. Covered by `SettingsContextSwitcher.test.tsx`, `SettingsShell.test.tsx`, and `e2e/program-general-settings.spec.ts`.

125. **The SCOPE switcher never navigates to a blank page; unavailable scopes are disabled, not faked.** `SettingsScopeLink.to` is `string | null` (+ optional `disabledReason`); a non-active scope segment with `to === null` renders **disabled** (`<button disabled aria-disabled title={disabledReason}>`, kept in the 3-way control so the tri-scope model holds) rather than falling back to a non-settings route (#776). **Targets:** Workspace → always `/settings/general` (always enabled — one workspace, always valid). Program → from a project, the project's **parent program** (`project.programId`) ?? first program ?? disabled "No programs yet"; from workspace, first program. Project → from a program, first project in that program ?? any project ?? disabled "No projects yet"; from workspace, first project. A scope switch always lands on **`/general`** (scopes have different sub-page sets, so do NOT preserve the sub-page across scopes — only the same-scope context pill does that, rule 124). While the backing `usePrograms()`/`useProjects()` list is still loading, the target is `null` → the segment is disabled until it resolves (prevents the transient blank). The disabled segment's `text-neutral-text-disabled` is **exempt from rule 87 / WCAG 1.4.3** as an inactive UI component (and must read dimmer than the `text-secondary` enabled segments). Covered by `SettingsShell.test.tsx` and `e2e/settings-shell-ux.spec.ts`.

## KPI Rollup / Overview Rules (Issue #713)

119. **A configured-but-not-computable metric renders as a muted card with a reason, never hidden.** When a dashboard or overview is driven by a user-selected metric set (e.g. the program rollup KPIs), a metric the user enabled but for which no value can be computed yet MUST render as a muted card (`border-dashed border-neutral-border`, value `—` in `text-neutral-text-disabled`) with a short plain-language reason in the `sub` slot (e.g. "Needs cost data"). Do not silently drop it from the grid — the user enabled it and must see *why* it is blank. The machine-readable `reason` from the API drives the human label via a lookup map; never render the raw reason code. See `ProgramOverviewPage.tsx`.

120. **Health/variance values pair color with a non-color signal (rule-12 reinforcement for rollups).** A health band card shows the band *text* ("At risk") alongside the semantic color and an `aria-label` on any pill; a day-variance shows a signed `+9d` / `-3d` so the sign — not only the color — conveys late-vs-early. Counts and scores are plain numerals. This keeps KPI cards WCAG 1.4.1-compliant without a legend.

## Enterprise Boundary Affordance (Issue #541)

121. **Enterprise-only capability rows carry an inline `EE` badge that is itself the link.** On any OSS surface that advertises capabilities reserved for TruePPM Enterprise (Settings → Roles & permissions matrix today; the future `/programs` and `/portfolio` gateways), mark each Enterprise row data-driven (`ee: true` on the capability/row object — never a hardcoded list in the render, so it stays in sync as the Enterprise repo adds capabilities) and render the `EnterpriseBadge`: a focusable `<a>` to the Enterprise page (`https://trueppm.com/enterprise`), `target="_blank" rel="noopener noreferrer"`, with `title` **and** `aria-label` "Available in TruePPM Enterprise". **The badge IS the link** — a hover-tooltip that contains a link is unreachable (it dismisses as the pointer travels to the link and is invisible to keyboard users), so never wrap the link in a tooltip. Gate rendering on `useEdition() === 'community'`: under the Enterprise edition the capability is available, so the badge is suppressed (it would be noise). Badge style (settings density, rule 118): `bg-brand-primary/10 text-brand-primary text-[10px] font-semibold uppercase tracking-wide` + the standard rule-4 `focus-visible` ring. This is the concrete implementation of the ux-review §6.2 boundary-affordance requirement — it turns "dead" Enterprise cells (a check in a column that does nothing in OSS) into a purchase-decision path and stops missing Enterprise features from reading as broken OSS. The badge is the shared `features/settings/components/EnterpriseBadge.tsx` (#791) — it **self-gates** on `useEdition()` (returns null unless community), so callers render `<EnterpriseBadge />` without an edition check of their own. It applies to whole action buttons too, not just matrix cells: an Enterprise-reserved action (e.g. "Sync from directory" = LDAP, "View change history" = audit trail) renders as a disabled button with the badge alongside it as the reachable upsell link.

## Disabled Placeholder / Stub Controls (Issue #791)

122. **Disabled placeholder/stub controls in `features/settings/` use the `.settings-stub` recipe — never `disabled:opacity-50`.** Half-opacity body text fails WCAG 1.4.3 and diverges from the established stub treatment. Two equivalent paths: wrap a whole section in `StubFieldset` (preferred — the `.settings-stub :disabled` rule in `globals.css` recolors every descendant in one place, used by `WorkspaceDangerPage`), or for an individual button apply the recipe as `disabled:` utilities at opacity 1 — `disabled:bg-neutral-surface-sunken disabled:text-neutral-text-secondary disabled:border-neutral-border/55 disabled:cursor-not-allowed` (drop the bg/border pair for borderless text-link buttons; recolor + `disabled:cursor-not-allowed` is enough). Always keep the full rule-4 `focus-visible` ring **including `ring-offset-1`**, and add a `title` explaining why the control is unavailable: `#NNN` for an OSS gap, "Available in TruePPM Enterprise" paired with `EnterpriseBadge` (rule 121) for an Enterprise-reserved action. Dead-but-clickable buttons are worse than disabled ones (#669) — disable the placeholder, link the tracking issue, ship. The **individual-button** form of this recipe (opacity-1 `disabled:` recolor + `disabled:cursor-not-allowed` + kept focus ring + explanatory `title`) is the app-wide convention for any inert placeholder, not only `features/settings/` — e.g. the schedule-export dialog's disabled "Layout B — Report" radio (until #1439) and its Export-when-empty button (#1438) both use it. Only the whole-section `StubFieldset`/`.settings-stub` path is settings-scoped.

## Program Navigation (Issue #790, ADR-0095)

126. **Program navigation lives in the global TopBar, mirroring project `ViewTabs` (ADR-0095).** `ProgramTabs.tsx` renders inside `TopBar` (alongside `ViewTabs`), gated on `useProgramId()`, `hidden md:flex h-full` so it adds no height. Canonical order: `Overview · Backlog · Projects · Members · Settings` — Settings is last, matching projects, and is the discoverable entry to program settings (#790). `ViewTabs` and `ProgramTabs` are **mutually exclusive** — each returns null without its id, and a URL is either project- or program-scoped — so exactly one renders; do not render program nav in-content (`ProgramShell` is a minimal `<Outlet>` wrapper, like `ProjectShell`). Styling is identical to `ViewTabs` (web-rule 38: underline active `border-brand-primary text-brand-primary` + `aria-current="page"`, rule-4 focus ring, leading `aria-hidden` icon, `text-sm`). Active tab = the path segment after `:programId`; the Settings tab stays active across every `/programs/:id/settings/*` sub-route. No role gating on the tabs — writes are gated inside each page. `BottomNav` program parity is a deferred mobile follow-up. Covered by `ProgramTabs.test.tsx` and `e2e/programs.spec.ts`.

## Schedule View Interaction Rules (Issues #351 / #491)

127. **Continuous zoom uses a stepper, not segmented tiers.** The Schedule `ZoomControl` is `[ − ]  {derived tier}  [ + ]` in a `role="group" aria-label="Timeline zoom"` (`h-7`, `border border-neutral-border rounded`). `pxPerDay` (in `scheduleStore`) is the source of truth; the discrete `ZoomLevel` is **derived** from it (`deriveTier`) and survives only as a label for header formatting + QuarterModeControl gating. The center readout is a non-interactive `role="status" aria-live="polite"` span showing the derived tier (Day/Week/Month/Quarter/Year) — it IS the active-tier indicator; there is no pressed/segmented active state. `−`/`+` step geometrically (×/÷ `ZOOM_STEP_FACTOR` = 1.5), clamped to the `ZOOM_CONFIGS` day…year band (`MIN_PX_PER_DAY`/`MAX_PX_PER_DAY`) and disabled at the edges; tooltips carry `⌘−`/`⌘=`. A separate `Fit` button (rule 82 style, label "Fit to project", `⌘0`) sits beside the group and calls `engine.fitToProject()`. ZoomControl + Fit are primary toolbar controls (rule 110); the toolbar stays `flex-nowrap h-10` (rule 113).

128. **Auto-tier header reads the continuous scale, not the discrete enum.** `drawTimelineHeader` chooses its emphasized (major) and de-emphasized (minor) units from the scale's continuous `pxPerDay` via `headerUnitsForPxPerDay`, so emphasis swaps smoothly across the whole Day↔Year continuum as the user pinch/Ctrl-wheel zooms. Thresholds (`HEADER_TIERS`, constants in `GanttScaleData.ts`): ≥80 Hour/Day · 24–80 Day/Week · 8–24 Week/Month · 2.5–8 Month/Quarter · 0.7–2.5 Quarter/Year · <0.7 Year. The 'hour' minor tick unit is **deferred** — above 80 px/day the header caps the major at Day until an hour unit ships.

129. **Cursor-anchored zoom for wheel/pinch; viewport-center for keyboard/toolbar.** Ctrl/Cmd+wheel (and trackpad pinch, delivered by the browser as a ctrl+wheel) zoom toward the pointer — the date under the cursor stays fixed (`setPxPerDay(px, { clientX })`: capture `anchorDate` + `viewportX` before the scale change, restore `scrollLeft = newAnchorCanvasX − viewportX`). The `−`/`+` buttons and `⌘=`/`⌘-` keyboard zoom use viewport-center anchoring (rule 80). Plain wheel over the timeline keeps scrolling; plain wheel over the left task-list pane keeps scrolling vertically — only the Ctrl/Cmd modifier zooms.

130. **Drag-to-pan uses a separate `GanttPanFSM` arbitrated on pointerdown.** When Space is held OR the middle button (`e.button === 1`) is pressed, the pan FSM claims the gesture and the task-bar drag FSM (`GanttDragFSM`) is bypassed entirely; otherwise drag behaves as before. Pan moves both axes (clamp `scrollLeft`/`scrollTop` to `[0, max]`); vertical pan flows to the task list via the existing `taskListScrollRef` scroll-sync, so no extra wiring. Only the timeline canvas pans — the left task-list pane never initiates a pan, and Space there does nothing pan-related.

131. **Pan cursor precedence extends rule 84; pan is exempt from rule 70.** In `_updateCursor`: `_panning` → `grabbing` (whole canvas) > `_panArmed` (Space held) → `grab` (whole canvas, overrides hit-zone cursors) > existing drag/hit-zone logic. Space-arming is scoped to canvas hover/focus (`_canvasHovered`) — never a global Space capture (which would break Space on buttons, checkboxes, and page scroll) and never inside an editable target. Pan is direct 1:1 manipulation with **no momentum/inertia**, so it is exempt from `prefers-reduced-motion` (there is no animation to suppress; do not add inertia without gating it on rule 70). A pan release suppresses the synthetic `contextmenu` once so middle/right-drag never opens the context menu.

132. **Pan discoverability lives as one line in the `ScheduleLegend` body, not a toast.** The hint "Hold Space + drag, or middle-drag, to pan" is a single `text-xs text-neutral-text-secondary` line under a `border-t` divider at the bottom of the legend body (rule 50 floor; not `tppm-mono`). The legend is the established "what do these affordances mean" surface — do not add a tutorial toast or coachmark that fires on every Schedule open.

## Schedule Backlog-Promote Rules (Issue #318)

133. **The Unscheduled gutter is a two-section tray (To Do above, Backlog below) in one scroll container.** `UnscheduledGutter` partitions its task list by `status === 'BACKLOG'` into an upper "To Do" section (NOT_STARTED, no committed `planned_start`) and a lower "Backlog" section (`BACKLOG` ideas). Each section is a `<section role="group">` with a count in its `aria-label`, and a sticky sub-header (`sticky top-0` inside the scroll container) using the rule-36/101 token (`text-xs font-semibold tracking-widest uppercase text-neutral-text-secondary`): `TO DO · UNSCHEDULED ({n})` and `BACKLOG ({n})` (the Backlog header adds a `hidden lg:inline` italic hint "drag onto the timeline to promote & schedule" only when non-empty). The gutter header count is the **sum** of both sections. Each section renders a muted `role="status"` empty row ("No unscheduled To Do tasks" / "No backlog items") rather than collapsing — **never hide one section while the other has items**. Both empty falls back to the existing whole-gutter "all scheduled" header behavior. Sections are separated by `border-t border-neutral-border`.

134. **Backlog chips differ from To Do chips by a 2px dashed left edge + a readiness label, never color alone.** `UnscheduledTaskRow` takes a `variant: 'todo' | 'backlog'` prop. The backlog variant adds `border-l-2 border-dashed border-neutral-border` (the at-a-glance cue that a drop **promotes** BACKLOG → To Do, not merely schedules) plus a readiness label reusing the `BacklogBand` ReadinessChip semantics (idea / estimated / ready / baselined) — the text label is the WCAG 1.4.1 non-color signal (rule 107). Idea-readiness rows render the name `italic text-neutral-text-secondary`. To Do chips carry no dashed edge.

135. **Promoting a backlog item is a `{ planned_start, status: 'NOT_STARTED' }` PATCH via `usePromoteTask` (decision A2).** Sending an explicit `status: 'NOT_STARTED'` in the body skips the server serializer's date-gated NOT_STARTED → IN_PROGRESS auto-bump (which only fires when `status` is absent), so a backlog promotion lands deterministically in To Do regardless of the drop date — and the success toast verb is fixed: `Promoted '{name}' to To Do and scheduled for {date}` (`{date}` = `MMM D`, matching the drop-indicator label). The To Do gutter path is unchanged — it sends only `planned_start` and the server owns the bump (#336). `usePromoteTask` carries an `onMutate` optimistic cache snapshot (the chip leaves its section immediately) + `onError` rollback (the chip returns), mirroring `useUpdateTask`/`useToggleComplete`. The offline guard (rule 29) skips the PATCH, clears the preview, and leaves the chip; aria-live is written via DOM ref (rule 30): "Promoted {name}, scheduled for {date}." / "Could not schedule {name}." Esc cancels mid-drag (rule 28). There is **no** server change and **no** new endpoint — reuse `PATCH /tasks/{id}/`.

136. **The keyboard alternative for backlog scheduling is the shared `ScheduleTaskDialog` (the rule-105 parallel).** `features/schedule/ScheduleTaskDialog.tsx` (`role="dialog" aria-modal="true"`, ~360px, `bg-neutral-surface border border-neutral-border rounded-lg`, no shadow) is reachable from **two** entry points but is a single component: the gutter backlog chip's `···` menu (replaces the inline form; the To Do row keeps its inline form) and the Board `BacklogCard`'s `···` action (mounted once in `BoardView`, single instance like `BacklogDemoteConfirmDialog`, passed down via `BacklogBand`'s `onSchedule`). The date input defaults to today (local ISO) and is focus-first; focus is trapped; Esc + ✕ cancel and **return focus to the trigger** (copy `BacklogDemoteConfirmDialog`'s pattern). Schedule issues the same A2 PATCH (`{ planned_start, status: 'NOT_STARTED' }`) and the same success toast + aria-live "Scheduled {name} for {date}."; on error it stays open with an inline `text-xs text-semantic-critical` message; offline disables the Schedule button with a `title`.

137. **Focus-ring form in the Schedule view: `focus-visible:` wins; no dark-mode override.** The Schedule view (`features/schedule/`) uses `focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-1 focus-visible:ring-offset-neutral-surface` per rules 46/126 — this overrides the generic ux-review `focus:` preference for standalone controls. The Schedule view adapts to dark mode (rule 40), but `brand-primary` is now mode-aware sage (sage-700 #316F57 light / sage-400 #66B998 dark, ≥3:1 on the dark surface #15223C), so `ring-brand-primary` passes WCAG 1.4.11 in **both** modes. The old `dark:focus-visible:ring-semantic-on-track` override — added for the pre-rebrand green #1C6B3A's 2.81:1 dark failure — is **retired; do not reintroduce it** (rule 4, ADR-0103). Steppers inside a bordered `role="group"` may use `ring-inset` without an offset.

## Sprint/Phase/WBS Guardrail Rules (Issue #875, ADR-0101)

138. **Guardrail warnings are inline, proceed-then-offer-undo notices — never a blocking modal.** A Tier-1 guardrail (summary/phase/recurring/out-of-window task assigned to a sprint) fires *after* the assignment already succeeds: the `GuardrailNotice` renders inline under the control with `role="status"` + `aria-live="polite"` (NOT `role="alert"`), a single `Keep it here` tap to dismiss, and `Undo` to revert. Only a Tier-2 *block* (`GuardrailBlock`) uses `role="alert"`, and it offers no override — it is resolved only by removing the offending state (`Got it` dismisses the notice; the server never changed the FK). Only bulk operations may use a confirm dialog (one aggregated confirm for N tasks, never N notices).

139. **The override reason field is always optional and never gates the proceed action.** `GuardrailNotice`'s note input is collapsed behind a `▸ Add a note (optional)` toggle and is one-tap-skippable; `Keep it here` works with an empty reason. No policy tier may make it required at the warn level — the override is recorded server-side via the task's history regardless.

140. **Guardrail / health UI mounts only on planning surfaces — never in a contributor view.** `GuardrailNotice`, `GuardrailBlock`, and Tier-3 health badges live in the schedule/sprint-planning/settings surfaces (task drawer `SprintSection`, board, sprint panel). They must never be imported into the contributor `me`/"My Work" tree, and guardrail warnings must never become a push notification (Priya non-goal — enforced structurally by *where* the component mounts, not a runtime role check).

141. **Guardrail copy uses outcome language, never WBS jargon.** User-facing strings come from the server in consequence terms ("This double-counts in velocity", "Phases group work; assign the tasks inside it instead") — never structural terms like "WBS L1 root" or "summary task". The frontend renders the server `detail` verbatim; it does not synthesize its own jargon copy.

## Design System v2.0 — Navy/Sage Brand (ADR-0103)

> The full token + role mapping is ADR-0103; the gold standard is the brand
> package at `packages/web/brand/` (`brand-guidelines.html`). `brand-primary` is
> now mode-aware **sage** (sage-600 #3E8C6D light / sage-400 #66B998 dark) — it
> reverses itself, so most pre-v2 rules that cite `brand-primary` are still
> correct; only the green hex *figures* changed. Rules 142–148 are the deltas.

142. **The brand mark is the duotone dependency-arrow `LogoMark`** (`Icons.tsx`) — navy nodes (`fill-navy-700 dark:fill-reversed`) + sage arrow (`fill-sage-500`, holds in both modes; the arrowhead is a *fill*, so sage-500 is correct there per rule 143). Never `currentColor` (it is two-color). The wordmark (`Logo.tsx`) is **"True" navy + "PPM" sage**, `font-display` (Space Grotesk) Bold `-0.02em`, no space; the accessible name lives on the lockup's `aria-label` (the visible text is split across spans — assert it via `getByLabelText`/`getByRole`, never `getByText('TruePPM')`). The "PPM" sage is **`text-brand-primary`** (sage-700 on light / sage-400 on dark) — it is AA *foreground text*, so it must NOT use the fills-only sage-500, which is only 2.88:1 as text on white (rule 143; was a 1.4.3 fail fixed in #1689). Render ≥24px; below 28px use the favicon build.

143. **Sage shade discipline (WCAG).** sage-500 `#4FA884` = **fills only**, always with navy text (navy-on-sage 6.8:1); it is ~2.9:1 on white so it is **never** body text, a foreground icon, or a thin border on a light surface. **sage-700 `#316F57` = foreground text / link / ring / border on light (5.93:1)** — this is what `brand-primary` resolves to in light mode. (sage-600 `#3E8C6D` is only **4.06:1** on white — it fails AA for normal text; it is a *fill/dot* weight, e.g. the on-track `-bg` tint, never foreground text.) sage-400 `#66B998` = text/affordance on **dark**. Putting sage-500 or sage-600 on white as foreground text is a 1.4.3 failure — use sage-700. This includes the **wordmark "PPM"**: it is foreground text, so it uses `text-brand-primary` (sage-700), not sage-500 (rule 142; #1689). The **only** sage-500-on-light usages that are legitimate are *fills* (bar/dot/tint fills with navy text, and the `LogoMark` arrowhead).

144. **Primary actions use the shared `Button` component** (`src/components/Button.tsx`), not ad-hoc `bg-brand-primary` fills. `variant="primary"` is the brand recipe `bg-sage-500 text-navy-900 border-sage-600` (+ sage-400 fill on dark). New primary/secondary/ghost/danger buttons import `Button`; do not hand-roll the fill recipe.

145. **Semantic amber/red: brand hue is a FILL, on-light text keeps the AA-dark variant.** at-risk fill/dot `#DE9326` but text/border `#92400E`; warning fill/dot `#D97706` (it lives only in `--sem-warning-bg`) but text/border `#854D0E` — yellow-800, ≥5.4:1 on the warning-bg tint; critical fill/dot `#CF4438` but text/border `#B91C1C`. Never set `text-semantic-at-risk`/`text-semantic-warning`/`text-semantic-critical` to the brand hue — those brand values fail AA as text on white (2.4:1 / 2.9:1 / 4.0:1; warning brand `#D97706` as text was only ~2.74:1, the 1.4.3 fail in issue 1377). `#854D0E` is kept deliberately distinct from at-risk's `#92400E` so the two amber states stay separable. on-track `#3E8C6D` and info `#2F6FD1` are AA as text and used directly.

146. **Canvas selection ring is navy/reversed, never sage** (rule 83). Because sage carries both action and positive-state, the navy ring is what stays visible on a sage complete bar. Triad: complete = sage *fill*, today = sage *line*, selected = navy *ring*. Two sage meanings on one surface must differ by component geometry + a text/icon label, never hue alone (1.4.1).

147. **Body ink and dark surfaces are navy.** `--neutral-text-primary` = navy `#1B2A4A` (light) / reversed `#E9EDF3` (dark). Dark surfaces are navy: `--neutral-surface` #15223C, `--chrome-surface` #0E1626. (Canvas bar-label `COLOR.text` stays `#1A1917` — legibility-tuned for bar fills, rule 72; the divergence is intentional.)

148. **Three type families (brand §06):** `font-display` (Space Grotesk) for display/wordmark/big numbers; `font-sans` (Inter) for UI/body; `.tppm-mono` (JetBrains Mono, rule 8c) for data. Load via the `index.html` Google-Fonts link.

## Sprint Scope-Injection Approve-Gate Rules (Issue #882, ADR-0102)

149. **Pending acceptance is a neutral read-state, not a warning — and it has one shared chip.** A task injected into an active sprint after activation (`task.sprintPending`) renders the shared `features/board/PendingAcceptanceChip.tsx`: neutral/gray surface (`bg-neutral-surface-sunken` + `text-neutral-text-secondary`) with a hollow `○` glyph — **never amber or red**, never a semantic at-risk/critical token. Pending means "visible but not yet committed", which is a state, not a problem. The same component renders on the planning board card and the contributor "My Work" row so the two surfaces can never drift in tone. Copy is outcome-language ("Pending acceptance"), never state-machine jargon.

150. **Accept is additive (confirm-toast, no undo); reject is destructive (proceed-then-undo).** A single-item accept is a one-tap ✓ with **no confirmation dialog and no undo** — it only adds work to the commitment, which is recoverable by rejecting later; it surfaces a confirmation toast. A single-item reject removes the task from the sprint, so it is a **proceed-then-offer-undo** action (undo re-links via sprint-assign), mirroring the rule-138 guardrail pattern. **Bulk** accept/reject is the one carve-out that uses a confirm dialog (one aggregated confirm for all N pending, never N toasts) because batch decisions are higher-stakes than a single tap. Reason/note fields, where shown, are always optional and never gate the proceed action (rule 139 carryover).

151. **Scope affordances are gated by `useCanManageScope` and never mount in the `me` tree.** The accept ✓, the reject overflow item, the board banner's `Review` button, and the `ScopePendingReviewPanel` controls render only when `useCanManageScope(projectId)` is true (role >= `ROLE_ADMIN`) — this is a **render-gate only**; the server enforces the real gate (role >= ADMIN **and** a real `ProjectMembership` on the task's project) and returns `403 scope_accept_forbidden` regardless of role ordinal, which the client treats as authoritative. The contributor "My Work" tree gets the passive `PendingAcceptanceChip` and **nothing else** — no accept/reject controls ever mount there (the decision is team-owned; the chip is a passive read-state, not a guardrail notice or a notification).

152. **Accept/reject are not offline-queueable — disable, do not queue.** When offline (`!navigator.onLine`), the pending chip still renders from synced data, but every accept/reject control is **disabled with an explanatory `title`** and the action is **never queued**. A stale offline accept could re-commit work the team rejected (or vice-versa) on reconnect; the decision must be made against live state. This is the deliberate exception to the optimistic-write pattern used elsewhere — scope decisions fail closed offline.

153. **Forecast-transparency copy is a shared, API-driven string gated on `pending_count > 0`, planning surfaces only.** Any commitment/forecast surface that can have pending items behind it (sprint panel committed line, burndown caption) renders `forecastScopeCaption(sprint.pending_count)` — the single shared helper in `features/sprints/sprintMath.ts` returning `"Forecast reflects accepted scope only — N pending acceptance"` — so the surfaces can never word it differently, and the client never derives the count (the API supplies `sprint.pending_count`). It renders `null` (nothing) when `pending_count <= 0` — no "0 pending" noise. This copy belongs on planning surfaces only, never the contributor view.

## Team Settings + Facet Axis Rules (Issue #927, ADR-0078)

154. **The Project Settings → Team tab is methodology-gated in 0.3, not team-count-gated.** The tab (`features/settings/team/ProjectTeamPage.tsx`) renders in the Setup nav group iff `project.methodology` is `AGILE` or `HYBRID` (hidden on `WATERFALL`) — so waterfall projects never see Team UI. This is deliberately **not** the ADR-0078 §F single-team-invisibility rule: that rule (`team_count === 1` → hide) governs *multi-team chrome* (team picker, team-name labels, Sprint/Task team fields), which this tab does not render until #599. With only the auto-created default team existing in 0.3, a count gate would hide the tab forever and make facet assignment impossible, defeating the issue. Gate by methodology; the tab shows the single default team's role+facet matrix with no multi-team chrome.

155. **Role and facets are independent axes; the two facets are soft-singletons reassigned by the server.** A `TeamMemberRow` carries a team-role `<select>` (Member/Admin) plus two `role="switch"` facet toggles (`FacetToggle`) for Scrum Master and Product Owner — never conflate them (an Admin need hold no facet; a Member can be Product Owner). Turning a facet **on** when another member already holds it does **not** PATCH immediately: the page shows an inline `role="alertdialog"` confirm ("{holder} is currently {facet}. Make {target} the {facet} instead?") with Reassign/Cancel, because the server clears the prior holder (at most one per team). Turning a facet **off** is immediate (the facet may sit vacant). On success, invalidate the whole `['team-members', teamId]` query — the prior holder's row changed too, so never patch a single row optimistically.

156. **Edit rights follow the ADR-0078 §D low-consent split: project Admin+ OR explicit team Admin.** `canEdit` is `myRole >= ROLE_ADMIN` (project inheritance) **or** the caller's own team-membership row has `role === 'admin'`. Read-only callers (viewers, plain members) get the same roster with the role `<select>` replaced by static text and the switches `disabled` — facet badges stay visible so everyone can *see* who the SM/PO is. The server enforces the same gate (`IsTeamFacetEditor`) and 403s regardless; the render-gate is UX only.

## Icon-prefixed input focus (Issue #933)

157. **Icon-prefixed inputs ring the wrapper, not the input (rule 4 corollary).** When an `<input>` sits inside a bordered `<span>`/`<div>` alongside a leading icon — the `flex items-center gap-2 rounded-md border … h-9` field pattern used in `PromoteMilestoneDialog` (`CreateModeBody`, `BindModeBody`) — the rule-4 focus ring goes on the **wrapper** via `focus-within:ring-2 focus-within:ring-brand-primary focus-within:ring-offset-1`, with `focus-visible:outline-none` on the inner input. Stripping the input's outline without a wrapper ring leaves the field with **no** focus indicator (WCAG 2.4.7) — the regression the #933 ux-review caught. A bare text/date input with no icon wrapper rings itself directly, per `PlanSprintModal`. (Same `focus-within` rationale as rule 124's settings search box.)

## Program visual identity & wayfinding (Issue #963)

158. **Program color is identity, not status — shape encodes the signal, color is the value.** A program's `program.color` renders ONLY through `ProgramIdentitySquare` (`features/programs/ProgramIdentitySquare.tsx`) — a rounded **square**. Project/program *health* renders ONLY as a **circle** dot (the existing `ProjectListItem`/`HealthDot`). The square-vs-circle distinction is the firewall that keeps an accent hue (even green) from ever reading as a status. Never tint the identity square by health, never render it as a hollow outline, and never render `program.color` directly in a component — the dynamic accent goes through the `style` prop only (rule 10), with the palette + `contrastText()` single-sourced in `programColor.ts` (no hex literals, rule 8). **Unset color is the common case** and is a faint **filled** `bg-neutral-surface-sunken` square, never health-tinted. The square is always `aria-hidden`; the program **name** is the accessible signal (rule 6). Sizes: `sm` (10px, wayfinding dot, no label) / `md` (16px) / `lg` (36px, `showLabel` renders ≤3-char code-or-initials). Per-row squares are only for genuinely **cross-program** lists; a single-program board marks the program **once in its header** (the #959 redundant-per-row-tag lesson).

159. **WIP-limit indicators use the shared three-band `wipState()` — never a local `count > limit` check.** Every WIP-limit surface (board column header/badge, the sprint-panel header chip, future swimlane/column counters) imports `wipState(count, limit)` from `features/board/wip.ts` and maps the band to chrome identically: `under` → neutral, `at` (count === limit) → `text-semantic-at-risk` amber, `over` → `text-semantic-critical` red (rule 145 keeps those as the AA-dark text variants, never the brand fill hue), `none` (limit null) → suppressed/neutral. A `5/5` at-limit state must read amber everywhere and never neutral on one surface and amber on another — a two-band local check is the drift bug the #546 ux-review caught. Color is never the sole cue: pair the flagged bands with a `⚠` glyph (`aria-hidden`) and an aria-label that names the band ("at limit" / "over limit"), per rule 107 (WCAG 1.4.1).

160. **The sprint-timeline selected-card ring is navy, never sage (rule 83/146 corollary).** `SprintTimelineStrip`'s selected card uses `ring-2 ring-navy-700 ring-offset-1 dark:ring-reversed` — NOT `ring-brand-primary` (which resolves to sage). The active card carries a sage on-track tint (`bg-semantic-on-track-bg`) + a sage progress fill, so a sage selection ring would put two sage meanings on one surface (action/positive + selected), a WCAG 1.4.1 hue-only collision. Selection = navy ring; active-but-unselected = the faint sage `ring-brand-primary/30` tint; the two never share hue. Same rule that keeps the canvas selection ring navy (rule 83), extended to the sprint-card selector (#567 ux-review).

## Responsive labels & presentational headers (Issue #975 / #974)

161. **A responsive abbreviation must always carry its full accessible name.** When a control shows a short label at narrow widths and the spelled-out term at `md:`+ — the `<span className="hidden md:inline">{full}</span><span className="md:hidden">{abbrev}</span>` pattern established by the Signal-privacy ladder rungs (`SignalLadder.tsx`), the matrix-lens column headers (`SignalMatrixLens.tsx` `<th>`), and any future status/role chip — the **full** term must be the element's `aria-label` (and `title` for hover) so a screen reader or pointer never gets the ambiguous abbreviation regardless of which span is visible. The decorative abbrev/full spans carry no `aria-*`. Where horizontal space always allows (a roomy vertical list, e.g. the raise-ceiling radio options), skip the responsive split and render the full term directly. A single source-of-truth label map pairs the abbreviated and full forms (`AUDIENCE_RUNG_LABEL` / `AUDIENCE_RUNG_LABEL_FULL`); never inline either string.

162. **An `aria-hidden` visual column-header row is the correct pattern when every column's control already carries its own accessible name and the mobile layout re-labels inline.** The Project → Team facet roster (`ProjectTeamPage.tsx`) renders a `hidden sm:flex` header row (`Member / Role / Scrum Master / Product Owner`) that is `aria-hidden="true"`: the role `<select>` and the two facet `role="switch"` toggles each own their `aria-label`, and the `< sm` layout labels each control inline per row, so a SR-exposed header would only duplicate. Do not "fix" such a header by removing `aria-hidden` (it re-introduces the duplication) and do not drop the header (sighted desktop users lose the column key). Column widths in the header must mirror the row's exactly (`flex-1 / w-32 / w-36 / w-36` + `gap-3 px-4`).

## Progress / commitment bars (Issue #1107)

163. **A bar that can exceed its target never clamps to 100% — it scales to `max(target, actual)` and shows the overage past a capacity marker.** The `CommitmentBar` in `SprintTimelineStrip.tsx` is the reference: bar width represents `max(committed, completed)`; the on-track portion (`0→committed`) fills `bg-semantic-on-track`; the overage (`committed→completed`) fills `bg-semantic-at-risk`; a 2px `bg-neutral-text-primary` **capacity tick** (`aria-hidden`) marks the target line and is rendered **only when over** (under target, the bar end *is* the target). A clamped `Math.min(actual/target, 1)` bar that reads "done" when a sprint actually over-delivered is the bug this rule prevents (the #1107 VoC blocker). Colour is never the sole cue (1.4.1): pair the overflow with a text label carrying the count — `⚠ +{N} over` in `text-semantic-at-risk` (the `⚠` is `aria-hidden`; the count stays `.tppm-mono` per rule 8c — do **not** swap font-family mid-line). ARIA keeps `valuenow ≤ valuemax` by scaling `aria-valuemax` to the bar denominator and naming the target + overage in `aria-label` (`"{actual} of {target} … {N} over commitment"`), never `valuenow > valuemax`. The capacity tick uses `neutral-text-primary` (not a semantic hue) so it reverses to light in dark mode (rule 147) and clears 1.4.11 3:1 on both the green and amber fills. Reuse this anatomy for any future budget/capacity/load bar that can exceed its reference.

## List-detail inline-edit drawers (Issue #1043)

164. **A detail drawer opened from a list/backlog row owns a local deferred Save bar — never `useDirtyForm`.** The Product Backlog `StoryDetailDrawer` (`features/project/backlog/`) is the reference. It inline-edits an entity's scalar fields via a **drawer-local** model: a `dirty = JSON.stringify(draft) !== JSON.stringify(initial)` compare, a footer Save bar (`shrink-0 border-t`, hidden until dirty) that batches **all changed scalar fields into one PATCH** (send only the changed keys so a Member's title-only edit never posts Admin-gated structural fields), a Cancel that reverts to `initial`, and a discard-guard on Esc/✕/row-swap when dirty. It MUST NOT call `useDirtyForm`/`useSettingsSaveStore` (rule 115) — that hook publishes into the `SettingsShell` save bar, which is not mounted outside `/settings`. **Three carve-outs mutate immediately (optimistic, outside the dirty batch)**, because each is a different endpoint, gate, or server-owned toggle: (a) child-collection items on their own flat endpoint (acceptance criteria — Member+ writes to `/acceptance-criteria/`, while the scalar batch is Admin+/PO); (b) a single server-gated state toggle whose validity the drawer reflects live (Definition of Ready — the readiness gate re-evaluates as AC/points change in the same drawer); (c) a live computed preview (the scoring `score` is recomputed client-side to mirror the server `compute_score` exactly — same formula, same "any input missing OR denominator falsy → `—`" rule, 1-decimal; `scorePreview.ts`). **Fields whose server permission gate differs from the caller's role render read-only** (the scoring/type/epic structural fields are `useCanManageBacklog` / Admin+ / PO-facet; below that they are static reads with a one-line "managed by the Product Owner" note, never disabled inputs that imply editability). The selected row carries a **navy** selection ring (`ring-2 ring-inset ring-navy-700 dark:ring-reversed`), never sage — sage `ring-brand-primary` is already the drag-lift state (rule 102a) and the focus ring (rule 4), so selection must differ by hue (rules 146/160). The desktop drawer is non-modal (`aria-modal="false"`, no focus trap, no backdrop — the list stays usable, rule 89); only the mobile bottom sheet is modal. The drag handle calls `e.stopPropagation()` so a handle tap reorders without opening the drawer; the row body opens it.

## Forecast basis labeling (Issue #1094)

166. **A forecast surface must label its basis (velocity-band estimate vs Monte Carlo) with *visible* text — never a `title` tooltip alone, and reserve P50/P80/P95 percentile vocabulary for real simulation.** The bridge reforecast (`PercentileBar` in `PromoteMilestoneDialog.tsx`, `MilestoneForecast` in `VelocityForecastLine.tsx`) is a deterministic velocity-band heuristic, not a Monte Carlo distribution; presenting its dates as "P80" is false precision (the #1094 VoC finding). When `basis === 'velocity_band'`, drop percentile labels (use `Early`/`Likely`/`Late` and `est. {date}–{date}`) and show an on-screen `Estimate — velocity-based, not simulated.` caption / `(velocity estimate)` qualifier — not a `title` (which is not exposed to keyboard or screen readers, rules 22a/121). Keep `P50`/`P80`/`P95` only when `basis === 'monte_carlo'`. Same single-basis source drives both the dialog bar and the panel forecast line so the two never disagree.
## Live retro board + team-health pulse (Issue #851 / #923, ADR-0117)

167. **A roving-`tabindex` group (`role="radiogroup"`/`"tablist"`) MUST move DOM focus on arrow keys, and arrow navigation MUST NOT commit a side-effect — only activation (click / Enter / Space) commits.** `PulseRadioGroup` in `TeamHealthPulse.tsx` is the reference: it holds a `focusIdx` + a `btnRefs` array; arrow keys advance `focusIdx` and call `btnRefs.current[next]?.focus()` (real focus movement — setting `aria-checked`/`tabIndex` alone strands the user on a now-untabbable element, WCAG 2.1.1), and they do **not** call the commit handler. The native `<button role="radio">` commits via `onClick` (which fires on Enter/Space too). The bug this prevents: routing arrow keys through the same `onPick`→mutate path as click, so a keyboard user scanning 1→5 silently records four throwaway answers — and for the pulse, those pollute the privacy-sensitive team aggregate (the #923 ux-review 🔴). Only the focused option is tabbable (`tabIndex={i === focusIdx ? 0 : -1}`); the group is reached by Tab, navigated by arrows, committed by activation. This applies to **every** `role="radiogroup"`/`"tablist"` built from native buttons — including single-select **filter-segment** controls (e.g. the Risk register's All/High/Unmitigated/Mine segment, `RiskSegmentedFilter.tsx`). A facet filter is a radiogroup and MUST use the roving-tabindex + arrow-focus pattern, not N independent tab stops — even when the committed action is non-destructive (a filter), arrow keys move focus only and activation applies it.

168. **A privacy-gated read renders a content-free wall, never a redacted teaser — the gate is a server fact the client only branches on.** When a server signal is withheld (the pulse trend's `{gated: true}`, ADR-0104), the component returns ONLY the "kept private" wall — no count, no blurred chart, no "request access" CTA (social pressure is itself a leak). The gated/ungated shape is a discriminated union on a server `gated` flag (`usePulseTrend`); the client never receives the data and computes nothing from it. Mirror this for any future team-private signal surface: branch on the server's gate, render an explanatory wall with zero numbers, and keep the aggregate math server-side (the client renders `energy_declining` etc. as given, never derives a trend from raw responses it should never hold).

169. **`neutral-text-disabled` is for inert/disabled affordances ONLY — never for readable content.** The token resolves to `#A09D99` (~2.66:1 on white) light / `#64748B` (~3.7:1 on navy) dark — both below the WCAG 1.4.3 AA 4.5:1 floor for normal text. De-emphasized but *readable* content — timestamps, "by {user}" attributions, parenthetical context (the `SprintDailyDeltaPanel` since-hint and burndown `prior → current` context were the #925 ux-review 🔴) — must use `neutral-text-secondary` (4.63:1 light / 7.45:1 dark, passes), and lean on **size/weight** for the quieter visual hierarchy, not a sub-AA color. Reserve `neutral-text-disabled` for genuinely non-interactive/disabled controls where the low contrast *is* the "you can't use this" signal. Corollary (the #925 burndown 🔴): when meaning is carried by an `aria-hidden` glyph + semantic color (an ▲/▼ arrow + green/amber), the accessible name must restate that meaning in words (`aria-label="Remaining work down 8 points — on track"`), not just the raw number — the SR user otherwise loses the polarity a sighted user reads from the arrow/color (1.4.1).

## Board-scoped ephemeral drop notice (Issue #1140)

170. **A board-scoped ephemeral drop notice is bottom-center, `role="status"` + `aria-live="polite"`, neutral-toned, and carries the SAME hollow ○ as the pending chip — never success-green.** `BoardDropNotice` in `features/board/` is the reference: it fires when a card is dragged into an ACTIVE sprint and creates a *pending* scope-change (ADR-0102 post-activation injection) — a read-state, not a celebration — so it MUST NOT use success-green chrome (rule 149). Surface is `bg-neutral-surface-raised border border-neutral-border text-neutral-text-primary shadow-lg`, positioned `absolute bottom-4 left-1/2 -translate-x-1/2 z-40` inside the `relative` board container, auto-dismissing after 4s and fading via `transition-opacity`. The leading glyph is the SAME hollow `○` (`text-neutral-text-secondary`, `aria-hidden`) used by `PendingAcceptanceChip` so the toast and the card chip can never drift in tone. It is a single instance: a new message replaces the prior one and resets the timer (key the notice with a changing value so two identical drops still re-trigger it). Only an ACTIVE-sprint drop that creates a pending injection toasts — a PLANNED-sprint link (commitment baseline, no pending gate) and a plain status move do not. Reuse this anatomy for any future board-local transient confirmation; do NOT reach for a global toast library.

## Composite accessible name across visible child spans (Issue #1138)

171. **`aria-label` on a non-widget element (`<p>`/`<div>`/`<span>`) does NOT suppress its descendant text in NVDA/JAWS — to expose a multi-span line as ONE accessible string, mark every visible child `aria-hidden`.** The `BoardSprintHeader` meta line ("{name} · {range} · Day N of M") is the reference: the `<p>` carries the composite `aria-label`, and **all** visible children — the name/range/counter spans AND the `·` separators — are `aria-hidden="true"`, so the screen reader reads only the label, never the label *then* the spans again (the double-read bug the #1138 ux-review caught, WCAG 1.3.1). This applies only to non-widget containers: on a real widget (`button`/`role="radio"`/etc.) `aria-label` does override the accessible name, so this extra step is unneeded there. The alternative pattern — `role="presentation"` on the container + a single `sr-only` span for the composite string — is equivalent; pick one, never leave readable child spans exposed alongside a container `aria-label`.

## v2 view row — grouped view bar + methodology-adaptive health cluster (Issue #1167, ADR-0128)

172. **The project view row is a grouped, method-filtered bar + a single methodology-adaptive health cluster — both project-scoped chrome that self-suppresses off-project and on settings routes.** Shell slice 3 of the v2 redesign (ADR-0128). Reference: `features/shell/ViewTabs.tsx`, `HealthCluster.tsx`, `healthClusterModel.ts`, `methodologyTabs.ts`. **Amended by ADR-0134 (rule 174):** the view row is no longer its own row — `ViewTabs`/`ProgramTabs` + the health cluster now share the single unified shell bar (`TopBar`) with the context affordances, and the tab strip lives inside `ShellNavScroller`. The grouping, the methodology filter, the health-cluster semantics, and the off-project/settings suppression below are all unchanged by that move.
     - **Grouping is visual, not structural.** Route segments are unchanged (rule 108 / ADR-0030) — links stay `/projects/:id/:view`. The PLAN / TRACK / PEOPLE assignment lives in `methodologyTabs.ts` (`VIEW_GROUPS`); `groupedVisibleViews(methodology)` applies the ADR-0041 methodology filter *within* each group and drops fully-filtered groups (never render an empty group label). `overview` leads and `settings` trails **standalone** (outside the groups). The role gate for `resources` (Team, role ≥ SCHEDULER) stays in `ViewTabs`. Each group is a `<div role="group" aria-label="Plan views|Track views|People views">`; the visible mono header (`PLAN`/`TRACK`/`PEOPLE`, the rule-36/101 token `text-xs font-semibold tracking-widest uppercase text-chrome-text-secondary`) is `aria-hidden` so the group name is not double-read (rule 171). The `<nav>` keeps `aria-label="View"` (shared with `BottomNav`; do not rename — it churns the e2e nav-name specs for no a11y gain). The `{METHOD} Workspace` tag (`MethodWorkspaceLabel`) is `hidden xl:inline` and lives at the left edge of the TopBar right cluster (first to drop as width tightens).
     - **One bordered, fixed-slot health cluster replaces the three free-floating badges.** Build the segments from the pure `healthClusterModel(...)` selector (a discriminated union — never branch on raw nulls in the view). The three segments adapt to methodology: **Agile** = Sprint · Points · Velocity; **Waterfall** = Forecast · At-risk · Critical; **Hybrid** = Sprint · Forecast · Critical. Slots are **fixed** — a zero/empty segment renders a calm static read ("0 critical", "—", "No active {iteration}"), never vanishing, so the cluster never reflows. The cluster carries `data-testid="health-cluster"` for e2e (Chromium prunes the named `role="group"` from its a11y tree, so Playwright `getByRole('group')` can't see it — query the testid, keep the `role="group" aria-label="Project health"` for screen readers).
     - **The Forecast (P80) segment is NEUTRAL, not amber.** A forecast date is informational, not a risk state — render the value `tppm-mono text-neutral-text-primary`, label `text-neutral-text-secondary`. This **supersedes the pre-v2 rules 21/39** (the standalone P80 *pill* used an outlined-at-risk style; the unified cluster does not — color is reserved for the actual at-risk/critical *signals*, rule 8). The null/"—" no-value read uses `text-neutral-text-disabled` (sanctioned by rule 119 for a no-value placeholder, with a `title` explaining why — "Run the scheduler").
     - **At-risk / Critical segments are `text-semantic-at-risk` / `text-semantic-critical`, pair the count with the word, and are static at 0.** A count > 0 is a button opening a `role="menu"` task popover (kept local to the cluster so it reads as one bordered unit, not a nested bordered pill); a count of 0 is a non-interactive static read (no dead drill-down button). Never color-only (rule 7/120/145).
     - **Velocity / Points respect the ADR-0104 privacy gate.** When `ProjectVelocity.velocity_suppressed` is true, the Velocity segment renders a **content-free privacy wall** (rule 168): a lock glyph + "Kept to the team", **no number** — the client never holds the value, the team owns the policy. The Points segment is **throughput-neutral** (#1161): points when `committed_points != null`, else an item count (`committed_task_count`), else the segment is omitted entirely — never an empty "Points" badge. Velocity surfaces `excluded_count` (ADR-0113) as a `· N excl` affordance. The "Sprint" noun comes from `useIterationLabel().singular` (ADR-0111/0116), never the hard-coded string.
     - **Below `md` (phones only)** the cluster collapses to a single `Health ▾` dropdown (rule 109) expanding the same segments; the cluster proper is `hidden md:flex` and the dropdown is `md:hidden`. The expanded cluster holds from the tablet breakpoint up (`md`, ≥ 768px) so the 768–1024px tablet range keeps the P80 forecast inline (issue #1562) — it does **not** collapse at `lg` any more.

## Inline contextual prompts (Issue #1181, ADR-0129)

173. **Inline contextual prompts are page content, not modals.** A first-login/onboarding prompt that mounts inline (e.g. `LandingPrimaryUsePrompt`) uses `<section aria-label="…">`, never `role="dialog"`/`aria-modal`, no focus trap, no auto-focus. Animate-out is `motion-safe:transition-opacity` paired with an explicit `opacity-0` state applied before the unmount timeout (`motion-safe:opacity-0` driven by a `saved` flag in state). Skip/dismiss sets a localStorage flag only; it does not `navigate()`.

## v2 unified shell bar — one bar, adaptive identity, scrollable nav (Issue #1204, ADR-0134)

174. **The v2 top region is ONE bar (`TopBar`, `h-14`), not two — it merges the former context row + view row; the nav tabs scroll, the right cluster is pinned, and wayfinding is adaptive to rail state.** ADR-0134 supersedes the two-row split of ADR-0127/0128. Reference: `features/shell/TopBar.tsx`, `ShellNavScroller.tsx`. There is no `ContextBar.tsx` (deleted). The bar is `flex items-center` (never wraps): a `shrink-0` left group (mobile hamburger / desktop rail-re-open ≡), the adaptive identity, the `flex-1 min-w-0` nav scroller, then the `ml-auto shrink-0` right cluster (`MethodWorkspaceLabel` · `HealthCluster` · `CreateMenu` · `TaskRunIndicator` · `PresenceAvatarStack` · `NotificationBell` · `UserMenu`).
     - **Adaptive identity.** The `Breadcrumb` renders in the bar ONLY when the rail is not persistently visible: always on mobile (the rail is a drawer), and on desktop only when `sidebarCollapsed` (where it is the sole wayfinding and the hidden rail freed the width). When the rail is open on desktop it is `md:hidden` — `display:none`, which removes it from the a11y tree; **never `aria-hidden` on a still-rendered nav, and never a base-`hidden` that would also strip it on mobile.** It duplicates the rail's highlighted program/project otherwise — do not render it always-on.
     - **Scrollable nav, never icon-only, never clipped.** `ShellNavScroller` wraps `ViewTabs`/`ProgramTabs` in an `overflow-x-auto min-w-0` region so the ~950–975px grouped strip scrolls horizontally below ~1440px instead of clipping or dropping labels. Tab **text labels never degrade to icon-only** (VoC 🔴 + WCAG 1.1.1); the group labels drop before the strip scrolls. The active tab (`[aria-current="page"]`) is auto-scrolled into view on route change. Scroll affordances (edge fade `from-chrome-surface to-transparent` + chevron) render **only when the strip overflows** in that direction; chevrons are pointer-only (`tabIndex={-1}`) — the tabs are a `<nav>` of links (NOT a `tablist`), so sequential Tab focus + the browser's scroll-focused-into-view is the keyboard path, not arrow-key roving tabindex. The `HealthCluster` stays pinned (never behind a tab scroll); it collapses to `Health ▾` only on phones below `md` (rule 172/109), staying expanded with P80 inline across the tablet range (issue #1562).
     - **Tab focus ring is `ring-inset`, not offset, inside the scroller.** Because `overflow-x-auto` forces `overflow-y: auto`, an offset focus ring (`ring-offset-1`) on an `h-full` tab is clipped top/bottom (WCAG 2.4.7). `ViewTabs`/`ProgramTabs` links therefore use `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-brand-primary` (no offset) — the rule-137 precedent for rings in constrained/bordered contexts. Do not reintroduce `ring-offset-*` on tabs inside a scroll region.
     - **Theme toggle has exactly one home: `UserMenu`.** The standalone `ContextBar`/shell `ThemeToggle` is removed; do not re-add a second always-on theme control to the bar.

## Inheritable boolean settings — Workspace → Program → Project (Issue #978, ADR-0135)

175. **An inheritable boolean setting renders as a two-chip inherit/override radiogroup wrapping the shared `Toggle`, never a bare switch — and the inheriting state surfaces the resolved value as "Inherit (On/Off)".** `InheritableToggleField` (`features/settings/components/InheritableToggleField.tsx`) is the boolean analog of `InheritableIterationLabelField` (ADR-0116) and the reference for `public_sharing` / `allow_guests` at program/project scope (ADR-0135, #978). It reuses the **extracted** shared `Toggle` (`features/settings/components/Toggle.tsx`, lifted out of `WorkspaceGeneralPage`) and the **same** chip styling as `InheritableIterationLabelField` — do not fork either.
     - **The override is the raw nullable column** (`value: boolean | null`, `null` = inherit); the page PATCHes `null` verbatim to clear (no string-style `.trim()` coercion — a `boolean | null` is already canonical).
     - **Display is derived from `value` and the parent's resolved `inherited` only** (`effective = value ?? inherited`) so the chip suffix and the switch word can never contradict — this is the #978 unintuitive-toggle fix (the visible word is always state-derived, never a hardcoded "Disabled" beside an on switch). Entering override **seeds from the effective value** (`onChange(value ?? inherited)`), never a blank/false default that silently flips the live state.
     - **The two chips are a native-radio `role="radiogroup"`** (`sr-only` inputs → arrow-key nav + grouping for free; rule 167 satisfied natively, no hand-rolled button radiogroup).
     - **Below role ≥ ADMIN the control is a read-only indicator** (ADR-0133): no radios, no switch — a status dot + the literal word ("On"/"Off") + provenance ("inherited from the workspace default" / "set on this program"), one composite `aria-label` with `aria-hidden` children (rule 171) ending "View only.", body text in `neutral-text-secondary` not `neutral-text-disabled` (rule 169), color never the sole cue (rule 7).
     - Reuse this anatomy for any future inheritable boolean (e.g. a per-scope feature toggle), the same way `InheritableIterationLabelField` is the pattern for inheritable string/enumerated settings.

## Flow analytics & flow forecasts (Issue 1188, ADR-0137 / ADR-0130)

176. **A delivery forecast branches on `forecast_basis`, never the legacy `basis`; flow charts use Recharts with CSS-var tokens and an `sr-only` summary; suppressed flow metrics render a content-free wall and an in-audience flow panel carries a legible "aggregate only" caption.** The sprint-forecast contract carries two fields: `basis` is frozen at the legacy `"monte_carlo"` constant (kept only for back-compat) and **must never be branched on**; `forecast_basis` (`"velocity" | "throughput"`, ADR-0130 D3) is the discriminator. A `throughput` forecast is a continuous-flow team — render it in **item/week/date** vocabulary (`remaining_count`, P50/P80/P95 *dates*), never sprint/points language; `status: "insufficient_flow_history"` gets an explanatory line ("needs ≥4 weeks of completed-work history"), never a blank widget. Both bases are real Monte Carlo, so P50/P80/P95 percentile vocabulary stays honest (rule 166 reserves percentiles for simulation, which both are). Reference: `SprintForecastWidget`/`SprintForecastChips` (the branch), `FlowAnalyticsPanel` (`features/board/`). Flow charts (CFD stacked area, weekly throughput bars) use **Recharts with `var(--color-*)` fills + `isAnimationActive={false}`** (mirror `BurnChart`, never raw hex — the design-system-v2 hex gate), and every chart carries a `<p className="sr-only">` text summary (WCAG 1.1.1; charts are `aria-hidden`). Cycle/lead time is a **P50/P80/P95 stat strip, not a histogram** — the endpoint returns percentiles, not buckets. The historical distributions are team-private (ADR-0104 `flow_metrics`, TEAM/TEAM): when `flow_metrics_suppressed` the panel body is a content-free wall (rule 165), and when in-audience it shows a legible `🔒 Team-private · aggregate only — no individual breakdown` caption (the privacy guarantee must be visible, not doc-only). Forecast **dates stay visible** under suppression — only the series are gated. The panel is **collapsed by default** (localStorage-persisted) and only fetches once expanded. The per-column WIP breach chip (`WipBreachChip`) renders on `at`/`over` **independent of the "Show WIP limits" toggle** (a breach is a signal, not an opt-in detail) and is `aria-hidden` because the column `<h2>` accessible name already announces the breach.

## Warm empty / first-run states + motion (Issue 1171)

177. **A primary surface's zero-data state is a warm `EmptyState`, not a bare "No data" line; its single entrance animation is `motion-safe:` only.** The shared `EmptyState` (`components/EmptyState.tsx`) is the v2 anatomy — a `role="status"` block with a decorative (`aria-hidden`) icon in a `72px` `rounded-full border bg-neutral-surface-raised` circle, a `text-[17px] font-semibold` `<h2>` title, optional `max-w-[380px] text-[13px]` orientation copy, and an optional CTA (the shared `<Button>`). Use it for the genuinely cold cases (Schedule read-mode `ScheduleEmptyState`, Risk register, Grid); surfaces with bespoke needs — role-flavored copy or multiple CTAs (`MyWorkEmptyState`, `BuildModeEmptyState`, `EmptyBacklog`) — keep their own component but follow the same anatomy. Pass `className="h-full …"` when the host must fill its area; the icon is always decorative (the `role="status"` + title carry the announcement). **Motion:** the block fades+lifts once via `motion-safe:animate-empty-state-in` (160ms brand ease; keyframe in `globals.css` beside `cmdk-in`) — gated at the class layer so `prefers-reduced-motion` users get the content with no animation (the v2 motion contract: motion only, never content — rule 70). Do not add a second always-on empty-state animation, and never animate content in/out without the `motion-safe:` gate.

## Pre-1.0 Enterprise-gated nav rows (Issue #1173, ADR-0029/0030)

178. **A rail / daily-path nav row for an Enterprise destination OSS cannot reach renders as an *empty extension-point slot* — never a disabled teaser or padlock (rule 231 / ADR-0266).** The rail is the OSS daily path, so a cross-program / org destination that community edition cannot route to — the cross-program Portfolio rollup — is hosted as the `nav.portfolio_section` slot in `features/shell/Sidebar.tsx` ("Organization" group): `registry.get('nav.portfolio_section').map(({ id, component: Component }) => <Component key={id} />)`. In community edition the slot has no registration, so it renders **nothing** — no disabled row, no padlock, no upsell. The enterprise module injects its portfolio nav into the slot. This is the empty-slot pattern shared with `resources_heatmap.level_loads` (issue 1614), and it **supersedes** the former disabled-grayed-row treatment (the padlock removed in #1677). Discovery of the Enterprise portfolio moves to the `/programs` seam (rule 231(b)), not a rail teaser — hiding the row does *not* read as broken OSS here because the slot, not a gap, is the contract. The slot host stays `showFull`-gated (the collapsed rail is unchanged); the enterprise edition keeps its `/portfolio` `NavLink` until the enterprise module migrates its portfolio nav onto the slot. Contrast **rule 121**: a per-row `EnterpriseBadge` link is correct only on **seam surfaces** (the Settings → Roles capability matrix), never on the daily-path rail (rule 231's daily-path-vs-seam classifier).

## Segmented controls (Issue 1221)

179. **A segmented control (`role="radiogroup"` of `role="radio"` buttons) must fill the active segment with a token that contrasts against its container surface — never the same surface token the control sits on — and selection is conveyed by fill, not text color alone.** Active = `bg-brand-primary text-neutral-text-inverse`; inactive = `bg-neutral-surface text-neutral-text-secondary hover:bg-neutral-surface-raised` (visually distinct from the active fill). The failure this prevents: a toolbar segmented control filling the active segment with `bg-neutral-surface-raised` while sitting *on* a `surface-raised` toolbar renders zero fill contrast, leaving text shade as the only selection signal (WCAG 1.4.1). Roving tabindex (`tabIndex={selected ? 0 : -1}`), Arrow (wrapping) + Home/End key handling **bound to the focusable radios** (not the non-focusable group, per `jsx-a11y/interactive-supports-focus`), and the rule-4 focus ring (no dark-mode override — `brand-primary` is mode-aware sage, rules 4/137; the old `dark:focus-visible:ring-semantic-on-track` override is **retired**, do not reintroduce it) are mandatory; the selection commits immediately on move. Reference implementations: `features/schedule/ScheduleToolbarToggle.tsx` (single pressed/unpressed) and `features/schedule/ScheduleViewModeToggle.tsx` (multi-option Grid↔Timeline). Pattern also in use: `RetentionPurgePage`, `CalmToolbar`.

## Product Backlog sprint-state + ranked view (Issue #1223)

180. **A backlog story's sprint-commitment state is one chip with a fixed precedence, color is the brand accent (never the on-track green), and the "By epic / Ranked" toggle gates drag.** `SprintCommitmentChip` (`features/project/backlog/components/atoms.tsx`) is the single source for a story's sprint state on the Product Backlog grooming view, with strict precedence: `sprintPending` → the shared `PendingAcceptanceChip` (ADR-0102, rule 149 — the more specific state wins so the two pills never double up on one row); else `sprintId` set → **"Pulled"** (`bg-brand-primary/10 text-brand-primary`); else **"Proposed"** (dashed-outline neutral, the recessive default). "Pulled" uses the brand sage accent **deliberately not `semantic-on-track`** — the green DoR "Ready" chip sits in the adjacent Readiness column and two greens on one row read as a single cue (rule 7/8). Every chip carries its text label (rule 7/120 — never color alone). The header subtitle is data-derived (`{N} pulled into sprint · {M} proposed{ · {K} pending}`), never a static string. The **"By epic / Ranked"** segmented control (sr-only native radios → arrow-key roving for free, rule 175/167; persisted to `localStorage['trueppm.backlog.view']`) switches between the epic-grouped **draggable** view and a **flat, read-only** list in score order ("score drives the ranked view" — manual drag-reorder lives in the By-epic view only; ranked replaces the drag handle with a static rank number and shows the parent-epic **name** as a per-row breadcrumb, since the ID column already carries the story short-id and the epic group header is gone — the rule-959 no-redundant-tag lesson). The assignee avatar (`AssigneeAvatar`, same atoms file) is decorative initials (rule 6 — the assignee name / "Unassigned" is the wrapper `aria-label`); 2+ assignees stack a `+N` overflow. The epic-header points **progressbar** pairs the `{done}/{total} pts · {pct}%` text with a `role="progressbar"` bar (rule 120 — value + label, not color alone). Deferred (needs a Release model that does not exist — ADR-0099 Product Owner surface, #758/#1214 at 0.7): the epic `R1 · Jul` release/target-date line.
## v2 motion vocabulary — the fluidity foundation (ADR-0126, Issue 1224)

181. **All transform/overlay motion uses the shared `ease-brand` + a named `duration-*` token, and the four entrance keyframes live in `globals.css` and are applied under `motion-safe:` — never hand-rolled per call site, never ungated.** The v2 motion layer is centralized so it stays consistent and honors the motion contract (rule 70: motion only, never content).
    - **Tokens (`tailwind.config.ts`):** `ease-brand` = `cubic-bezier(.2,.7,.2,1)` is the standard easing for every transform/overlay transition; durations are the named `duration-fast` / `duration-base` / `duration-slow` (120 / 200 / 320ms, aliasing `--dur-1/2/3`). Do not hard-code `duration-150`/`duration-200` or rely on Tailwind's default ease for v2 surfaces. The `checkpop` spring is the one exception — it uses the overshoot ease `cubic-bezier(.34,1.56,.64,1)` for its 1.22 bounce, baked into the keyframe.
    - **Keyframes (`globals.css` `@layer utilities`, beside `cmdk-in`/`empty-state-in`):** `checkpop` (220ms, task-complete spring), `toast-rise` (240ms, global toast entrance), `modal-scale-in` (180ms, dialog/modal panel), `save-bar-slide` (220ms, docked save bar). Each has a paired `.animate-*` class applied at the call site under `motion-safe:` — the keyframe itself carries no `@media` guard (the gate is at the class layer, matching the established pattern). Do not add the dead `tailwindcss-animate` utilities (`animate-in`/`fade-in`); they emit no CSS.
    - **Micro-interactions:** the shared `<Button>` presses via `motion-safe:active:translate-y-px`; interactive cards (`ProgramCard`, `BoardCard`) lift via `motion-safe:hover:-translate-y-px`. **No shadow on the lift** (rule 1) — the card's own border (or a `hover:border-*` emphasis on a single-state border) supplies the depth cue the prototype's hover shadow would have; on a card whose border is state-driven (e.g. `BoardCard`'s critical/dashed border) use translate-only so the lift never overrides the state border. `shadow-pop` remains allowed only on true pop surfaces (toast/popover/drawer/modal/command palette — rule 1's standing exception).
    - **Coexisting transitions:** when a transform animation must share an element with an existing color/opacity transition, use ONE multi-property declaration (`transition`, or `transition-[opacity,transform]`) — never stack `transition-transform` on top of `transition-colors`/`transition-opacity`, which collide on `transition-property` (the later one in the cascade silently wins and drops the other).

## Per-user view visibility — "Customize views" (Issue 220, ADR-0139)

182. **Per-user view visibility composes on top of the methodology filter, never instead of it, and Overview is never hideable.** The "Customize views" menu (`ViewsMenu.tsx`, mounted in the `TopBar` right cluster before `MethodWorkspaceLabel`; self-suppresses off-project and on `/settings/*` like `ViewTabs`) toggles only views that survive `groupedVisibleViews(methodology)` — a view the methodology preset hides (ADR-0041) never appears as a toggle. The effective bar is `groupedVisibleViewsForUser(methodology, hiddenViews)` (methodology filter → personal hidden-set filter → role gate, in that order). `overview` (`STANDALONE_LEADING`) is rendered outside the personal filter and has no toggle — this is the structural "nav never empty" guarantee (no runtime min-count check). A hidden view's route still renders (cosmetic hide only — never a 404/permission change); it stays reachable via the menu and a ⌘K "Go to {label}" jump (`useCommandItems`, `current` group, `tag: 'View'`). The hidden set is the per-user **global** `UserProfile.hidden_views` (server fact on `/auth/me/`, read via `useCurrentUser().hidden_views`; written via `useUpdateHiddenViews` PATCH to `/auth/me/profile/` with the full desired set, optimistic local state reverted on error). Toggle rows are `role="menuitemcheckbox" aria-checked` with an eye/eye-off glyph swap as the WCAG 1.4.1 signal (label dimming alone is not enough; hidden-label color floor is `neutral-text-secondary`, never `-disabled` — rule 169). "Reset to {methodology} default" clears only the methodology-visible hidden keys, preserving global hides for views not shown on this project. Render metadata (label + icon) is shared via `viewMeta.ts` (`VIEW_TAB_META`) so the bar, the menu, and the palette never drift; the server-side hideable vocabulary lives in `profiles/constants.py::HIDEABLE_VIEW_KEYS` (keep the two in sync). Mobile `BottomNav` does **not** apply the personal set in 0.3 (deferred). The `/me/settings/general` variant (`ViewVisibilitySection.tsx`) is the same preference as a plain `role="switch"` form (no methodology context → lists all hideable keys, "Reset to default" clears all). Reorder and a future admin-pin/force-hide are out of scope (the admin-lock is Enterprise; this seam — the single composition function — is where it would register, with a *visible* disclosure indicator, never a silent lockout).

## Global toast system (Issue 1225, ADR-0126)

183. **App-wide confirmations use the global `toast()` API + the single `ToastHost`; board-local transient notices stay in `BoardDropNotice` (rule 170).** The toast system (`components/Toast/`) is a Zustand store (`toastStore`, pure — auto-dismiss timing lives in the host) + an imperative `toast.success/info/error/warm(message, durationMs?)` API callable from anywhere (mutation `onSuccess`, event handlers, plain modules — it reads the store via `getState()`, no hook) + ONE `ToastHost` mounted once in `AppShell`. The host is a bottom-center stack of inverse **ink pills** (`bg-neutral-text-primary text-neutral-text-inverse`, always high-contrast against the canvas in both themes), each rising in via `motion-safe:animate-toast-rise` (rule 181) with a decorative sage `✓` on success/info (`aria-hidden` — the message text carries the announcement), auto-dismissing after `durationMs` (default ~2.6s). The wrap is `role="status" aria-live="polite" pointer-events-none` so toasts are announced without stealing focus and never block the UI beneath; individual pills re-enable pointer events. `shadow-pop` is allowed (a toast is a pop surface — rule 1's standing exception). **Fire it only for genuinely app-wide events** (task created/completed/saved, pin/unpin, theme); do NOT route board-local pending-state notices here — rule 170 keeps `BoardDropNotice` board-scoped, neutral-toned, and off the global toast library.

## Task-complete celebration (Issue 1226, ADR-0126)

184. **Marking a task complete fires the checkpop spring + a warm toast from the completion *call site's* success path — never from the generic edit mutation — and the contributor surface (My Work) gets a one-tap checkbox.** Completion is the v2 "moment of delight": the check element plays `motion-safe:animate-checkpop` (rule 181, one-shot, cleared on `animationend`) and `toast.warm("Nice — {name} done.")` fires (rule 183). Wire it at each completion call site's `mutate(…, { onSuccess })` — `MyWorkTaskRow.completeTask` (the checkbox + the picker's Complete entry, `useMyWorkStatusUpdate`) and `TaskListRow.handleToggleComplete` (Space / context-menu, `useToggleComplete`, **only when transitioning INTO complete**, never un-completing) — **NOT inside the generic `useUpdateTask` / `useMyWorkStatusUpdate` hook bodies**, which also serve non-completion edits (name/dates/other status moves) and would over-fire. The spring fires immediately on click for snappy feedback; the warm toast fires on confirmed success (the mutation is optimistic with rollback — never celebrate a reverted completion). My Work's checkbox is a 44px touch target on mobile / compact on md+ (`h-11 w-11 md:h-7 md:w-7`), `aria-pressed`, disabled-when-complete (un-complete via the status picker), the 18px box carrying the sage fill + `✓` when complete. The schedule task list has no checkbox (completes via Space/menu) so it gets the warm toast only; board complete is a follow-up.

## Overlay motion (Issue 1227, ADR-0126)

185. **Overlay entrances use the shared keyframes, and the dead `animate-in`/`fade-in` utilities are never used (no `tailwindcss-animate` in this repo — they emit no CSS).** A centered dialog/modal **panel** scales in via `motion-safe:animate-modal-scale-in` (rule 181, 180ms); its **scrim** fades via `motion-safe:animate-scrim-fade` (150ms — replaces the old dead `animate-in motion-safe:fade-in`); the new-task modal's scrim also carries `backdrop-blur-[2px]`. A docked **Save bar** (settings + the task drawer, both gated on `dirty`) slides up via `motion-safe:animate-save-bar-slide` (220ms). The desktop **task drawer** slides on `transition-transform duration-slow ease-brand` (proto .26s), never the default ease; per rule 89/164 it has **no desktop scrim** (non-modal). On create success the new-task modal fires `toast.success("Created {name}")` (rule 183) — `onSuccess`/post-`mutateAsync`, never optimistically. A **bottom sheet** (`BottomSheet`, mobile) keeps its own `translate-y` slide-up — the scale-in is for centered surfaces only. Every entrance is `motion-safe:`-gated at the class layer (rule 70); the keyframes live in `globals.css` beside `cmdk-in`. Reference: `TaskFormModal`, `SettingsShell`, `TaskDetailDrawer`, and the confirm/editor dialogs (`ConfirmDiscardDialog`, `DeleteConfirmDialog`, `SessionExpiredBanner`, `WebhookEditorModal`, `ConnectedAccountsPage`).

189. **The desktop Schedule view has exactly ONE Monte Carlo forecast surface (`ScheduleForecastBar`), the percentiles render exactly ONCE on it, and every forecast date routes through `lib/formatUtcDate` (ADR-0144).** The former two-surface split — a top `MonteCarloRow` and a bottom `ScheduleInsightsBar` — is deleted; both rendered the P50/P80/P95 percentiles (up to three times total) and *disagreed on the calendar day* because some formatters omitted `timeZone:'UTC'` and drifted a day west of UTC. **Server ISO dates are UTC and authoritative** — never format an MC/forecast date with a bare `toLocaleDateString`/`new Date(iso).getDate()`; always use `fmtUtcShort`/`fmtUtcLong` (or pass `{timeZone:'UTC'}` explicitly). `ScheduleForecastBar` is desktop-only (`hidden md:block`); mobile keeps `MobileMonteCarloCard` + `MonteCarloSheet`. It owns the MC hooks (`useMonteCarloResult`, `useRunMonteCarlo`, history), the stale/recomputing machinery (`mutationVersion` → stale; `lastRunAt` change → clear; `runMc.isPending` → recomputing, `data-testid="mc-recomputing"`), and the single no-result "Run a simulation" prompt — there is no second prompt to double-claim it. The collapsed header shows the percentile chips once (P80 is the commit, accented `text-semantic-at-risk`, with the server-owned `+Nd vs CPM` delta — never a client recompute) plus the top sensitivity driver and three distinct affordances: the maximize/minimize **toggle** (`aria-expanded`/`aria-controls`, persisted to `localStorage['schedule.insightsExpanded']` — exact key), **Rerun** (`aria-label="Rerun Monte Carlo forecast"`), and **Details** (`data-testid="mc-details-btn"`, opens `MonteCarloDetailPanel`). Expanded body = `MonteCarloHistogram` + `SensitivityList` + `ForecastHistorySection` — **no `ForecastStat` triplet** (that was the duplicate). Entrance motion is the shared `animate-empty-state-in` only (rule 177/181). `MonteCarloHistogram` distinguishes `buckets.length === 0` (cold / not-persisted past cache TTL → "Run a fresh simulation" prompt, `role="img"`) from `buckets.length === 1` / `p50===p80===p95` (genuine zero-spread → "every simulation finished on {date}" prose) — never claim a converged date the run never had. The run-history list is visible to all project members; `triggeredByName` attribution is gated server-side (render only when present), a workspace with history off returns `enabled:false` (quiet "Run history is turned off for this workspace." note, not an empty shell), and a past run's persisted distribution loads on demand via `useMonteCarloRunDistribution` (`?expand=distribution`). A rerun fires NO push notification.

## Task notes (Issue 740, ADR-0143)

190. **Notes is a SEPARATE drawer section from Comments — flat, pinned-first, immutable rows; never folded into the comment thread.** The task's why/decision log lives in `NotesSection` (`features/schedule/sections/NotesSection.tsx` + `NotesComposer.tsx`, hooks in `useTaskNotes.ts`), registered on the **activity** tab at **priority 480** — just above Comments (500) — so the record-of-decisions reads before the discussion. Notes differ from Comments deliberately: no threading/replies, no @mention, no reactions/acks, no attachments; each row is one author + timestamp + body, ordered pinned-first then newest (the server orders; the client does not re-sort). Pin is open to any MEMBER+ (`usePinNote`); edit is author-only within the window (rule 192); delete is author-or-ADMIN. All write controls gate on `editable = canEdit ?? canEditTask(userRole)` (rule for ADR-0133) — a Viewer sees every note but no composer/pin/edit/delete. The **freshness signal** is the server-annotated `latest_note_at` (mapped to `task.latestNoteAt`), surfaced as a `📝 {relative}` chip on the board card (`BoardCard`, non-compact only) and on the schedule row name's composite title/aria-label + a `📝` marker (`TaskListRow`) — the Gantt bar is canvas-rendered so it carries no DOM tooltip; the row name is the carrier. WS `task_note_*` events invalidate `['task-notes', taskId]` **and** `['tasks', projectId]` (so peers' freshness chips refresh). The `decision` field exists on the `TaskNote` type as the #748 seam but is **never surfaced** in this UI.

191. **Card-scoped dim-search dims non-matches to `opacity-30` — permitted ONLY because the dim is transient, user-initiated, and never the sole signal.** The notes search filters the already-fetched list client-side (substring over body + author name); matches stay `opacity-100`, non-matches drop to `opacity-30` (they remain visible, not removed). Dimmed text at 0.3 is below the AA contrast floor, so it is allowed **only** with all of: (a) the dim is driven by the user typing and clears on empty/`Escape`; (b) matched bodies carry a `<mark>` highlight (`bg-brand-primary/20`) so the match is conveyed by more than opacity; (c) a `role="status" aria-live="polite"` "N of M notes" counter announces the result count to AT. Never use `opacity-30` as a persistent or load-time state for text the user must read — it is a search-affordance pattern, not a disabled/secondary style (use `text-neutral-text-secondary`/`-disabled` tokens for those).

192. **The 15-minute self-edit window is a quiet affordance, never a ticking countdown — show Edit only while editable, and let the server be the authority.** An author may edit their own note's body within 15 min of `created_at`; the Edit button renders only when `own note && Date.now() - created_at < EDIT_WINDOW_MS` (`NotesSection`). There is NO live countdown timer (rule against anxiety-inducing chrome) — the affordance simply appears while open and is absent once closed. The client check is convenience only: the server (`NOTE_EDIT_WINDOW_SECONDS`) is authoritative, so a save that races past the window returns 403 and the inline editor surfaces "Couldn't save — the 15-minute edit window may have closed." A set `edited_at` renders a non-anxious `· edited` marker (mirrors the comment pattern). Do not block the save button on the client clock — let the optimistic edit attempt and show the server's verdict.

## Mobile board reflow (Issue #853, v3 case 8)

193. **On a phone the Kanban board reflows into full-width snap-scroll status columns with a dot-strip map above; the reflow is gated behind `isMobile`, snap is native CSS (no JS scroll animation), and the phase grouping collapses to a flat per-status list.** Below `md` (the board's `useBoardDensity().isMobile`, matchMedia `max-width: 767px`) `BoardView` renders `MobileBoard` instead of the desktop phase × status grid — the grid is unreadable on 375px, so each **status column** becomes a full-width page (`min-w-full snap-start`) inside a `snap-x snap-mandatory` scroller, and the phase swimlanes collapse: each column shows a flat list of its cards across every phase (`mobileTasksByStatus`, derived from `phaseTaskMap` so the same task-level filters + sort carry through). Desktop is unchanged — never restyle the grid, branch a new tree behind `isMobile`. **Snap is native**: no JS scroll animation, so it is inherently `prefers-reduced-motion` safe; the only programmatic scroll (strip tap → `scrollIntoView`) downgrades `behavior` to `'auto'` under `prefers-reduced-motion: reduce` (rule 70). The active column is tracked by an `IntersectionObserver` on the scroller (guard `typeof IntersectionObserver === 'undefined'` for jsdom/old browsers — tap-to-jump still works without it). The **dot-strip** (`MobileColumnStrip`) is the map: one `<button>` per column (min-h-44px, rule 5) carrying a health dot (`aria-hidden`, same status-vocabulary color as the desktop column-header dot — rule 6), the column's **first word**, and its count; the active segment's bar fills solid (`bg-neutral-text-primary`) and is marked `aria-current` (the non-color signal — never rely on the bar fill alone). The strip group is `role="group" aria-label="Board columns"`, **not** a tablist (the columns stay mounted; nothing swaps). **Card anatomy, the WIP pill, the critical (red left-border) / blocked treatment, and the status vocabulary are unchanged** — reuse `BoardCard` as-is; only the layout reflows. Reference: `features/board/MobileColumnStrip.tsx` + `MobileBoard` in `BoardView.tsx`.

194. **Build-mode's hint strip is contextual chrome, not persistent chrome — mount it only while the user is engaged (`focus.state.mode !== 'NoSelection'`); the always-on discovery affordance is the toolbar pill, not the strip.** `BuildModeHintStrip` shares the Schedule's bottom band with `ScheduleForecastBar` (the P50/P80/P95 signal). An always-on 28px strip of static `↑↓ Select row` hints stacked above the forecast was poor real estate (#1250, VoC avg 4.0/10) — discoverability chrome must not permanently subordinate a headline signal. So `ScheduleView` gates the strip on focus state: idle (`NoSelection`) renders no strip and the Forecast bar sits flush; focusing a row (`RowFocused`) or editing a cell (`CellEdit`) reveals it with that mode's three contextual hints. Discoverability is preserved by the **always-on `BuildModePill`** in the toolbar (keyboard glyph + click → cheatsheet) — that is the persistent "build mode exists" signal for first-time users, so the strip is free to be ephemeral. Keep the reveal **policy in the caller**: `BuildModeHintStrip` stays total over `FocusMode` (it still renders `NoSelection` hints when unit-tested directly); only `ScheduleView` decides when to mount it. Two consequences of mounting-on-focus: (a) the strip carries **no live-region semantics** — a `role="status"`/`aria-live` here would re-announce on every `NoSelection→RowFocused` / `RowFocused↔CellEdit` transition (aria-live churn for decorative chrome); the AT signal for build mode is the always-on pill (clear `aria-label`) + the accessible cheatsheet, so the strip stays in the reading order (cheatsheet button keeps its name) but is never auto-spoken; (b) it enters with `motion-safe:animate-save-bar-slide` (the docked-bottom-bar vocabulary, rule 70) so the reveal slides up rather than popping. The general principle: a discoverability surface that competes with a data signal for the same band must yield the band when idle.
195. **Entity Settings is ONE scrolling page per entity (workspace/program/project): every section is an anchored `<section data-settings-section="<id>">` region mounted together, the left rail is a scroll-spy (buttons, not links), and the dirty/discard save bar is a single surface keyed across all sections.** (ADR-0146, supersedes ADR-0061.) Do **not** add a new settings section as its own route + `lazy()` chunk under an `<Outlet/>` — that was the per-section architecture that flashed/refetched on every nav. Instead mount the section's existing form inside a `<SettingsSection id="<slug>">` on the entity's consolidated page (`ProjectSettingsPage`/`ProgramSettingsPage`/`WorkspaceSettingsPage`) and add a rail entry (no `to` → scroll-spy button; a `to` makes it a real route departure for tool areas like System Health). `SettingsSection` wraps children in a `SettingsSectionContext.Provider` (section identity) and a `SettingsSectionErrorBoundary` (blast-radius containment — because all sections mount at once, an unguarded render throw would hit the **root** boundary and replace the whole app; the boundary degrades the one section to a contained "unavailable" message, mirroring ADR-0050 §Error containment). The save store (`useSettingsSaveStore`) is a **keyed** multi-section registry: each section registers under its id via `useDirtyForm`, the bar aggregates dirtiness across sections, **Save** commits every dirty section sequentially (short-circuiting on the first rejection with that section's message), **Discard** reverts them all. A rail **section** click scroll-spies in place — it must NOT trip the dirty guard and must NOT change the route; the guard fires only on a real departure (scope switcher, or a `to`-routed rail item). Deep links (`…/settings#<slug>`) scroll to the section on load (instant under `prefers-reduced-motion`, else smooth, focusing `[data-settings-section-heading]`); legacy `…/settings/<slug>` paths are sibling redirect routes (`SectionRedirect` → `…/settings#<slug>`) — they must be siblings of the consolidated route, never nested children, because the consolidated page renders **no `<Outlet/>`**. **E2E consequence:** all sections render at once, so a label/heading/button that appears in multiple sections is a strict-mode collision — scope locators to `page.locator('[data-settings-section="<id>"]')`; rail section nav is a `button` (`exact: true`), not a `link`; test the dirty guard via the scope switcher, not a section click; and a data-driven section needs its object endpoint mocked with its real shape or it renders the contained error fallback. Reference: `features/settings/SettingsShell.tsx`, `SettingsSection`, `SettingsSectionErrorBoundary.tsx`, `hooks/useScrollSpy.ts`, `hooks/useSettingsSaveStore.ts`.

## Methodology cascade (ADR-0107, issue 955 / issue 1169)

196. **Methodology cascades Workspace → Program → Project but is NOT-NULL at every scope, so inheritance is POLICY-driven, not override-presence driven — never reuse the `InheritableSelectField`/`null`-sentinel pattern for it.** Unlike `iteration_label` and the sharing toggles (where `null` on a lower scope means "inherit"), `methodology` carries a concrete `AGILE`/`WATERFALL`/`HYBRID` at every level. The inheritance switch is the **workspace** `methodologyOverridePolicy` (`suggest` | `inherit` | `enforce`), exposed on `WorkspaceSettings`. The per-scope picker is therefore a plain radio group whose **editability is gated on the policy**, not a tri-state inherit control: `locked = ws.methodologyOverridePolicy === 'inherit'` (OSS never has an active Enterprise `enforce` provider, so `enforce` behaves like `suggest` on the client — only `inherit` locks). When locked, the picker is read-only and **displays `project.effective_methodology` / `program.effective_methodology`** (the server-resolved workspace default), NOT the scope's own stored override; when unlocked, it shows and edits the scope's own `methodology` and surfaces `inherited_methodology` as informational context. The server is the source of truth — a methodology PATCH under lock returns **403** — so the client lock is a render-gate that spares a doomed save, layered on top of the existing Admin+ `canEdit` gate (`editable = canEdit && !locked`). **Tab/view gating reads the SERVER-RESOLVED value, never the raw override**: `ViewTabs`, `ViewsMenu`, `BottomNav`, and `MethodWorkspaceLabel` all read `project.effective_methodology` (default `'HYBRID'` until loaded) so a workspace `inherit` lock correctly hides the other methodology's chrome. The Enterprise-only `enforce` policy radio is `disabled` with an `EnterpriseBadge` and its click is a no-op (it must never become the selected value on the OSS surface). Reference: `WorkspaceMethodologyPage`, `ProjectMethodologyPage`, `ProgramGeneralPage` methodology row, and the `effective_methodology`/`inherited_methodology` fields on `useProject` + the `Program` type.

## Inheritable set (allow-list) field (ADR-0153, issue 976)

197. **A cascading set field (an allow-list that inherits Workspace → Program → Project) is a TRI-STATE override (`null` = inherit, `[]` = explicit empty, `[...]` = explicit set), rendered with `InheritableMultiSelectField` — never collapse the empty set and the inherit sentinel into one.** This is the set analog of the `InheritableToggleField`/`InheritableSelectField` family (rule for ADR-0135/0144): the lower scope's own column is `string[] | null` where `null` means "inherit the parent's resolved value" and `[]` means "explicitly allow nothing" — two distinct states the UI must keep separate (an empty allow-list with attachments on is a real, deliberate "no file can be uploaded" policy, surfaced as a `role="note"` warning keyed off the **resolved effective** value, not the raw override). `InheritableMultiSelectField` wraps an inherit/override `role="radiogroup"` pair around an `AttachmentTypesChecklist`: "Inherit (N types)" emits `null`; "Override" **seeds the checklist from the inherited set** (`onChange(value ?? inherited)`) so it opens on reality, not empty; a non-blocking delta line (`Same as parent` / `Narrower than parent (removed: …)` / `Wider than parent (added: …)`) compares the override to `inherited` and is informational only — it never prevents diverging. Below Admin (`!canEdit`) it collapses to a read-only comma-joined summary + provenance + "View only.", one composite `aria-label` with `aria-hidden` children (rule 171). The checklist itself uses **native `<input type="checkbox">` + `<label htmlFor>`** grouped under `<fieldset><legend>`, where the legend is a group checkbox that goes `indeterminate` on partial selection and toggles all children; the **permanent security denylist** (`text/html`, `image/svg+xml`, `application/xhtml+xml`) renders in an "Always blocked" group as DISABLED `aria-disabled` rows that are **NOT real inputs** — they can never enter the value, mirroring the server's subtract-the-denylist guarantee in the UI. The catalog + `labelForMime` fallback live in `lib/attachmentTypes.ts` (keep in step with the backend system default; unknown server MIMEs fall back to the raw string). The Workspace (root, non-null) uses a bare `Toggle` + bare `AttachmentTypesChecklist`; Program/Project use `InheritableToggleField` + `InheritableMultiSelectField`. The client allow-list that gates the task-drawer upload validator is the project's **server-resolved** `effective_allowed_attachment_types` threaded into `AttachmentDropZone`/`useTaskAttachments` — there is no static client-side MIME Set anymore (the old `ALLOWED_ATTACHMENT_MIMES` is gone). When the resolved `effective_attachments_enabled` is false, the drawer keeps listing/downloading existing files but replaces the add-controls with one muted `role="note"` line (shown only to users who would otherwise have the controls). Reference: `InheritableMultiSelectField`, `AttachmentTypesChecklist`, `WorkspaceAttachmentsPage`/`ProgramAttachmentsPage`/`ProjectAttachmentsPage`, `AttachmentSection`/`AttachmentDropZone`, and `lib/attachmentTypes.ts`.

198. **A multi-`<fieldset>` checklist and its permanently-disabled (server-denylist) rows must be programmatically named and explained, not left to per-group legends alone.** Two a11y requirements for the rule-197 checklist family (and any future grouped-checklist control): (a) the checklist container that holds several `<fieldset>`s carries a single `role="group"` + `aria-label` (threaded from the field's `ariaLabel`) so screen-reader users hear one accessible name for the whole region — the per-group `<legend>`s name the *groups*, not the collection; (b) a row rendered DISABLED to mirror a server-enforced denylist (`aria-disabled="true"`, not a real input) must point at its reason via `aria-describedby` (e.g. a shared "Blocked for security and can't be enabled." note with an `id`) — `aria-disabled` alone leaves SR users with an unexplained dead control. Also name each `<fieldset>` via `aria-labelledby={legendId}` (the group name), not `aria-describedby` (a description). The informational delta line that changes as the user edits gets `role="status" aria-live="polite"` so the narrower/wider feedback is announced. Reference: `AttachmentTypesChecklist` (`ariaLabel`, `deniedReasonId`), `InheritableMultiSelectField`.

## Board swimlane grouping (Issue #324)

## Board swimlane grouping (Issue #324)

199. **Board swimlane grouping is a per-user *client view* (`groupBy: 'phase' | 'assignee'` in `useBoardToolbarPrefs`), never saved-view/server state — and the two modes emit the same lane shape so everything downstream stays mode-agnostic.** The board's "Group" toolbar chip is a real `role="radiogroup"` (Phase / By assignee), mirroring the Sort/Density chips. `groupBy` is an additive, backwards-compatible field on the same `trueppm.board.toolbarPrefs.v1` localStorage blob as `zoom`/`density` (a stored blob without the key defaults to `'phase'`; an unknown value coerces to `'phase'`) — it is a personal lens, not shared board config, so it does **not** live in `BoardSavedView`. Phase grouping (`buildPhases`, by WBS summary parent) is unchanged; assignee grouping (`features/board/grouping.ts::buildAssigneeLanes`) groups leaf cards by **primary assignee = the first of `assignees[]`** (matching the card chip's lead) into one `BoardLane` each, sorts lanes alphabetically by name with the **"Unassigned" lane pinned last**, **excludes summary tasks** (WBS structure, not assignable work), and returns only lanes that hold cards (no empty-assignee noise). Both modes feed the same `phaseTaskMap` (which buckets each lane's `phase.tasks` by status) and `mobileTasksByStatus` (which flattens across lanes) — so the mobile reflow (rule 193) and the drag handler need no per-mode branch. **Workshop mode always forces phase grouping** (`groupMode = workshopMode ? 'phase' : prefs.groupBy`) because workshop authors WBS phase structure. Two consequences for assignee lanes: (a) the per-lane "+ add task" affordance is **suppressed** — a lane id there is a *resource*, not a parent, so `LaneMeta` renders the add button only when `onAddTask` is provided (it is optional; `BoardView` passes none in assignee mode), never a dead button (rule 122); (b) **drag-to-reassign is deferred** — a cross-assignee-lane drop never changes the assignee, but a same-drop status (column) change still applies and the `aria-live` region announces "reassign from the card". **Deferred follow-ups, do not assume they exist:** team grouping (needs a server-side `task.team` field — team is derived via `TeamMembership` today) and cross-lane drag-to-reassign (a separate `TaskResource` multi-assignee write-path design pass; note the legacy `assignee` User FK vs `assignees[]` mismatch). Reference: `features/board/grouping.ts`, `useBoardToolbarPrefs.ts`, the Group chip in `CalmToolbar.tsx`, and the `groupMode` branch + reassign hint in `BoardView.tsx`.

## Role-context lens — "View focus" (ADR-0162, issue 1263 / issue 412)

200. **The `role_context` lens ("View focus": PM / Scrum Master / Unified) is PRESENTATION-ONLY — it re-orders and re-points already-permitted surfaces for the active user and MUST NEVER gate a write, a permission, a data fetch, or what anyone else sees.** A user in "PM mode" has exactly the same RBAC authority as in "Scrum Master" or "Unified" mode; the lens is never read in a permission class, a `canEdit`/`canManage` predicate, a mutation guard, or a query-enable condition — only in presentation code that arranges surfaces the user is *already* allowed to see. `unified` is the genuine neutral default (the value while `user` is still loading), so an unconfigured or loading user gets exactly today's behavior — **no flash of the wrong lens**. The lens does two things, both additive: (a) **default project view** — `ProjectIndexRedirect` (`router.tsx`) lands `/projects/:id` on `lensDefaultView(role_context)` (PM→`schedule`, Scrum Master→`board`, Unified→`overview`), holding (renders nothing; the `ProjectShell` chrome stays painted) until `['current-user']` resolves so it redirects once; (b) **view-bar emphasis** — `applyRoleContextLensOrder(groups, lens)` (`features/shell/lensOrder.ts`, pure) promotes the lens's priority views to the front of their *own group*, composed in `ViewTabs.tsx` **after** `groupedVisibleViewsForUser` + the role gate (it only re-orders already-permitted, already-visible views — never reveals or hides one) and **never edits the canonical view registry** (`viewMeta.ts`/`methodologyTabs.ts`). It is orthogonal to `default_landing` (rule 182 / ADR-0129): `default_landing` owns the surface at `/`; the lens owns emphasis *inside* a project. The switcher lives in two places mirroring existing patterns: an inline segmented control in `UserMenu` (`RoleContextMenuRow`, the `ThemeToggle`-style `role="group"` + `aria-pressed` toggle group with a **rule-179** `bg-brand-primary text-neutral-text-inverse` active fill — never a same-surface shade) and a radio-card group on `MyGeneralPreferencesPage` (`RoleContextRadioGroup`, the rule-167 roving-tabindex radiogroup). Both write `role_context` via `useUpdateRoleContext` → `PATCH /auth/me/profile/` with optimistic state reverted on error (the reverted selection + an `aria-live` line are the error feedback). Read it via `useCurrentUser().role_context` (server fact on `/auth/me/`, default `'unified'`). **Deferred, do not assume they exist:** bespoke PM/SM dashboards + per-lens alert widgets, a Sprint-Goal-health callout, velocity-forecast surfacing, per-team lens context, and mobile (`BottomNav`/React Native) lens parity.
## Cloud-file link preview (#571, ADR-0163)

201. **A cloud-file task link (`google_drive`/`dropbox`/`box`/`onedrive`) renders a neutral `preview_type` chip in the link right-slot, NEVER a lifecycle `StatusBadge` — status pills are reserved for git lifecycle states.** A file has no open/draft/merged/closed lifecycle, so reusing `StatusBadge` (which would show "UNKNOWN" for a file's always-`unknown` status) is wrong. `LinkRightSlot` (`ExternalLinksSection.tsx`) branches: file provider + `preview_type` set → `<TypeChip>` (a neutral `bg-neutral-surface-sunken` `text-neutral-text-secondary` chip whose visible Title-Cased label IS its content, plus `aria-label="File type: {Label}"`); file provider not yet refreshed (`preview_type === ''`) → **renders nothing** (the "never refreshed" stamp + the ⟳ button are the call to action — do NOT show an em-dash, which reads as "n/a status"); git/generic → the existing `StatusBadge` unchanged. The chip may use `text-[11px]` **specifically to match its sibling `StatusBadge` in the same slot** — the one sanctioned sub-12px exception outside `features/settings/`/`EnterpriseBadge` (rules 118/121), justified because a 12px chip beside an 11px badge breaks slot uniformity and the chip's meaning is independently carried by its `aria-label`. Do not generalize the [11px] exception beyond badge-class chips adjacent to `StatusBadge`.

202. **An external preview thumbnail is DECORATIVE chrome and MUST carry an `onError` glyph fallback — never render a remote `<img>` from an unfurled URL without one.** In `FilePreview`, the thumbnail `<img>` uses `alt=""` + `aria-hidden="true"` (the link title + description carry the meaning) + `loading="lazy"`, plus an `onError` handler that flips to a `preview_type` glyph placeholder (`previewTypeGlyph`, `text-xl`, `aria-hidden`). The fallback is load-bearing, not polish: the image host is external, so it fails **offline** (the card still renders from the synced `thumbnail_url`/`description`/`preview_type` fields — only the `<img>` fetch fails) and for **private files** (no `og:image`). The same glyph box renders directly (no `<img>`) when `thumbnail_url` is empty. `FilePreview` mounts only when `description || thumbnail_url` is present, so a not-yet-refreshed link adds no empty box and keeps the row's `gap-1` rhythm; it adds **no new focusable element**, so keyboard tab order is unchanged. `thumbnail_url` is https-only (enforced server-side) — assume the server already dropped non-https; do not client-sanitize-and-trust.

203. **The `preview_type → glyph/label` map (`lib/previewType.ts`) is the single source for cloud-file iconography and MUST stay exhaustive: an unknown key falls back to the generic `📄` / "File".** `previewTypeGlyph(type)`/`previewTypeLabel(type)` mirror the server's `PREVIEW_TYPE_VALUES` (`registry.py`); a future server-added `preview_type` the web doesn't know yet renders the generic file glyph/label rather than a blank box or a crash. Keep the map in step with the backend enum; keep `''` (no preview) handled by the *caller* (rule 201's gates), not the map — the map only translates a *known-or-unknown non-empty* type.

204. **A full-surface "drive the room" overlay (the standup walk-the-board, `StandupMode`) is a `role="dialog" aria-modal="true"` and MUST trap focus with `useFocusTrap` even though it is operated primarily by arrow keys and is often projected — AT users in the room are still real.** Four bindings that travel together (any future projected/stepper overlay inherits them): (a) `useFocusTrap(true, onClose)` owns both the Tab cycle and Escape (it listens on `document`) — do **not** also handle Escape in a window `keydown` listener or it double-fires; the window listener carries only the ← / → walk keys so they work without first tabbing to a control. (b) On advance, move focus to the person heading — a `tabIndex={-1}` `<h*>` styled with `focus:` (NOT `focus-visible:`, which a browser may withhold on a *scripted* `.focus()`) so the ring reliably renders. (c) The `aria-live="polite"` `role="status"` region carries the **position only** ("Person N of M") — the teammate name is read by the heading-focus move, so repeating it in the live region double-reads it. (d) The person-rail jump dots are a plain `role="group"` of labeled `<button>`s with `aria-current` on the active one (NOT a `role="tablist"`/`radiogroup`, which would require roving tabindex + in-group arrow handling the window listener already owns); each dot button carries a `min-h-[44px] min-w-[44px]` hit area around a small visual dot (rule 5). Every interactive control in the overlay — Exit, Prev/Next stepper, dots, card rows, links — uses the full rule-4 ring (`focus-visible:ring-2 focus-visible:ring-brand-primary focus-visible:ring-offset-1`) and ≥44px targets (Prev/Next/Exit are `min-h-[44px]`). Reference: `features/board/standup/StandupMode.tsx`, `StandupPersonCard.tsx`, ADR-0166.

## In-flow rail beside a non-modal detail drawer (Issue 1291)

205. **A `hidden lg:flex` planning/context aside that is an in-flow sibling in a `flex-row` page MUST be suppressed while a non-modal (`aria-modal="false"`) detail drawer is open in the same layout region — the drawer is `absolute right-0` and silently occludes the aside regardless of DOM order.** The `SprintPlanningRail` on the Product Backlog (issue 1291) is the reference: the page root is `relative flex flex-row`, the rail is a `w-[320px] shrink-0` in-flow right column, and `StoryDetailDrawer` is `absolute right-0 w-[480px]` (rule 89/164, non-modal so the list stays usable). Because the drawer overhangs the rail by ~160px, rendering both leaves planning context (capacity, milestone) hidden *behind* the drawer with no signal it is there. Gate the aside on "no drawer open" — `{plannedSprint && !selectedStory && <SprintPlanningRail …/>}` — so it cleanly re-appears on drawer close; the rail's utility while a drawer is open is marginal (the user is focused on that one story). Extend this to any future `flex-row` page that combines an in-flow rail with an absolutely-positioned non-modal detail drawer: the in-flow sibling yields to the overlay. (A truly modal drawer with a backdrop does not need this — the backdrop already signals the rail is dismissed.)

## Bare-mounted modal dialog must own its focus trap (Issue #1339)

206. **A dialog that declares `role="dialog"`/`role="alertdialog"` + `aria-modal="true"` MUST trap focus itself (`useFocusTrap`) — you cannot clone a confirm dialog that relied on a parent modal for focus containment into a standalone/bare mount and keep `aria-modal` honest.** The board's `DeleteConfirmDialog` has no internal trap because it is always nested inside `TaskFormModal`, which contains focus. `EpicDeleteConfirmDialog` (#1339) was cloned from it but is mounted **bare** on the Product Backlog page (rendered by `EpicHeader`, no outer modal), so without a trap Tab/Shift-Tab walks straight out behind the scrim — violating the `aria-modal` contract (WCAG 2.4.3 / 2.1.2). The fix is the house hook: `const ref = useFocusTrap<HTMLDivElement>(true, onCancel)` on the **panel** (not the scrim) — it autofocuses the first focusable (which, for a destructive dialog, must be **Cancel**, never the destructive button — so order Cancel before Delete in the DOM), wraps Tab, and routes Escape to the close handler (do NOT also add a separate `document` Escape listener — it double-fires, the rule-204 lesson). Pair this with **focus restoration**: the surface that opened the dialog (e.g. the epic kebab) must return focus to its trigger on close — for a trigger that unmounts while the action is open (the kebab is hidden during rename), restore via an effect keyed on the open/close state, not a direct `.focus()` in the close handler. Reference: `EpicDeleteConfirmDialog.tsx`, the `restoreFocusRef` effect in `EpicHeader.tsx`, `useFocusTrap`.

## Inline text control that is a row's sole open/edit affordance must meet 44px (Issue #1346)

207. **When an inline text control (a name/title rendered as a `<button>`) is the *sole* open/edit affordance for a list row or group header — i.e. the row itself is not the click target — that control MUST meet the 44px touch target (rule 5), not just the text bounding box.** Wrap it `inline-flex min-h-[44px] items-center` (a negative `-my-*` keeps the *visual* header height compact while the hit area still reaches 44px), or — the alternative — make the entire row the click target the way `StoryRow` does. A bare `text-sm px-1 py-1` text button is ~28px tall and fails rule 5 on the one element a touch user must hit. This surfaced on the Product Backlog `EpicHeader` (#1346): clicking the epic *name* opens its detail drawer (the name is the edit affordance, mirroring how a story row opens its drawer), but the epic header row — unlike a `StoryRow` — is not itself clickable, so the name button carries the whole burden of the touch target. Pair the open affordance with the same **selection ring** the sibling rows use while the drawer is open (`selected ? 'ring-2 ring-inset ring-navy-700 dark:ring-reversed'` on the row, rule 164) so the open/selected state reads consistently across stories and epics. Reference: the epic-name button + `selected` prop in `EpicHeader.tsx`, `StoryRow` in `ProductBacklogPage.tsx`.
208. **A *categorical identity* color (role chip, methodology accent, group-avatar swatch — a hue that carries no on-track/at-risk status meaning) is single-sourced in `lib/identityColors.ts` and applied via the `style` prop, NEVER as a raw `bg-[#hex]`/`text-[#hex]` arbitrary-value Tailwind class.** This is the rule-158 `programColor.ts` precedent generalized: any "N distinct, meaningless hues" surface draws its hex from `IDENTITY_SAGE`/`IDENTITY_AMBER`/`IDENTITY_VIOLET` or the `IDENTITY_SWATCHES` array, and a tinted chip uses `tintedChipStyle(hex)` (a 10% wash behind solid accent text — the `${hex}1a` alpha equivalent of the old `bg-[#hex]/10 text-[#hex]`). The reason is **not** dark-mode adaptivity — an identity hue is theme-static by definition, the same in light and dark — it is (a) single-sourcing so one hue lives in one place, and (b) keeping raw `bg-[#hex]`/`text-[#hex]` literals out of the tree so the `design-system-v2` arbitrary-color gate (`BASELINE_ARBITRARY`) stays at floor. A *semantic status* color is the opposite case: it MUST go through a status token (`text-semantic-at-risk`, `bg-brand-primary-light`, …), never `identityColors` and never an arbitrary hex. The line: does the color mean a state (→ token) or an identity (→ `identityColors` + `style`)? Reference: `lib/identityColors.ts`, the `ROLE_PALETTE` `style` threading in `WorkspaceRolesPage.tsx`/`WorkspaceMembersPage.tsx`, `IDENTITY_SWATCHES` in `WorkspaceGroupsPage.tsx`, ADR following rule 158.

## Async action launched from an overflow menu needs a persistent status region (Issue #1437)

209. **An async action wired into a `ToolbarOverflowMenu` (or any menu that closes on select) MUST surface its in-flight/disabled state in a *persistent* region — a `role="status"` toast/bar or an always-visible control — NOT on the menu item itself.** Action items call `activate()` → `setOpen(false)`, so the menu (and the item) unmount the instant the user clicks; a busy label (`'Generating…'`) or `disabled` set on the item is rendered into a closed popover the user never sees, leaving a slow operation (a 1–2s client-side rasterize, a network export) with *zero* feedback until it errors. This surfaced on the Schedule's **Export schedule as PDF** item (#1437): the `exportingPdf ? 'Generating PDF…'` label on the overflow item was invisible because the ⋯ menu closes on select. The fix is the same docked-bottom `role="status"` region the **MS Project export** uses (`ScheduleView.tsx` — "Preparing your export…" / "Generating your PDF…"), or the **board export**'s always-on toolbar button that shows "⏳ Generating PDF…" inline (`CalmToolbar.tsx`, never in a menu). The `role="status"` form is preferred for menu-launched actions — it also gives screen-reader users the busy announcement the transient menu item cannot. General principle: feedback for an action must outlive the surface that launched it. Reference: the `exportingPdf` status region in `ScheduleView.tsx`, the MS Project export status region beside it, `ToolbarOverflowMenu.activate()`.

## Hover-reveal + click disclosure must source aria-expanded from the toggle state only (Issue #1305)

210. **A region that is *both* a pointer hover-reveal and a click/tap disclosure MUST source `aria-expanded` and its collapse behavior from the explicit toggle state only — never add `group-focus-within:block` alongside the toggle.** Mixing the two desyncs the announced state from what is visible: tabbing into the card fires `group-focus-within` and renders the region `display:block` while `aria-expanded` is still `false` (WCAG 4.1.2 state mismatch), and the collapse toggle is *inert* for a keyboard user because focus inside the card keeps `group-focus-within` true regardless of the toggle. The correct split: `group-hover:block` is the **pointer-only** convenience (mouse users don't consume `aria-expanded`); keyboard and touch reveal flow through the toggle state (`peekOpen`) which also drives `aria-expanded`, so the announced state always matches what is visible and the collapse is never inert. Everything the toggle reveals must live inside the single `aria-controls` target — fold sibling panels (e.g. a baseline-variance sub-panel) into the controlled region rather than gating them on the same state from outside it. Reference: the worst-offender badge peek in `features/board/BoardCard.tsx` (#1305).

## Responsive rail/sidebar collapse: conditional-render on useBreakpoint, do NOT CSS-hide a duplicated control set (Issue #539)

211. **When a persistent left rail collapses into a mobile header that re-hosts the *same* interactive controls (scope switcher, context switcher, copy-link — anything queried by role/label), render exactly ONE copy via a `useBreakpoint()` conditional, NEVER two copies gated by `hidden md:flex` / `md:hidden`.** Tailwind's `hidden` is a CSS-only visibility toggle: both the rail and the mobile header stay mounted in the DOM, so every shared control exists twice. That is invisible in the browser but breaks every role/label query — jsdom (`window.matchMedia` absent → `useBreakpoint()` reports `lg`) and Playwright strict mode both resolve `getByRole('button', { name: 'Workspace' })` to two elements and throw. Extract the shared controls into small components (`ScopeSwitcher`, `ContextRow`) and render `{!isMobile && <aside…>}` / `{isMobile && <header…>}` off `const isMobile = useBreakpoint() === 'sm'` so only one instance is ever in the tree. The `SettingsShell` collapse is the reference: the 240px scroll-spy rail (md+) becomes a compact header below `md` whose scroll-spy nav is a native `<select>` "Jump to section" (`value={activeId}`, `optgroup` per nav group — OS-native, 44px, zero popover code), and `FieldRow` swaps its fixed inline `gridTemplateColumns: '240px 1fr'` for `grid-cols-1 md:grid-cols-[240px_1fr]` so label+control stack instead of overflowing a phone. Corollary for a `NavLink` whose active state must persist across sub-routes (a Settings gear that stays active on `…/settings/general`, `…/settings#access`, …): point `to` at the base the URL actually settles on (`…/settings`), because `NavLink` overrides an explicit `aria-current` prop with its own route match — a `to` that never equals the settled path renders `aria-current` unset even when your own `isActive` styling says active. Reference: `SettingsShell.tsx` (`isMobile` split, `ScopeSwitcher`/`ContextRow`, the jump-to-section `<select>`), `BottomNav.tsx` (the Settings entry's base `to`).

212. **A link to product documentation points at the canonical published docs site (`https://docs.trueppm.com/...`), NEVER an in-app `/docs/...` path.** The `docs/` and `packages/website/` trees are built and served by the Docusaurus/Astro site at `docs.trueppm.com` — the React app's router has no `/docs/...` route, so an in-app `<a href="/docs/administration/mcp-server">` is a dead link that 404s inside the SPA shell (or falls through to the not-found boundary). Render external documentation links as `<a href="https://docs.trueppm.com/<section>/<page>" target="_blank" rel="noopener noreferrer">` with the standard rule-4 `focus-visible` ring (a doc link is a standalone interactive control; the older settings text-links that omit the ring, e.g. `WorkspaceEmailPage`, predate rule 4 and are not the pattern to copy). Keep the path in step with the file's location under `docs/` (`docs/administration/mcp-server.md` → `/administration/mcp-server`). Reference: `ApiTokensManager.tsx` (the MCP "MCP server setup →" link in the connect panel), `WorkspaceEmailPage.tsx` (external issue links use the same `target`/`rel` pattern).

## Mobile bottom rail caps at 5 slots; overflow goes to a BottomSheet, never a scroller (Issue #1464)

213. **The `< md` `BottomNav` rail renders at most 5 `flex-1` cells: up to 4 primary tabs plus a "More" `<button>` in slot 5 when the reachable set exceeds 5 (ADR-0196). Overflow views open in a reused `BottomSheet` (`MoreSheet`) as ≥44px `NavLink` rows — NEVER a horizontal `overflow-x` scroller (that is the desktop strip's job, ADR-0134 rule 3; off-screen tabs are undiscoverable and fail the gloves/glare touch case).** The rail shares the desktop *set* of reachable views but not its order: compose the set the same way the desktop bar does — `groupedVisibleViewsForUser(effective_methodology, hidden)` where `hidden = user.hidden_views ∪ surfaceHiddenViews(effective_surface_visibility)` (ADR-0139 hide now applies on mobile; ADR-0193 `reporting` hides `reports`), then apply the `resources`→Scheduler+ role gate and prepend `overview` / append `settings` (both standalone, never hideable). Then split with the pure `selectMobileNav(reachable, methodology)` (`bottomNavItems.ts`) which orders methodology-priority-first (`MOBILE_PRIMARY_PRIORITY`) then canonical, caps at 5, and returns `{primary, overflow}`. Keep this mobile ordering **out of** `methodologyTabs.ts` (that file owns the desktop grouped bar and churns under ADR-0195). Two invariants: (a) `overview` always leads and `today` is always primary — do not let desktop group order (PLAN-first) bury Today (regresses #1324); (b) when a surface parked in the overflow is the active view, the More button must read active (`text-brand-primary font-medium`) and enrich its `aria-label` to `` `More, ${label} selected` `` so a view living in More (e.g. Settings) still reads as the active surface (issue 539). The More button is a dialog trigger — use `aria-haspopup="dialog"` + `aria-expanded`, NOT `aria-current` (that keyword is for the current-page link; use it on the primary tab `NavLink`s and the active overflow row instead). `BottomSheet` traps focus but does not restore it — the caller returns focus to the More button (`moreButtonRef.current?.focus()`) in `onClose`. When ≤5 views are reachable, render them all as tabs and omit More.

214. **Rail tabs, the "More" trigger, and `MoreSheet` nav rows use `focus:ring-*`, not `focus-visible:ring-*`.** They are standalone tab/menu controls — `focus-visible:` withholds the ring on pointer-driven focus in Firefox/desktop Safari (the ux-review HIGH gate). This is the rule-4/ux-review focus rule applied to the mobile rail; when rebuilding a rail file, convert any pre-existing `focus-visible:` on its controls to `focus:` in the same change rather than carrying it forward. Do NOT reuse the backlog `FOCUS_RING` constant (`features/programs/backlog/styles.ts`) in rail/sheet nav rows — it is `focus-visible:`-based; write `focus:outline-none focus:ring-2 focus:ring-brand-primary focus:ring-inset` inline. Reference: `BottomNav.tsx`, `MoreSheet.tsx`, `bottomNavItems.ts`.

215. **Chrome that mirrors another nav surface must derive its active-view state and its labels from the SAME shared source as the surface it mirrors — never reimplement either.** When a second navigation surface (the mobile `BottomNav`) reflects the same view set as a primary one (the desktop `ViewTabs`), three things silently diverge if reimplemented locally, and each is a ux-review finding: (a) **active view** — derive it from the path segment *after the projectId* (`pathSegments[indexOf(projectId) + 1]`), exactly as `ViewTabs` does, NOT from the last path segment; the last segment misfires on nested detail routes (`/board/<cardId>`) and leaves no tab marked current. (b) **iteration label** — resolve the Sprints/Cycles label through the shared `useIterationLabel(projectId)` chokepoint (ADR-0116 workspace→program→project inheritance), NOT off the raw `iteration_label` field; the raw field is null when the label is inherited, so a local read shows "Sprints" while the desktop bar shows the inherited "Cycles". (c) **overflow-active label** — when an overflow-parked surface is active, resolve its accessible name off the *view key* (`activeOverflowView`), NOT off `currentView`; on a sub-route like `/settings/notifications` the trailing segment is `notifications`, so a `currentView`-based label degrades the More button to "More, view selected" instead of "More, Settings selected". Reference: `BottomNav.tsx` (mirror) against `ViewTabs.tsx` (source).

216. **A status adornment inside a header decides `aria-hidden` vs `role="img"`+`aria-label` by whether its meaning is ALREADY in the header's accessible name — never by "it's a small glyph, hide it".** A header (a board column `<h2>`, a card title, a lane label) that already folds a state into its own `aria-label` — e.g. the column name computed as `` `${label}, ${count} tasks, over limit` `` — must render the *visual* chip for that same state as `aria-hidden` so a screen reader hears it once, not twice (the `WipBreachChip` case, ADR-0130 D2). But an adornment that carries **net-new information not present in the accessible name** must be an announced element (`role="img"` with an `aria-label`, plus a `title` mirroring it), or it silently vanishes for AT users. The `WipTrendArrow` (#1213) is the second case: the recent-trend direction is nowhere in the column's `<h2>` name, so it is `role="img" aria-label="trending up toward WIP limit"` with the visible ▲/▼ glyph `aria-hidden` inside it. Before adding a header glyph, check the header's computed `aria-label`: if the state is in there, `aria-hidden` the glyph; if not, label the glyph. Do NOT make a net-new-info glyph `aria-hidden` (drops it for AT) and do NOT label a redundant glyph (double announce). Direction/trend meaning must also survive color loss (rule: color-not-alone) — the glyph orientation carries it, amber (`text-semantic-at-risk`, AA-as-foreground per the token) marks only the actionable "about to breach" case, everything else is `text-neutral-text-secondary`. Reference: `WipTrendArrow` (labeled, net-new) vs `WipBreachChip` (aria-hidden, redundant) in `features/board/BoardView.tsx`.

217. **Every dialog/drawer/modal/panel that edits existing information or collects new information presents an explicit, consistent commit/discard affordance — Save (primary) + Cancel (secondary), Save disabled until dirty, a Cancel that reverts to the original values, and an unsaved-changes guard on any dirty dismiss — composed from the shared `@/components/dialog` primitives, never re-decided per surface.** The contract (issue #1361, generalizing web-rule 164 from the backlog drawers to all editable surfaces):
    - **Save is disabled until the form is dirty** (a field actually changed vs. the last-saved snapshot) and re-disabled after a successful save. Dirty is a structural `JSON.stringify(draft) !== JSON.stringify(baseline)` compare of flat scalars.
    - **Cancel discards pending edits and restores the baseline** — it never mutates.
    - **Dismissing while dirty** (Esc, scrim/backdrop click, close ✕) opens the **unsaved-changes guard** (`role="alertdialog"`, focus-trapped, default focus on the safe "Keep editing") instead of silently committing OR silently discarding. A clean dismiss closes immediately with no prompt.
    - Compose these, do not hand-roll them: **`useDirtyDraft<T>(initial)`** owns draft/baseline/dirty + `reset()` (Cancel) + `commit()` (post-save re-snapshot); **`useUnsavedChangesGuard({ dirty, onClose })`** owns the close-vs-guard decision, the guard's open state, and the document Escape listener; **`DialogFooter`** renders the Save/Cancel bar (shared `Button` primitive, ghost Cancel + primary Save, the muted "Unsaved changes" status, an inline `role="alert"` for validation/save errors); **`UnsavedChangesDialog`** is the discard prompt. Two valid compositions: a **deferred drawer** mounts `{dirty && <DialogFooter/>}` so the bar appears only while dirty (backlog drawers, rule 164); an **always-on modal** renders the footer for its whole lifetime and passes `dirty` so Save stays inert until a change. Reference implementations: `EpicDetailDrawer.tsx`, `StoryDetailDrawer.tsx`.
    - **Instant-toggle carve-out (must be visually distinct, never batched behind a Save bar):** a single non-destructive control whose change *is* the action — a lone switch/checkbox that optimistically PATCHes on toggle, a child-collection row on its own flat endpoint (acceptance criteria), a server-gated semantic toggle the surface reflects live (Definition of Ready). These commit immediately and correctly have **no** Save/Cancel/guard, because there is nothing staged to lose. The line: *does a dismiss risk losing staged edits?* If yes → the surface needs the footer + guard; if the change already persisted on interaction → it is an instant toggle and is exempt. Never give an instant toggle a Save bar (implies a batch that isn't there), and never let a surface that stages multiple scalar edits commit-on-dismiss without a guard.
    - **Settings pages are the shell-scoped specialization:** inside `/settings` the equivalent contract is provided by `useDirtyForm` → `useSettingsSaveStore` → the `SettingsShell` save bar + its own `ConfirmDiscardDialog` (rule 115). Settings forms use *that* mechanism, not `@/components/dialog`; the two are parallel implementations of the same rule for different mount contexts.
    - **Documented deviation:** the Schedule `TaskDetailDrawer` (#962, "B-lite") is a VoC-tuned exception — its Description/name stage behind a save bar but **flush** (auto-save) on blur / tab-switch / close rather than opening a guard, and its per-section editors autosave immediately. This predates this rule and its flush-on-close is a deliberate design decision, not a bug; do not "fix" it to a guard without a fresh VoC pass. New surfaces follow the guard model above.

218. **Public auth/recovery screens (login-adjacent, outside `RequireAuth`) share the `AuthShell` centered-card primitive and treat any client-side password meter as advisory-only.** The password-reset flow (issue 765, ADR-0209) added `features/auth/passwordReset/AuthShell.tsx` — a mobile-first centered card with the brand lockup, an optional 3-step progress-dots indicator (`role="img" aria-label="Step N of 3"`), title/subtitle, body slot, and an optional "Back to sign in" footer link, all on theme-aware neutral/brand tokens (light+dark). New public auth surfaces (email verification, SSO-error, future recovery steps) render inside `AuthShell` rather than re-rolling a centered card, so the whole unauthenticated set stays visually one product. Two conventions travel with it: (a) an inline **requirements checklist** built from icon + color must also carry the state in text for AT (rule 6) — client-verifiable items toggle a check/dot with an `sr-only` "Met:"/"Not yet met:" prefix, and a server-only item (e.g. "not a previously used password") stays neutral `text-neutral-text-secondary` (never `text-disabled`, which fails 1.4.3) with no met/unmet prefix since it is unknown until submit; (b) the **strength meter is advisory** — the score/label and the two live checks gate the submit button, but the server's `AUTH_PASSWORD_VALIDATORS` + reset-confirm policy are the authority, so never present the client meter as the enforcement. Reference: `AuthShell.tsx`, `ResetPasswordConfirmPage.tsx` (`RequirementItem`, `StrengthBar`).

219. **A bulk-action control that displays an affected count `(N)` must derive its enabled/visible state — and, where it can, its `N` — from the *actionable subset* for that verb, not the raw filtered total; when no row in the current filter is actionable for that verb the control hides (or disables), mirroring the per-item gate.** When a surface gates a single-item action by an actionability predicate (status, permission), its bulk counterpart over the same set must apply the same predicate. Showing the raw filtered `N` on a bulk button whose server-side effect is only a subset produces a dishonest affordance — it promises `N`, delivers fewer, and in the worst case (a terminal-only filter) renders a dead control that promises `N` and does `0`, plus a confusing count/result mismatch between the button, the confirm dialog, and the result toast. When the client only holds the current page (so it cannot compute the exact server-wide actionable count), gate visibility on the filter that *can* contain actionable rows and let the confirm-dialog body copy + the result toast carry the true processed count; prefer a server-provided actionable count when one is available. Reference: the dead-letter inspector bulk bar (`DeadLetterInspectorPage.tsx` — `canBulkRequeue` gates Requeue-all to `dead`/`pending_retry`/all, `canBulkDrop` hides Drop-all under a `dismissed` filter; #695, ADR-0210), mirroring the per-task `canRequeue`/`canDrop` gate in the same file.

220. **A live region (`role="status"` / `role="alert"` / `aria-live`) must not carry a per-tick-updating accessible name — announce discrete state transitions (start / stop / appear), never a running clock or counter; put the ticking value in an `aria-hidden` element and keep the region's name stable.** A live region whose accessible name embeds a value that changes every second (a running timer, an elapsed counter, a live countdown) fails two ways depending on the AT: screen readers that recompute the name re-announce roughly once per second (an announcement flood), and those that key off subtree text announce nothing on the state change that actually matters (start/stop). The region's name must reflect *what changed* (the timer started on task X; it went stale), not *the current value*. Keep the digits in a sibling `aria-hidden` span. Reference: the running-timer chip `TimerChip.tsx` (#1415, ADR-0185 §C) — `role="status"` named `Timer running on {task}` (plus the discrete `— running a long time` stale suffix), with the `formatElapsed` clock rendered in an adjacent `aria-hidden` span; contrast the safe `TaskRunIndicator` whose name changes only on discrete count changes. A non-live control (a plain `<button>`) may hold a ticking label — this rule is specifically about live regions.

221. **A control that is the *sole* affordance for a primary action meets the ≥44px touch target even on a desktop-dense data surface — density is not a licence to shrink the only way to do the thing.** A glyph-only stepper/pager (`‹` / `›`), the single button that changes the data the whole view is about, must have a ≥44px hit area regardless of how compact the surrounding grid is; `px-2 py-1` around a one-character glyph (~22px) is a ux-review finding even under a 2.1-AA scope because on the mobile/tablet horizontal-scroll layout it is the primary navigation. Give each button `inline-flex min-h-[44px] min-w-[44px] items-center justify-center` and center the glyph; let the *content cells* be dense (a spreadsheet row may be `h-9`), but never the sole navigation control. Reference: the timesheet week-stepper (`features/timesheet/TimesheetPage.tsx`, #1435) — contrast the dense `h-9` grid cells in the same feature, which are acceptable because they are not a sole affordance.

222. **When a `role="grid"` navigates by Tab-through-`<input>` (not a roving-tabindex arrow-key model), every *non-input* cell that carries user-relevant state must be `tabIndex={0}` so its `aria-label` is reachable in a screen reader's focus mode — a `title` tooltip must never be the sole channel for "why is this cell different?".** A grid that announces `role="grid"` primes AT users for arrow-key cell traversal; if the real model is tabbing between editable inputs, the read-only / summed / over-limit cells fall out of the focus order and their explanatory `aria-label` (and any mouse-only `title`) becomes unreachable in the focus mode the grid role advertises. Make those cells focusable (`tabIndex={0}` + the standard `focus:ring` per rule 4) and keep the explanation in the `aria-label`, with `title` only as a redundant mouse affordance. Reference: the read-only multi-entry summed cell in `features/timesheet/TimesheetCell.tsx` (#1435, ADR-0224) — a `(task, date)` cell with ≥2 entries is a non-editable sum, made `tabIndex={0}` so its "N entries — edit on My Work" name is announced.

223. **A co-located "velocity proves the schedule" card renders the deterministic date and the velocity estimate as two separately-labeled reads that never merge into one authority, and computes any "since last close" delta on the CPM finish only.** When a CPM finish and a velocity forecast sit in the same card region (the `MilestoneBridgeForecast` bridge, #730, ADR-0241), the exact CPM date carries **no** estimate qualifier (it is deterministic), while the velocity read **always** carries its basis marker — `est. {p50}–{p80}` + an on-screen `(velocity estimate)` qualifier when `basis === 'velocity_band'`, and `P80 {date}` **only** when `basis === 'monte_carlo'` (extends rule 166 to the two-column co-located form). The "changed since last {iteration}" delta chip is derived from `cpm_finish` movement **only** — never from the velocity band, which is noisy sprint-to-sprint — is hidden when the prior snapshot is absent or unchanged, and states its direction in words (`+3d later` / `-2d earlier`, `→` `aria-hidden`) so polarity never rides on tone color alone (rules 120/169). Muted supporting text (qualifier, `since …` attribution, projection) uses `neutral-text-secondary`, never `neutral-text-disabled` (rule 169). Reference: `features/sprints/MilestoneBridgeForecast.tsx`.

## Route-level error surface (Issue #1654)

224. **A route-level error surface (`errorElement`) that offers recovery actions must move focus to its heading on mount — the erroring subtree unmounted, dropping focus to `document.body`, so keyboard/AT users otherwise cannot reach the recovery CTAs.** When a route subtree throws (or a lazy chunk fails to load) React Router swaps it for the route's `errorElement`; the element that had focus (the `NavLink` the user just clicked, a button) unmounts, so focus falls back to `<body>`. An error surface whose whole point is recovery (Reload / Go to home) must pull focus to itself in a mount `useEffect` — a `tabIndex={-1}` `<h2>` with a `focus:` ring (**not** `focus-visible:`, which a browser may withhold on a *scripted* `.focus()` — the rule-204(b) / rule-214 caveat) — or the keyboard/screen-reader user has to blind-Tab from `body` to reach the only actions on the page. This is the inverse of the calm `EmptyState` (rule 177), which is a zero-data view with no required action and therefore does **not** grab focus; an `errorElement` is `role="alert"` (assertive, an error the user landed in) and self-focuses, whereas `EmptyState` is `role="status"` (polite) and does not. Reference: `components/RouteErrorBoundary.tsx` (the `headingRef` mount-focus + `isChunkLoadError` chunk-vs-generic copy split), wired as `errorElement` at the route-tree root and on `ProjectShell`/`ProgramShell` in `router.tsx`.

## Form validation gating (Issue #1416)

225. **An `aria-invalid` field must gate its form's primary action — the submit/primary button's enabled state derives from that validity AND the submit handler re-guards it; never leave the primary action live while showing an error, and never let it commit a stale/previous value the user did not type.** The trap is an input whose parse only *updates* the committed value on success (`if (parsed !== null) setValue(parsed)`): on invalid text the field shows red + `role="alert"`, but the underlying value silently keeps its last valid figure, so a primary button whose `disabled`/`canX` predicate omits the invalid flag stays enabled — and Enter or a click logs the previous value the error is actively warning against. Compute the invalid flag before the enabled predicate, fold it into both the predicate and the submit-handler early-return, so an `aria-invalid={x}` field always has its `x` term reflected in what commits. Reference: the quick-log manual-duration input in `features/timeentry/QuickLogTime.tsx` (#1416, ADR-0185 §C) — `manualInvalid` gates both `canLog` and `handleSubmit`, so a half-typed "abc" disables Log rather than logging the prior 1h.

## Offline-queued write affordance (Issue #1159)

226. **An offline-queued write (a mutation the app defers to a durable queue while `!navigator.onLine`, replaying on reconnect) reuses the shared calm-offline "pending sync" vocabulary — it never invents a new offline signal, and it never disables or relabels the action button offline.** The vocabulary, established by the board card-status queue (ADR-0220) and extended to blocker flags (ADR-0247), is fixed so every offline surface reads as one product: (a) a passive **pending badge** — `role="status"`, `bg-semantic-at-risk-bg text-semantic-at-risk` (the same amber as the shell `SyncStatusBadge` / `OfflineBanner`), a 12px `aria-hidden` cloud-off glyph, and a full `aria-label`/`title` ("… queued — it will save when you reconnect."), with an optional `compact` variant that drops the visible "Pending" text but keeps the accessible name (rule 6 — color is never the only signal); (b) the mutating control **keeps its normal label and stays enabled** offline — flag/save/unblock *enqueue* instead of firing a live PATCH; disabling or relabeling it ("Flag when online") reads as *deferred/blocked*, the opposite of the reassurance the queue provides. The global `OfflineBanner` already carries the "you are offline" state, so the per-control surface stays quiet — one short helper line (`text-xs text-neutral-text-secondary`, e.g. "Offline — this will be saved and synced when you reconnect.") and the badge, never a toast per enqueue. (c) Announce the two discrete transitions on a visually-hidden `role="status" aria-live="polite"` region — *queued* on enqueue and *synced* on a **successful** flush only; drive the "synced" cue off a flush-success signal (never a `pending → absent` transition), because a conflict yield also clears the pending op and already shows its own conflict toast — announcing "synced" there would contradict it (rule 220 — announce what changed, not a running value). (d) An optimistic patch that fabricates a server-stamped field (a `blocked_since`/age not yet assigned) must render as a neutral "queued" label, never a fake precise value ("just now"/"0s"). Reference: `features/blocker/BlockerPendingBadge.tsx`, `features/blocker/offline/{blockerQueue,blockerOutboxStore,useBlockerOffline}.ts`, and `BlockerSection.tsx`; the flush is mounted once in `AppShell` (`useBlockerOffline`), not in the drawer, so a queued write syncs even when its originating surface is unmounted.

## One-time secret reveal (Issue #283)

227. **Any dialog that reveals a value the user cannot retrieve again (API token, board/share link, webhook secret) must disable both Escape and backdrop-click dismissal for as long as that value is on screen — the user leaves only via an explicit "Done" control.** A one-time secret uses the standard `useFocusTrap` + fixed-overlay dialog shape, but the reveal state is different from the form state: an accidental Escape (a near-universal reflex after finishing a dialog action) or an outside click discards the secret permanently, and nothing in the UI stores it afterward (the list rows show only a non-revealing prefix). Gate the dismiss paths on the reveal state: `useFocusTrap(true, revealed ? undefined : onClose)` for Escape, and add `&& !revealed` to the backdrop `onPointerDown` guard. The reveal warning itself carries real visual weight — a bordered `border-semantic-warning/70 bg-semantic-warning-bg` box, not bare warning-colored text — since it is the highest-consequence warning in the flow. References: `features/settings/token/PersonalAccessTokensPage.tsx` (the original), `features/settings/project/ProjectSharingPage.tsx` (#283 board share links).
## Touch targets and calendar day-type encoding (Issue #906)

228. **Touch-target size compaction keys off `md:` (≥768px), NEVER `sm:` (≥375px).** A control that is 44px on touch and collapses to a compact desktop size (32/36/38px) must express the shrink with a `md:` variant — `min-h-[44px] md:min-h-[32px]`, `h-11 w-11 md:h-8 md:w-8` — not `sm:`. The subtlety: `useBreakpoint`'s `sm` tier is "< 768px" (touch/mobile), but Tailwind's `sm:` utility fires at **375px**, which is still a phone. So `min-h-[44px] sm:min-h-[32px]` silently drops the target below the 44px WCAG 2.5.5 minimum on every real phone from 375px up — the compaction fires exactly where you needed the large target. Compact only at `md:`+, where a pointer is the primary input. Reference: the month-pager, row-remove, Retry, empty-state CTA, and bottom-sheet footer buttons in the Working-calendars panel (`features/settings/project/ProjectCalendarsPage.tsx`, `AddCalendarPicker.tsx`) — all `md:`-keyed.

229. **A calendar day cell never encodes its day-type by color alone (WCAG 1.4.1): each non-working type carries a pattern fill + a legible glyph/label + an `aria-label` naming the blocking calendar(s), and a multi-source day adds a split-corner mark.** The four-part encoding is mandatory on any `--cal-*` day-cell grid: (a) a **pattern fill** — weekend diagonal hatch, holiday dots, shutdown cross-hatch — built as inline `background-image` gradients over the translucent `--cal-*-bg`; (b) a **≥12px-legible glyph** (rule 50 floor) — holiday `H` / shutdown `S` — placed as the cell's own content (in a ~18px dense month grid, swap the day number for the glyph rather than shipping a sub-12px corner tag; the exact date stays in the `title`/`aria-label`); (c) an **`aria-label`** giving the full date plus the names of the calendar(s) that blocked the day, so the "why is this day off?" answer is never mouse-`title`-only; (d) a **split-corner** triangle (`aria-hidden`) when more than one calendar overlaps the day. The `--cal-*-fg` ink for the glyph/label is contrast-checked against the **composited** fill (the translucent `-bg` over the white card, e.g. holiday ≈ `#FBF0DB`), not the raw rgba — the composite reads lighter, so a token that clears 4.5:1 on paper can fail on the cell. Reference: `features/settings/project/ProjectCalendarsPage.tsx` (`DayCell`) + the `--cal-*` tokens in `styles/globals.css` (ADR-0251).

## Canvas drag-to-create gesture previews (Issue #1666)

230. **A canvas drag-to-create gesture (drag-to-link and any future create-by-drag on the schedule canvas) paints its preview only to the `canvas-interaction` layer using the `linkPreview` palette token, arms pointer-fine-only, and confirms success via the drawn artifact — never a success toast.** The drag-to-link preview (`drawLinkPreview` — dashed `[4,3]` line while hunting, flipping to solid + a 2px target ring on snap, `not-allowed` cursor on a self-drop) uses `linkPreview` (brand-primary sage-700/400) from the renderer `COLOR`/`COLOR_DARK` palettes — the sanctioned home for canvas hex (rule 40), never an ad-hoc rgba. Three invariants for any new create-by-drag gesture: (a) it arms only when `matchMedia('(pointer: fine)').matches` **and** `e.pointerType !== 'touch'`, so it never fires on touch — a keyboard/touch-complete alternative must remain (the `ScheduleDependencyPicker` drawer is the drag-to-link equivalent, and must not be regressed); (b) the pointer-down branch that arms the gesture **short-circuits before the bar move/resize path** (`_onPointerDown` returns on a `link-dot` hit before `_dragFSM.onPointerDown`), and Escape / `pointercancel` silent-cancel it; (c) success is confirmed by the artifact itself (the drawn dependency arrow, redrawn off the mutation's cache invalidation) with the accessible equivalent announced on the polite `sr-only` region — **no** success toast (a toast per link is noise during rapid linking); only the failure paths speak (cycle → `toast.error`, offline → `toast.info`). Reference: `features/schedule/engine/{GanttLinkFSM,GanttRenderer,GanttEngineImpl}.ts` + the `create-link` → `useAddDependency` wiring in `ScheduleView.tsx` (#1666).

## OSS / Enterprise Surfacing Rules (ADR-0266)

231. **The OSS UI shows what one team can do; an Enterprise affordance appears ONLY at a surface where the user reaches for something inherently cross-program or org-governing — never as an ambient padlock, disabled control, or upsell teaser in the OSS daily path.** TruePPM is adoption-first (the GitLab model): the free tier must be *completely sufficient for one PM/team/program*, and the Enterprise need is discovered **structurally** — when a second program, a portfolio roll-up, org-wide SSO enforcement, or a directory sync becomes the actual task — not through locked doors sprinkled across the shell. Three constraints for any **new** shell/navigation or extension-point-slot surface:
     - **(a) No render absent the edition.** Enterprise features register into stable OSS extension points (slot registration ADR-0029; edition-based routing ADR-0030). When the enterprise package is not installed, `registry.get(<slot>)` returns `[]` and the OSS UI renders **nothing** in that slot — no grayed-out or padlocked placeholder. A disabled control the OSS user can never enable is a wall, and walls in the daily path are the "you must upgrade to work" signal adoption-first exists to avoid.
     - **(b) Discovery at the seam, not on the surface.** The upgrade signal is allowed only where the *shape of the work* crosses the boundary — the moment a user tries to compare two programs, open a portfolio roll-up, enforce org-wide SSO, or sync a directory. That is contextual and honest (it coincides with a need the user just expressed) and is the ONLY sanctioned place a "this is an Enterprise capability" affordance may appear. It is never ambient chrome.
     - **(c) An Enterprise affordance is a distinct opt-in surface, not a disabled OSS control.** Where an enterprise slot *is* registered, present it as its own surface with its own label — never as `disabled aria-disabled="true"` OSS UI. (Contrast rule 42's WBS/Table `disabled` state, which gates an *OSS* feature coming to *this* edition — a legitimate "not built yet" disable; an Enterprise capability is "not in this edition," a different meaning that must not read as "temporarily off.")

     Load-bearing at the shell seams: the 3-tier rail (You / This project / **Jump**, #1642) and the location switcher (#1643) are exactly where cross-program/portfolio scope becomes visible. In OSS, `Program` is a first-class entity, so *within-program* navigation is fully OSS; only **cross-program** and **portfolio** destinations are the Enterprise seam. The classification test is the CLAUDE.md two-repo test verbatim: "Would a PM/program manager need this to run their program?" → OSS, render it; "Is this cross-program coordination, org policy, or compliance evidence?" → Enterprise, surface it only at the seam per (b). Basic OIDC/OAuth login is OSS (auth carve-out) — never gate it. If a practitioner hits a locked door doing single-program work, the boundary is drawn wrong and no visual treatment can rescue it — fix the boundary, not the padlock.

     **Surface classifier — daily path vs seam.** Rules 231 and 121 are the *same* principle applied to two surface types, not competing rules. **Daily-path surfaces** (the shell, rail, nav, project workspace, My Work) get 231: empty slots, zero ambient upsell. **Seam surfaces** — where a user's own action crosses into governance (Settings → Roles & permissions, org-identity/governance settings, the `/programs`→portfolio boundary) — may carry a single contextual affordance, because that *is* discovery at the seam (constraint (b) above), not ambient chrome. This maps onto the project's own adoption-vs-governance boundary: the daily path is where a team gets work done, the governance/settings seam is the natural home for an upgrade signal. Consequences:
     - **Rule 121's `EnterpriseBadge` on the Settings→Roles capability matrix stays** — the matrix's job is to show the *full* capability space, so hiding Enterprise rows would misrepresent it. The badge is constrained to **seam surfaces only**, never the daily-path shell/nav.
     - **Rule 178 is the one correction being made:** the disabled "Portfolio rollup" row in the rail (`Sidebar.tsx`) is a slot on the *daily path* wearing a padlock, so it converts to an empty `nav.portfolio_section` slot with discovery moved to the `/programs` seam. That scoped change is tracked in **#1677**; until it lands, leave the shipped `Sidebar.tsx` row in place (do not hand-delete it outside #1677).

## Client-rasterized export surfaces — SVG stroke + row-aware pagination (Issue #1694, ADR-0276)

232. **In any `html-to-image`-rasterized export surface, set SVG `stroke`/`fill` via inline `style` (a `rgb(var(--token))` CSS var), NEVER a Tailwind `stroke-`/`fill-` class — html-to-image silently drops CSS-class `stroke` on `<path>` (it keeps class `fill` on `<polygon>`), so a class-based connector rasterizes as 0 ink while its arrowhead survives.** This was the schedule PDF's "arrowheads but no dependency lines" bug (#1694): confirmed empirically — a class-stroke path rasterizes to 0 ink, an inline-style stroke path rasterizes fully. The inline-`style` CSS-var form (`style={{ stroke: 'rgb(var(--neutral-text-secondary))' }}`) is the reliable path AND stays gate-safe (no hex literal; single-sourced through the DS custom property, so the `design-system-v2` hex gate passes) — do not "fix" it back to a `stroke-*`/`fill-*` utility class. This is `style`-for-a-reason (rule 10 carve-out: a rasterizer-compatibility requirement, not styling convenience); comment the why at the call site. Print-surface FS dependency arrows are **charcoal always** (`--neutral-text-secondary`), hard vs soft = **solid vs dashed** (never color — ADR-0063 rule 75: critical is the red bar; a red arrow over a red bar merges). Reference: `schedulePrintTheme.ts::arrowColorVar`, the `<path>`/`<polygon>` `style` in `SchedulePrintLayout.tsx`.

233. **The schedule PDF report paginates ROW-AWARE with repeated headers — it never slices the rasterized bitmap at a fixed pixel height.** The single-column (chart-fits-one-page-wide) Layout-A report breaks only on safe boundaries (between whole Gantt rows, before the CP card, between CP-list grid rows, before the keep-together footer) and repeats the header a continuation page needs: a Gantt continuation re-composites the frozen **Activity + date-scale** header band from the source bitmap (pixel-accurate, so bars stay aligned to dates); a CP-chain continuation stamps a **"Critical Path Chain (Continued)"** running header as real `pdf.text`; the bottom-right caption is **"Page n of N"**. The layout tags its blocks with `data-print-vmark` markers (`gantt` / `gantt-rows` / `cp` / `cp-list` / `footer`) + stamps `data-print-gantt-row-count` / `data-print-cp-row-count`; the rasterizer **measures** the marker rects (`getBoundingClientRect` on the off-screen-but-laid-out surface) — never stamps text-driven block heights from React — and feeds the pure, unit-tested `scheduleVerticalPlan.ts` planner. The wide multi-column (#1440) horizontal-banding path is unchanged and keeps its "Sheet n of N" wording. Masthead/KPI/legend appear on page 1 only. Reference: `scheduleVerticalPlan.ts`, `exportSchedulePdf.ts::{readVFlowGeometry,paginateVerticalReport}`, the `data-print-vmark` markers in `SchedulePrintLayout.tsx`.

## Schedule PDF report — risk on the border, shape-encoded states, expanded legend (Issue #1686, ADR-0277)

234. **On the schedule PDF report, HUE encodes the worst risk band and TEXTURE/SHAPE encodes the redundant grayscale-safe signal — and the risk band lives on the bar's BORDER, never its fill (which is progress).** The export is a static, `html-to-image`-rasterized light document (no hover, no interactivity), so every state must be self-evident on paper and survive grayscale + deutan/protan (WCAG 1.4.1). The firewall (ADR-0277, supersedes the fill-by-risk model):
     - **Risk = border, progress = fill.** `barBorderClass(band)` frames the bar (critical = 2px `semantic-critical`, at-risk = 2px `semantic-at-risk`, on-track = 1px `neutral-text-secondary` hairline — the fainter `neutral-border` fails WCAG 1.4.11 for a bar that is still data); the interior keeps the green `semantic-on-track` progress fill. This is because the old model painted the green progress fill *over* the risk fill, so a 100%-complete critical task rendered solid green and the red critical base was fully masked (the "bars all green despite 18 critical tasks" bug). A completed critical task must read as a green bar in a **red frame**. **Never** put the risk band back on the fill. The 2px critical border clears WCAG 1.4.11 vs the white sheet; critical is additionally backed non-color by the row-label red dot + the Critical-Path-Chain list (do not remove either — they are the 1.4.1 redundancy the thin red-vs-green interior boundary, ~1.6:1, relies on).
     - **"Behind schedule" = a diagonal hatch overlay**, not a color. `hatchBackgroundStyle()` is a `repeating-linear-gradient` `background-image` (a `<div>` background, so it rasterizes reliably — rule 232 only bites SVG class-strokes) composed over *any* border, so a critical-and-behind bar reads red-frame + hatch. Keep `isBehind` as its own `SchedulePrintRow` flag: a critical bar's `riskBand` is `critical` (precedence), which would otherwise hide that it is also slipping.
     - **Overdue = shape, never a fourth hue** (red is exhausted by critical). A task past its finish and not complete (`isRowOverdue`) gets a red past-due flag polygon at its finish edge + a dashed red overrun tail to the data date (inline-`style` stroke/fill CSS-vars, rule 232). A pending milestone past its date is a **hollow red-outlined** diamond + a `!` glyph. Met milestones are never overdue.
     - **Milestone met vs pending = filled vs hollow diamond** (`milestoneDiamondClasses(met, overdue)`), a shape cue — resolves the #1686/#1683 color-only-amber gap. Never distinguish them by hue alone. **Both diamonds are outlined in navy `neutral-text-primary`, never amber:** the static `brand-accent` (#E8A020) is only ~2.2:1 on the white sheet — below the WCAG 1.4.11 3:1 floor for a content-bearing mark — so it is the **fill only**, never the sole boundary of a mark. More generally, any content-bearing mark on the export sheet must clear ≥3:1 against white; pair a sub-3:1 hue (brand-accent amber) with a dark outline or use a darker token.
     - **Row leaders are faint round-dotted hairlines** (label→bar), made distinct from dashed *soft* dependency arrows on three axes: dot pattern (`0.5 4` round vs `3 2` square), weight/color (`neutral-border` 0.75px vs `neutral-text-secondary` 1px), and zone (left gutter, horizontal, per row vs bar→bar connectors). A leader that would be a stub (`< 12px`) is omitted.
     - **The legend is grouped Bars / Links / Markers**, each swatch rendering its ACTUAL treatment (border/hatch/flag/filled-vs-hollow/dash) so the legend itself is grayscale-legible. Page 1 only, in the keep-together footer flow.
     - **Pagination reserves a bottom footer band** (`RESERVED_FOOTER_PT`, `exportSchedulePdf.ts`) so the "Page n of N" counter never collides with content; a centered **"continued on next page"** caption (plain ASCII — no `→` glyph, which jsPDF's standard font drops) + a hairline rule seal every non-final page. `scheduleVerticalPlan.ts` keeps the CP card whole when it fits a page and avoids stranding `< MIN_GANTT_ORPHAN_ROWS` (3) Gantt rows on a continuation page — a widow/orphan guard, **not** a rescale (the fixed print scale is deliberate; bars stay the same size on every page). Reference: `schedulePrintTheme.ts::{barBorderClass,hatchBackgroundStyle,milestoneDiamondClasses}`, `schedulePrintData.ts::{isRowOverdue,isBehind}`, `SchedulePrintLayout.tsx`, `scheduleVerticalPlan.ts`.

## Interactive canvas Gantt — critical path is a border frame, not a fill (Issue #1699, ADR-0280)

235. **On the interactive canvas Gantt, the task bar FILL carries task STATE and the critical path is a red BORDER frame — never colour the fill by criticality.** Same principle as the PDF export (rule 234 / ADR-0277), applied to `GanttRenderer.ts`. `barFillColor()` resolves fill by state only (external → `textSecondary`, summary → `barSummary`, complete → `barComplete` sage-600 green, else → `barNormal` blue-500); it must **not** return `barCritical`. `drawTaskBar()` draws a **2px red critical frame** (`barCritical` = #B91C1C light / #F87171 dark) as an inset border for non-external, non-summary critical tasks. The reason: `barFillColor` checks `isComplete` *before* `isCritical`, so a red critical fill lost to a completion-wins-precedence race — a done critical bar rendered solid green and the critical path vanished on a mostly-complete schedule. A completed critical task must read as a **green bar in a red frame**; the critical signal can never be masked by state. Constraints:
     - **Draw order:** the 30% progress overlay paints BEFORE the borders so they stay crisp rather than dimmed by the tint; the selection ring paints next; the **critical frame paints LAST**.
     - **Selection nests inside the critical frame; risk outranks selection.** The navy `selectionRing` (#1B2A4A) is a 2px inset ring; when a critical frame is present and `barWidth >= MIN_NEST_WIDTH` (12px) it nests one step further in (inset 3px, 1.5px wide) so a selected critical bar reads red-frame-outside / navy-ring-inside. Below 12px the two concentric rings collide, so the ring uses the standard inset and — because the frame is drawn last — the red frame paints over it: a narrow selected critical bar keeps its criticality rather than losing it under the navy ring (the dropped-signal bug the #1699 UX review caught). **Gantt bar visual channels are orthogonal — fill = state, border = risk, ring = selection, line = time (ADR-0103/0277/0280); never encode two on the same channel** (the pre-#1699 bug was critical stealing the fill channel from complete). When channels stack on one bar, each must stay independently perceivable: draw inner channels at a smaller inset than outer ones, and below a minimum width abandon nesting in favor of a treatment that preserves the higher-priority signal (**risk > selection**). A channel another can fully occlude is a dropped signal.
     - **The `%` chip uses the surface treatment only** (`chipPillOnSurface` dark pill + `chipTextOnSurface` white). The old white-on-red chip existed for the red critical fill; with no bar red-filled, every chip pairs with the blue/green state fill via the surface treatment. Do not reintroduce a criticality branch in `drawTaskBarChip`.
     - **Border clears WCAG 1.4.11** (≥3:1 non-text) in both modes as a 2px frame (#B91C1C ≈ 7:1 on white, #F87171 ≈ 4.87:1 on navy); criticality is additionally backed non-color by the task-list red dot + the critical-path-only filter (1.4.1) — do not remove them.
     - **`ScheduleLegend.tsx::CriticalSwatch`** must mirror the canvas: a red-framed neutral bar, not a solid red fill.
     - **`barCritical` still legitimately strokes late actuals** in `drawActualDateBar` (#80) — that path is unrelated and must stay.
     - **Scope:** the canvas `Task` exposes only `isCritical` (binary), so this is a critical-only red frame; graded at-risk/on-track amber bands (present on the PDF) wait until the risk-band data is plumbed to canvas tasks. Reference: `GanttRenderer.ts::{barFillColor,drawTaskBar,drawTaskBarChip}`, `ScheduleLegend.tsx::CriticalSwatch`.

## Responsive table→card reflow for the task Grid (Issue #1701)

236. **A fixed-width "table" row (flex row of `w-*` cells) that must stay legible on a phone reflows to a two-line card BELOW `md` using paired `md:contents` wrappers — and when the list is virtualized, the virtualizer's mobile row height MUST equal the card's fixed mobile height, or the second line is clipped.** The Grid's rows (`features/grid/{TaskRow,OutlineRow}.tsx`) are a flex row of fixed-width cells (~744px total) that collided and clipped on a phone. The reflow pattern, without duplicating any datum or plumbing an `isMobile` prop into every cell:
     - **Two `md:contents` group wrappers** (`role="presentation"`, so row→gridcell ARIA ownership survives the intermediary): line 1 = WBS/name/owner, line 2 = dates/duration/progress/status. The row is `flex flex-col md:flex-row`; below `md` the wrappers are real flex lines that stack, and at `md`+ they collapse to `display:contents` so the cells lay out as the **byte-identical original desktop table**. Give each cell its width as `md:w-14` (natural width on mobile), not `w-14`.
     - **Virtualized modes (Flat/Grouped via `VirtualRows`) need a breakpoint-aware fixed height.** `VirtualRows` positions each row at a fixed `estimateSize` — it does not measure — so a taller two-line card gets clipped by its slot unless `estimateSize` returns the mobile height. Drive it off `useBreakpoint()==='sm'` (which is "< 768px", correctly covering every phone incl. 320px) and set the row's mobile class to the matching fixed height (`h-14` = 56px = the estimate); call `rowVirtualizer.measure?.()` on breakpoint change. Non-virtualized modes (Outline) use `min-h` and grow freely.
     - **Line 2's shrink-0 cells must sum under the 320px budget (~296px after `px-3`).** Everything on line 2 is `flex-shrink-0` except the progress bar (`flex-1 min-w-[1.5rem]`), so surplus does not wrap — it clips/pushes. The **status pill is what tips a row over**: hide the numeric `%` on mobile with `sr-only md:not-sr-only` (kept for screen readers, since the bar is `aria-hidden`) to reclaim ~34px. Verify with a Playwright spec at a 390px viewport asserting `scrollWidth - clientWidth <= 1` on the scroll container.
     - **Hide the desktop column-header row on mobile** (`hidden md:flex`) — the card is self-describing; and demote low-value columns (Outline predecessors, the phase subtitle) to `hidden md:block`/`md:inline`. Reference: `features/grid/{TaskRow,OutlineRow,VirtualRows,FlatMode,GroupedMode,OutlineMode}.tsx` (#1701).

237. **A collapsed board-column stub must surface a WIP breach with the same always-on visibility as the expanded header's `WipBreachChip` — breach visibility is independent of the "Show WIP limits" toggle (rule 176 extends to stubs), and an empty folded column must read as empty, never as an ambiguous blank (#1695/#1697).** A stub is a *lens* on the column, not a place to hide a signal the expanded header shows: collapsing to reclaim width must not suppress scope invisibility. In `ColumnStub` (`features/board/BoardView.tsx`) the two signals are split: (a) the **count badge's limit portion** (`N/limit`) obeys `showWip` — plain `N` when the toggle is off and the column is *not* breaching; (b) the **breach state** (`at` → amber `--semantic-at-risk`, `over` → red `--semantic-critical`) renders **always**, and a breaching stub shows the `N/limit` ratio (tabular-nums) in the breach color regardless of the toggle. The call site therefore passes `wipBand={wipState(count, col.wipLimit)}` computed **unconditionally** (not gated on `showWip`), matching the header's always-on breach chip (ADR-0130 D2). "Folded ≠ empty" (#1697): a populated stub shows a filled `--brand-primary` count pill; a 0-card column shows a **dashed hollow "0"** (`border-dashed`, no fill) with the accessible name `"…, empty"`. The stub `aria-label` encodes the breach (`"…, over WIP limit of 5"`) so the signal survives for screen readers too. Reference: `ColumnStub` in `BoardView.tsx`, `wip.ts::wipState`.

238. **Never let a collapse/stub affordance hide the current user's assigned cards without a quiet, always-present signal: an edge accent on the affected stub plus an expand affordance in the collapsed-count banner (#1696).** Once a column folds to a stub a contributor can lose track of their own cards inside it; the signal must be *quiet* (no toast, no push — churns on noise) but *findable*. Two tiers, both reusing existing state (`myResourceId` from `useCurrentUserResourceId`, the same `task.assignees.some(a => a.resourceId === myResourceId)` predicate as the "My tasks" filter): (a) a **2px `--brand-primary` left-edge accent** (`border-l-2 border-l-brand-primary`, plus `data-has-my-cards`) on any stub holding ≥1 of the current user's cards, with `aria-label` gaining `", contains N of your cards"`; (b) a **secondary clause on the "N columns collapsed" banner** — `· N of your cards hidden`, a `--text-secondary` text button (accessible name `"Expand columns containing your cards"`, 44px mobile touch target) that calls `expandColumn` on the affected stubs. The signal is absent (never wrong) when `myResourceId` is null — a user with no resource identity on the project sees no accent and no banner clause. Reference: `ColumnStub` + the collapsed-columns banner in `BoardView.tsx` (`myCountByStatus` memo).

239. **The schedule/board PDF export carries a SELECTABLE invisible-text layer over the raster — every content string a reader needs (row labels, KPI values, the critical-path chain, board cards/columns) must be marked `data-print-text="<role>"` on the print layout, or it will not be selectable/searchable/screen-reader-perceivable (ADR-0289, #1687).** The export is still a `html-to-image` raster (the visual layer), but `pdfTextLayer.ts` now stamps a real jsPDF text run in **rendering mode 3 ("invisible")** over each marked element's measured box, re-projected through the *same* per-page scale/offset transform each pagination branch already computes (single page, vertical header-repeat, week-snapped label-strip + chart band, plain banding, board banding). The rules: (a) new content in `SchedulePrintLayout.tsx` / `BoardPrintLayout.tsx` that a person would read on paper **must** get a `data-print-text` marker (roles: `masthead`/`kpi`/`row`/`cp`/`footer` for schedule, `masthead`/`column`/`lane`/`card`/`footer` for board) — decorative swatches, `title`-tooltip-only bars, and duplicate strings stay unmarked; (b) markers are emitted in **DOM document order = logical reading order** (masthead → KPIs → rows → CP chain → footer), because jsPDF cannot emit a PDF/UA StructTree — content-stream order *is* the reading-order guarantee; (c) the collector uses **`getBoundingClientRect`** (measured, not layout constants) so it serves both the schedule's fixed-pitch rows and the board's variable-height cards, and skips zero-area rects so jsdom/unmeasured surfaces just get no layer (raster still exports); (d) `stampTextLayerForPage`/`setPrintDocumentMetadata` are `typeof`-guarded so the hand-rolled jsPDF test double is unaffected unless it opts in by stubbing `text`/`setLanguage`/`setProperties`. Full PDF/UA structure-tree tagging is **deferred** (needs a server-side renderer, which reverses ADR-0159). Reference: `packages/web/src/features/export/pdfTextLayer.ts`, `exportSchedulePdf.ts`, `exportBoardPdf.ts`, the `data-print-text` markers in both print layouts.

## Status-affordance glyphs: a "disabled/off/muted" glyph means an actual disabled state, never an empty one (Issue #1707)

240. **A control whose icon can imply a disabled/off/muted state (a slashed bell, a crossed-out eye, a muted speaker, a strikethrough) must reserve that glyph for a REAL such state driven by a server/preference fact — never render it as the resting appearance of an empty count.** The `NotificationBell` (`features/shell/NotificationBell.tsx`) rendered the muted-bell glyph `🔕` whenever `count === 0` and the active bell `🔔` only when `count > 0`; the slashed bell is the universal "notifications turned off" symbol, so the default resting state (what every user sees most of the time) falsely read as *notifications disabled* even though no mute state exists. The fix: one plain active `BellIcon` (`components/Icons.tsx`, the SVG house style — never an emoji, which renders inconsistently across platforms) in **all** states; unread is conveyed by the count **badge** (`aria-hidden`, count carried in the button `aria-label` per rule 171) plus the `text-brand-primary` accent (color is never the sole signal — rule 6/1.4.1 — the badge digits and `aria-label` carry it). The icon shape never swaps between empty and non-empty. A genuine account-wide muted state now ships as **Do Not Disturb** (#1707, ADR-0292): the bell adds a **distinct crescent `MoonIcon` corner chip** — never a slashed bell — gated on the real `user.dnd_enabled` fact, and the DND state is carried in the button `aria-label` (rule 6); the count badge is unchanged. The preferred remedy for a real muted/DND state is a **semantically-different** glyph (a moon/quiet-mode mark), not a bell-off, because DND that keeps the in-app inbox live is not "notifications off". General principle: an "empty" data state and a "turned off" control state are different meanings and must not share a glyph. Reference: `NotificationBell.tsx`, `MoonIcon`/`BellIcon` in `Icons.tsx`.

## Gated affordance for an unbuilt flow — a passive label, not a disabled button (Issue #1420, ADR-0291)

241. **When the flow behind an affordance does not exist yet (its connect/manage screen is a separate unbuilt issue), the seam is a passive, non-interactive `<span>` label — "Coming soon" — never a disabled `<button>`.** This is the complement to rule 122's disabled-control recipe: rule 122 applies when a control *exists but is momentarily unavailable* (keep it a focusable `<button disabled>` with an explanation). When there is *nothing to click yet at all*, a disabled button is a **phantom affordance** — it advertises an action that has no destination, sits in the tab order promising a dialog that never opens, and is exactly the dead-click friction a contributor churns on (VoC 🟡). Use a plain label span: no `role`, no `tabindex`, no handler, out of the tab order. Pair it with a passive **connected/active state** that mirrors the page's existing "connected" pill (`ConnectionPill`) rather than inventing a new one, so the surface is forward-compatible: when the real flow lands (#1421), the label span becomes the live button and the passive state gains its manage action, with no restructure. The affordance must stay a **text label, not color-only** (Active/Coming soon words carry the state; any status dot is `aria-hidden`), and a trust/reassurance badge row uses `text-neutral-text-primary` (not `-secondary`) on `surface-sunken` to clear WCAG AA — `-secondary` there is 4.36:1, below the 4.5:1 floor. Reference: `SourceStatus` + `AvailableSourcesSection` in `features/me/ConnectedAccountsPage.tsx`.

<!-- rules 242 (#1739 emoji→SVG) and 243 (#1741 toolbar clustering) are claimed on their branches. -->

## Canvas content must handle forced-colors itself — the UA transform does not reach it (Issue #1742)

244. **A `<canvas>` is not touched by the forced-colors (Windows High Contrast) user-agent transform, so canvas-drawn UI must detect `(forced-colors: active)` and repaint with CSS *system-color* keywords — otherwise it renders its fixed colors into a high-contrast theme and becomes invisible/unreadable.** DOM chrome gets forced-colors for free (the UA remaps `color`/`background`/`border` to the theme); pixels a component draws into a canvas do not. The Gantt engine (`GanttEngineImpl`) subscribes to `window.matchMedia('(forced-colors: active)')` (mirroring the `prefers-reduced-motion` pattern — detect at init, re-detect on `change`, full-repaint on flip, unsubscribe in `destroy`) and passes the flag to `setRendererColorMode(dark, forced)`, which selects a third palette `COLOR_FORCED` built entirely from system-color keywords the 2D context resolves against the active theme: `Canvas`/`CanvasText` (surface/ink), `GrayText` (de-emphasis), `Highlight` (accent + selection), `LinkText` (links). Because the system palette is intentionally flat, **bar *kinds* must not depend on hue** in this mode — lean on shape (milestone diamond, summary endcaps) and the `Highlight` accent for critical/selection (color is never the sole differentiator, rule 6). Semi-transparent tints (row banding, weekend shading, ghost fills) collapse to the solid surface/ink so nothing relies on alpha the theme may not honor. Keep the palette-selection function (`pickPalette(dark, forced)`) pure and exported so it is unit-testable without a canvas, and assert that a draw pass in forced mode emits system-color `fillStyle`s (not brand hex). Forced-colors wins over dark/light — it is a hard accessibility preference, not a cosmetic mode. Reference: `COLOR_FORCED` / `pickPalette` / `setRendererColorMode` in `engine/GanttRenderer.ts`, the `_forcedColors` wiring in `engine/GanttEngineImpl.ts`.

## A functional glyph is a house SVG icon, never an emoji (Issue #1739)

242. **A glyph that stands in for a *UI icon* — a status/affordance mark like a warning, lock, target, chart, pin, or disabled sign — must be a house SVG from `components/Icons.tsx`, never a Unicode emoji.** Emoji render inconsistently across platforms/fonts (flat vs. glossy, differing metrics, some monochrome some multicolor), do not inherit `currentColor`, and never match the design-system stroke weight — so a functional emoji reads as a foreign, off-brand mark and can't be tinted to the surrounding text/semantic color. #1739 converted the affordance families to house icons: `⚠`→`WarningIcon`, `🔒`→`LockIcon`, `🎯`→`TargetIcon`, `📊`→`BarChartIcon`, `📌`→`PinIcon`, `🚫`→`BanIcon`, and the file-type mime map (AttachmentSection, `previewType`) to the `FileIcon`/`FileTextIcon`/`FileSpreadsheetIcon`/`FileImageIcon`/`FilePdfIcon`/`FolderIcon`/`PaperclipIcon` set. The recipe for an inline `<span aria-hidden="true">GLYPH</span>`: replace with `<Icon className="inline-block h-3 w-3 align-[-0.125em]" aria-hidden="true" />` (the `inline-block h-3 w-3 align-[-0.125em]` baseline-aligns the SVG like the emoji it replaces; a trailing space inside the old span becomes `mr-1`; preserve any existing span classes by merging them before the standard ones). Keep the glyph `aria-hidden` — the adjacent text or an `aria-label` on the wrapper carries the accessible name (rule 6). **Three carve-outs stay as emoji** (they are *content*, not icons): user-chosen **reactions** (`CommentSection` 👍), an **emoji-scale input** (`TeamHealthPulse` mood 😞😐🙂😀🤩), and third-party **brand logos** (GitHub/GitLab/Figma/Notion/Slack host marks — a separate brand-asset/licensing decision, tracked in #1748). The remaining long tail of functional glyphs (✓/✕/🗑/🔗/⏱/⚑/… and string-stored `icon:`/`glyph:` data-model fields) is a phased follow-up (#1749) — convert a family at a time, updating its e2e/vitest assertions in the same commit. When a test asserts on the old glyph text (`getByText('⚠')`), re-anchor it to the rendered SVG (`getByLabelText(/…/).querySelector('svg')` or `.closest('.inline-flex')?.querySelector('svg')`) or a `data-testid` on the container. Reference: `components/Icons.tsx`, `previewType.ts`, `features/schedule/sections/AttachmentSection.tsx`.

## Toolbar clustering — the Display popover is the filters' home at every width (Issue #1741)

243. **When a toolbar exceeds ~6 top-level affordances, group its controls into named clusters (Time / Show / Actions) and fold the display/filter controls behind a single "Display" popover — which is their home at EVERY width, never migrating into the `···` overflow.** The Schedule (Gantt) toolbar (`features/schedule/ScheduleView.tsx`, `role="toolbar"`) was a flat ~12-control row. #1741 clustered it to ≤6 interactive affordances at lg — two primary creates (`+ Task`, `+ Milestone`), the standalone `Grid/Timeline` toggle, the **Show** cluster (a `ScheduleDisplayMenu` "Display ▾" popover holding the four view/render filters + column-visibility as `menuitemcheckbox` rows), the **Time** cluster (`role="group"` wrapping Today · Zoom · Quarter), and the **Actions** `···` (`ToolbarOverflowMenu`: Import/Export/Share) — kept single-row `flex-nowrap` (rule 113) so a missing collapse rule clips rather than wraps. The key departure from rules 111/112 (which dump *secondary* controls into the `···` overflow at sm): **filters have a permanent home in the Display popover and never move to `···`** — putting them in both places is redundant, and the popover is visible at every tier (only its trigger label collapses to icon-only below lg, rule 114). The Display trigger carries an **active-filter count badge** (counts only the *data* filters, not column visibility — hiding a table column is a layout pref, not a data filter) whose number is echoed in the trigger `aria-label` (`"Display, 2 active filters"`; the pill is `aria-hidden`, rule 6). The popover reuses the `ToolbarOverflowMenu` keyboard contract (rule 112: ArrowUp/Down rove focusable rows skipping section headings, Home/End, Enter/Space toggle in place with the menu staying open, Escape restores focus to the trigger, Tab/outside-pointerdown close) — non-modal, so **no focus trap** (a trap is only for modal dialogs). A cluster is a `role="group"` with an `aria-label`; hairline `w-px` dividers between clusters are `aria-hidden`. Export PDF stays hidden at sm (a deck-style export is a desk task, rule 110 carve-out). When controls move into a popover, **every e2e/vitest assertion that located them as inline toolbar buttons must be re-anchored** to open the popover first and query `menuitemcheckbox`/`menuitem` by accessible name. Reference: `ScheduleDisplayMenu.tsx`, the toolbar block in `ScheduleView.tsx`, `SlidersIcon` in `components/Icons.tsx`.

## Modal focus traps: multi-state dialogs re-seat via focusKey; nested confirm dialogs own their own trap (Issue #1776)

245. **A dialog that stays open while its content swaps state passes that state as `useFocusTrap`'s `focusKey`; a confirm dialog nested inside a modal whose parent trap yields must run its own `useFocusTrap` — never ship a modal surface where Tab can reach the background page (WCAG 2.4.3 / 2.1.2).** Two failure shapes, one hook. (a) **Multi-state dialogs**: `useFocusTrap(active, onEscape)` seats initial focus only when `active` toggles, so a wizard/phase dialog (ImportModal's picking → uploading → success/error, PromoteMilestoneDialog's conflict ↔ form) drops focus to `<body>` when the phase unmounts the focused control — and Tab then escapes the "modal." Pass the phase/mode as the third `focusKey` param; the trap re-seats on change (first focusable, else the container — give the container `tabIndex={-1}` so a control-less phase like a progress screen still has a seat), skips the re-seat when focus is still inside (so swaps that keep the focused control mounted don't yank it), re-seats off the container itself once controls reappear, and still restores the *activation-time* trigger on close. A consumer wanting a nicer target than "first focusable" keeps a small `useEffect` focusing its preferred ref *after* the hook (call order = effect order), like PromoteMilestoneDialog's first-form-field seat. Do **not** hand-roll trap/Escape/restore logic in a component — convert it to the hook. (b) **Nested confirm dialogs**: a parent modal that disables its trap while a child `alertdialog` is open (TaskFormModal's `!showDeleteConfirm && !showDiscardConfirm` gate) is only safe if **every** such child runs its own `useFocusTrap` — an untrapped child (the old DeleteConfirmDialog: autofocus + Escape only) leaves Tab free to tunnel into the background form. Order the child's buttons so the safe action is the first focusable (the trap's default seat) — destructive actions never autofocus the destructive button. Reference: `hooks/useFocusTrap.ts` (`focusKey`), `components/import/ImportModal.tsx`, `features/sprints/PromoteMilestoneDialog.tsx`, `features/board/TaskFormModal/DeleteConfirmDialog.tsx` vs `features/settings/components/ConfirmDiscardDialog.tsx`.
246. **A TanStack Query fetch failure on a primary surface renders the shared `QueryErrorState` (`components/QueryErrorState.tsx`), never an empty or perpetual-skeleton state — a dead request must be visually distinct from "nothing here yet".** A page that destructures only `data`/`isLoading` and ignores `error` renders a failed fetch as empty (Board) or a skeleton that never resolves (Overview) — indistinguishable from a genuinely empty or slow surface, which corrupts beta feedback and support triage (#1764). Wire `error`/`isError` into the query hooks and render `QueryErrorState` in place of the empty/skeleton branch. Two variants: **`fill`** (`role="alert"`, `bg-neutral-surface`, `h-full`, centered) for a whole-page/pane failure the user navigated to (Board, Grid, Schedule) — assertive because the surface is dead; **`inline`** (`role="status"`, `rounded-card border-neutral-border bg-neutral-surface-raised`, `min-h-24` to match the skeleton footprint it replaces) for a single widget on a still-working page (each Overview widget) — polite, so a total outage does not fire N assertive announcements that interrupt each other. Retry defaults to `window.location.reload()`; pass a query `refetch` (`onRetry={() => void refetch()}`) when the hook exposes one so retry re-runs just the failed request. Copy is one terse `Couldn't load <X>.` per surface. This completes the surface-state trio with `EmptyState` (rule 177, `role="status"`, calm zero-data) and the route `errorElement` (rule 224, `role="alert"`, self-focuses on a render-throw): widget fetch error → polite in-place; empty data → calm; route throw → assertive-and-self-focusing. Reference: `components/QueryErrorState.tsx`, `features/board/BoardView.tsx`, `features/project/ProjectOverviewPage.tsx`.

247. **Promoting a hover-only or desktop-only control to touch is a touch-target-and-focus change, not just a visibility flip — audit size, hit area, and focus restoration when you make a control visible/active below `md` (#1770).** Removing `hidden md:*` or adding `max-md:opacity-100` surfaces a control that was previously mouse-only; on a phone it now must clear three bars it didn't before: (a) **Size** — the tap target is ≥44px on mobile. A flat desktop `h-8` (32px) is wrong once the control is a primary mobile affordance; chrome controls follow the responsive-shrink pattern their TopBar siblings use (`TimerChip`/`NotificationBell`: `h-11 w-11 md:h-7`), i.e. `h-11 md:h-8`. (b) **Hit area** — a row/card affordance that *can't* resize (a `w-4` drag handle, a `w-6` `···` glyph) gets a 44px touch zone via a mobile-scoped transparent pseudo-element: `relative max-md:before:absolute max-md:before:content-[''] max-md:before:-inset-[14px]` (16px + 2×14px = 44px) — scope it to `max-md:` so the invisible zone never captures clicks on desktop where the control is hover-hidden. This mirrors `BoardCard`'s always-on `before:inset-[-10px]`. (c) **Focus restoration** — a control that moves from an anchored popover (`useFocusTrap`, which restores focus to its trigger on close) to a shared `BottomSheet` must restore focus itself: `BottomSheet` deliberately leaves close-side restoration to the caller, so the mobile branch keeps a `triggerRef` and `.focus()`es it when the sheet transitions open→closed (an `isMobile`-gated effect), or focus drops to `<body>` after submit/cancel/scrim/Escape — an asymmetry with the desktop path the same component guarantees. Gate the breakpoint with `useBreakpoint() === 'sm'` (`< md`), render exactly one shell (`open && !isMobile` popover vs `isMobile` sheet) so there is no double focus-trap and no duplicate-`id` collision, and scope any shared roving-focus `querySelector` to the form element (a `formRef`), not the shell, so it resolves under either surface. Reference: `features/timeentry/QuickLogTime.tsx`, `features/board/BoardCard.tsx` (`before:inset-[-10px]`), `features/grid/OutlineRow.tsx`, `components/ui/BottomSheet.tsx`, `hooks/useBreakpoint.ts`.

248. **A data surface's loading state is a skeleton ghost that mirrors the shape of the real content, never a bare `Loading…` text line.** Every primary surface (Schedule, Grid, My Work, Overview, Reports, Today, and — since #1774 — Board, Sprints, and the project Backlog) renders pulsing placeholder blocks while its query is in flight, so the layout doesn't jump when data lands. The idiom: one `role="status"` wrapper with an `aria-label="Loading X…"` accessible name (the E2E wait-for-paint gate resolves on this node detaching — gate specs on `getByRole('status', { name: /Loading X/i })`, not on a visible text string), and every ghost child is a `motion-safe:animate-pulse rounded-* bg-neutral-surface-sunken` block marked `aria-hidden="true"`. Mirror the surface's real shape — column ghosts for the Board (a row of lane placeholders), row ghosts for list/table surfaces (Backlog, Grid), card ghosts for card lists (Sprints). `motion-safe:` is mandatory so the pulse honors `prefers-reduced-motion`. Never gate an E2E on the *absence* of a visible "Loading…" string (it reads count-0 before the surface even mounts, racing the assertion); gate on the skeleton's `role="status"` node detaching, which is the true "loading finished" signal. Reference: `features/board/BoardView.tsx`, `features/sprints/SprintsView.tsx`, `features/project/backlog/ProductBacklogPage.tsx`, `features/grid/GridView.tsx`.

249. **Below `md` the Schedule renders a dedicated DOM list-timeline (`features/schedule/mobile/MobileSchedule.tsx`), not the desktop canvas Gantt — a mutually-exclusive `isMobile` branch, the same reflow discipline as `MobileBoard` (rule 193).** The canvas engine (rules 59–85) is mouse-and-pixel native; on a phone it opens on whitespace with unreadable bars (#1671, ADR-0348). The mobile surface is a vertically-scrolling, WBS-ordered list: each row is a two-line touch target (≥44px) — line 1 identity (critical **rail**, shortId, name, Blocked badge, dates) + a 44px one-tap complete; line 2 a **shared-project-window** mini-timeline strip (all rows share one scale so they read as a cascade; per-row scales are banned — they make every bar identical). Placement math lives in the pure, unit-tested `mobileScheduleGeometry.ts` (`leftPct`/`widthPct` as % of the window, 0.75% min bar). **It is a READ/NAVIGATE surface** (ADR-0348): no inline reschedule, no drag-to-schedule; a row tap opens the shared `TaskDetailDrawer` via `scheduleStore.setSelectedTaskId` (the row is an **overlay `<button>` with `absolute inset-0`**, and the complete control is a **sibling** `pointer-events-auto z-10` button — never a `<button>` nested in a `<button>`, which is invalid HTML). Critical = border/rail + redundant glyph, never fill (rules 234/235); progress = fill. The desktop `UnscheduledGutter` (drag-to-schedule) is replaced by a tap-to-open **Unscheduled tray** (collapsible, `localStorage` key `trueppm.mobile.schedule.unscheduled.collapsed`, auto-expand on 0→>0). Offline schedule writes are deferred to the native app — the surface edits online only. Never restyle the canvas to "fix mobile"; branch a new tree behind `isMobile`. (This slot previously held the #1787 quick-win exemption — mobile `fitToProject()` + folded Gantt toolbar; that code was removed with the mobile canvas, which this surface supersedes.)

## Group headers and overlapping avatar stacks (Issue #1804)

250. **Menu/panel group headers (eyebrows) use one canonical recipe: `text-xs font-semibold uppercase tracking-[.06em] text-neutral-text-secondary`; inside a `role="menu"` or dialog, wrap the grouped rows in a `role="group"` labeled "like the header" (`aria-label` or `aria-labelledby`), with the visible header div `aria-hidden`.** The tree had grown ≥4 competing eyebrow treatments (`text-[11px] tracking-[.08em]`, `text-[11px] tracking-wide`, `tracking-widest`, …); `text-xs` is the rule-50 floor, so it is the canonical size — do not mint new size/tracking combos. `role="group"` is a permitted owned element of `role="menu"` and is transparent to `useFocusTrap` (descendant selector) and to Escape/outside-click handling. Reference: `features/shell/UserMenu.tsx` ("Personal" group).

251. **An overlapping avatar stack (`-space-x-*`) must composite each circle over an opaque `rounded-full` underlay matching its cutout-ring token.** The canonical `AvatarInitials` fill (rule 143, #1705) is translucent `bg-brand-primary/15`; overlapped directly, the top circle's wash double-tints over the circle beneath (~28% vs 15%) and the under-avatar's initials ghost through the overlap lens. Give the wrapper span `rounded-full bg-<surface>` where `<surface>` is the same token as the `ring-*` cutout — and pick that token from the surface the stack actually sits on (`chrome-surface` in the TopBar, not `neutral-surface`), or the ring reads as a faint mismatched halo. Solid-fill stacks (`RetroPresenceChips`) don't need the underlay. Reference: `features/shell/PresenceAvatarStack.tsx`.

## Semantic status text token has no `-text` suffix (Issue #1392)

252. **Status text color is `text-semantic-{critical|warning|on-track|at-risk}` — there is NO `-text` suffix variant.** The `semantic` palette in `tailwind.config.ts` defines exactly the four foreground tokens (`semantic-critical`, `semantic-warning`, `semantic-on-track`, `semantic-at-risk`) plus their `-bg` tint variants (`semantic-critical-bg`, …) for pill/card fills. There is no `semantic-*-text` key, so a class like `text-semantic-on-track-text` or `text-semantic-critical-text` compiles to **nothing** — Tailwind silently emits no CSS for an undefined token (rule 40), and the element falls back to inherited color, dropping the status-color intent while still *looking* fine to a casual glance. Use `text-semantic-critical` for red status text on a `bg-semantic-critical-bg` tint, etc. (A pre-existing instance of this latent bug lives in `WorkspaceEmailPage.tsx`'s send-test result and `HealthChip` map — left untouched by #1392's scope, but the same fix applies.) Reference: `components/OSSChip.tsx`, `features/auth/SsoCompletePage.tsx`, `features/settings/workspace/WorkspaceSsoPage.tsx`.

## A neutral read-state chip may become a tap-to-explain disclosure (Issue #1472)

253. **A neutral read-state chip may become a tap-to-explain disclosure, but the disclosure must stay neutral, grant no capability, and portal out of any clipping scroll container.** `PendingAcceptanceChip` (`features/board/PendingAcceptanceChip.tsx`) is the reference: pass an optional `explainer` string and the chip renders a real `<button aria-expanded aria-controls>` toggling a `role="note"` popover with one plain-language sentence + a **neutral** "Got it" close button; omit `explainer` and it stays the passive read-state `<span>` (rule 149) with no disclosure state mounted (a hook-free early return, so read-only surfaces keep the passivity contract — no `button` in the tree). Five invariants: (a) **tone** — trigger, popover, and close button use neutral tokens only (`bg-neutral-surface-sunken`/`surface-raised`, `text-neutral-*`, `border-neutral-border`, `shadow-pop`); do **not** reach for the shared `Button` `ghost` variant, which is sage-brand-toned (`text-brand-primary`) and violates rule 149 — hand-roll a neutral ghost. (b) **No capability** — the popover explains only; it never gains accept/reject/any-mutation control (the decision stays team-owned on role-gated planning surfaces, rule 144), and the close button uses dismissal copy ("Got it"), never an action verb; the acknowledge is ephemeral (close-only, no persistence, no API) because the read-state is server-owned and clears on accept/reject (ADR-0102). (c) **Disclosure a11y** — Escape and "Got it" both close **and** return focus to the trigger; click-outside closes without stealing focus (check the trigger **and** the portaled popover, which is no longer a DOM descendant of the wrapper); `stopPropagation` on pointerdown+click so opening never drags/selects/opens the host card; `before:inset-[-12px]` for a ≥44px hit target; rule-4 `focus-visible` ring. (d) **Escape the clip** — the chip lives in a board column whose card stack is `overflow-y-auto` and can be as narrow as 208px (narrower than the popover); an in-flow `absolute` popover would be clipped and `z-index` does **not** defeat an overflow-clipping ancestor, so **portal the popover to `document.body`** with `position: fixed`, position it from the trigger's `getBoundingClientRect()` (flip above when there's no room below, clamp horizontally to the viewport), and **close on scroll/resize** since a fixed popover can't track its anchor (pattern: `features/schedule/UnscheduledTaskRow.tsx`). Portaling also sidesteps the `<p>` phrasing constraint on My Work — the wrapper/trigger stay `<span>`/`<button>` (phrasing content) inside the paragraph while the popover renders out in the body. (e) **Copy** — built by `pendingAcceptanceExplainer(iterationLabelLower?)`: role-neutral ("someone on the team accepts it", never "your Scrum Master" — acceptance authority is any Admin/SM/PO holder), outcome-only (no state-machine jargon like "scope injection"), iteration-label-aware on the board (`itl.lower`) and generic on cross-project My Work. Reference: `features/board/PendingAcceptanceChip.tsx`, `features/board/BoardCard.tsx`, `features/me/MyWorkTaskRow.tsx`.

254. **A semantic-toned status value must also differ in *text*, not tone alone (WCAG 1.4.1): give each band its own label/delta string so on-track vs at-risk vs over never read identically to a colorblind user.** Reference: `features/me/myWorkFocus.ts` (`utilizationCard` — "of capacity" / "near capacity" / "over capacity" per band, #1912).

## A clamped/truncated line must never be the only place a user-authored value appears (Issue #1883)

255. **Any `line-clamp-*` or `truncate` element that renders user-authored or variable-length data (titles, filenames, comment previews, risk names) must expose the full value via `title` — and via `aria-label` where the clamped line is the sole channel for that value.** Rule 161 already covers truncated *abbreviations*; this extends the same guard to *user data*. A clamped line silently drops text with no hover or assistive-tech recovery, so the full string must live somewhere reachable. The task-drawer Activity timeline's secondary detail line (`features/schedule/ActivityTimeline.tsx`) is the reference: its `line-clamp-1` `<p>` carries `title={secondary}` so a long risk title or filename is recoverable on hover. Do not clamp a value whose full form appears nowhere else in the view without adding `title`.

## A board-card affordance whose meaning is hover-only or truncated must promote to a tap-to-peek on coarse pointers (Issue #1947)

256. **On a coarse pointer, a board-card affordance whose meaning is otherwise hover-only (a `title`/`aria-label` on a display span) or truncated (a clipped title) must promote to a tap-to-peek `CardPeekButton` (`features/board/CardPeekButton.tsx`), gated on `useIsCoarsePointer()` so the fine-pointer render stays byte-identical.** `title=`/`aria-label` are unreachable on touch and a `truncate` line silently drops its tail there, so on mobile (where boards force compact density, rule 193) the compact card's worst-offender health badge and its truncated title strand meaning the user cannot recover. The fix is NOT to persist a mobile comfortable-density override — rule 193 forces compact deliberately; the fix is a real `<button aria-expanded aria-controls>` that toggles a portaled `role="note"` popover carrying the full text (rule-253 disclosure mechanics: `createPortal` to `document.body`, `position:fixed` from the trigger rect, flip-above + horizontal clamp, close on Escape/"Got it"/outside-pointerdown/scroll(capture)/resize, return focus to trigger, `before:inset-[-12px]` ≥44px hit target, rule-4 focus ring, `stopPropagation` so the card-open handler never fires). Four invariants: (a) **coarse-only** — every new trigger is gated on `useIsCoarsePointer()`; on a fine pointer the branch collapses to today's exact DOM (no button, no aria), so desktop — including desktop *compact* density — is byte-identical. (b) **title disclosure is a dedicated end-of-title glyph** — an ellipsis (`MoreHorizontalIcon`, never an emoji, rule 242) `<button>` mounted **only** when `useIsCoarsePointer() && useIsOverflowing(titleRef)`; when the title fits, render nothing (rule 122); the title text itself is never the trigger — the card body stays the task-open target. (c) **zero added height** — the closed peek is portaled, so it adds no layout and the 36px compact bar height is unchanged whether or not the affordance is present. (d) **tone stays on the trigger, peek stays neutral, color is never the sole signal** — the badge trigger keeps its semantic tone (`cardSignalToneClass`), the peek surface uses neutral tokens only, and the peeked text is the full `srText` sentence (never color-only). Reference: `features/board/CardPeekButton.tsx`, `features/board/BoardCard.tsx` (compact branch), `hooks/useIsOverflowing.ts`.

## Per-user timezone re-clocks instants; the date format restyles every date (Issue #1953, ADR-0410)

257. **Timezone re-clocks instants; the date format restyles every date; a calendar date is never re-timezoned.** A datetime with a time-of-day (`…THH:MM:SSZ`, from a server `DateTimeField`) is an *instant* — format it via `useUserDateFormat().formatInstant` / `formatInstantDate` (re-clocked to the user's timezone AND styled by their date format). A date-only value (`YYYY-MM-DD`, from a `DateField`) is a *calendar date* — format it via `formatUtcDate` (`fmtUtcShort`/`fmtUtcLong`) or `useUserDateFormat().fmtDate*` (UTC-pinned per ADR-0144/rule 189, but styled by the user's date format). **Never apply a user timezone to a calendar date** (it shifts the displayed day per viewer — the ADR-0144 bug) and **never UTC-pin an instant**. The date *format* (`iso`/`us`/`eu`/`auto`) applies to BOTH (a style change is timezone-independent and never moves a day); the *timezone* applies to instants ONLY. Pick the path by the wire shape, not the field's name. The style is single-sourced in `lib/dateFormatStyle.ts`; `formatUtcDate.ts` keeps `timeZone:'UTC'` INVARIANT (never parameterized) and reads a module-level style default that `AppShell`'s `DisplayFormatSync` syncs from the user's `date_format` — so every forecast/CPM/schedule date already routing through `formatUtcDate` (rule 189) reflects the format with no call-site change. A surface that must live-update on a preference change (rather than on next navigation) reads `useUserDateFormat()` instead of the bare functions. Reference: `lib/dateFormatStyle.ts`, `lib/formatUtcDate.ts`, `lib/formatUserDateTime.ts`, `hooks/useUserDateFormat.ts`, `features/shell/DisplayFormatSync.tsx`.

## A large enumerable single-value selector is a searchable combobox, not a native `<select>` (Issue #1953)

258. **A single-value selector over a large enumerable set (timezones, locales, currencies — anything ≳50 options where the label is a compound `Group/Item` string) uses the rule-124 trigger + popover + searchable-listbox pattern, never a native `<select>`.** A native `<select>`'s built-in type-ahead only prefix-matches the option text, so a user cannot find `Europe/London` by typing `london` — the compound-id case fails exactly the search users reach for. Render a trigger `<button>` showing the current value (`aria-haspopup="listbox"`, `aria-expanded`, rule-4 ring, `h-11`) opening a popover with a `role="combobox"` search input (`focus-within` ring, rule 124/157) over a `role="listbox"` filtered by **case-insensitive substring on the whole id** (match `europe` *and* `london`). An "Automatic / detected" sentinel option is pinned as the first row **above a divider and exempt from the filter** so the user can always return to it; its label embeds the resolved value (`Automatic — detected: {Intl…timeZone}`). Empty result is a `role="status"` row. Reuse the rule-124 `SettingsContextSwitcher` (trigger/open/close/focus-return) and `ResourceSearchCombobox` (listbox internals) patterns. Corollary: an **enumerated display-format** choice (date/number/time format) is a native-radio `role="radiogroup"` (rule 167) that renders a **live sample of real current data inline per option** (right-aligned `.tppm-mono`) so all formats are compared at a glance, with a non-color note column ("Year first" / "Month first") differentiating options whose samples can coincide (rule 6). Reference: `features/me/TimezoneFormatSection.tsx`.

## The board activity rail opens sprint-scoped by default; `scope` is view context, not filter state (Issue #1946, ADR-0412)

259. **When `BoardActivityPanel` receives a truthy `sprintId`, it initializes `filters.scope = 'sprint'` and renders the `role="group" aria-label="Activity scope"` toggle ("This sprint" / "Whole board"); with no sprint the toggle is omitted entirely (never an empty control group) and scope is `'board'`.** Three invariants a refactor must preserve: (a) **`scope` is NOT filter state** — it is deliberately excluded from `isFiltered` and from the `DEFAULT_FILTERS` reset, so **"Clear" preserves the current scope** (`onChange({ ...DEFAULT_FILTERS, scope: filters.scope })`); scope is a view context (which cards' activity is shown), not a narrowing filter, and resetting it on Clear would yank the user out of their sprint. (b) **label-vs-value split** — the sprint-transition event-type chip is *labeled* **"Scope changes"** for findability (the `entered_sprint`/`exited_sprint`/`moved_sprint` events PO/SM watch) while its underlying `TypeGroup` value stays `'sprint'`; never rename the value (it is the server `type=` param contract). (c) **empty-state precedence** — a type/actor/time filter narrowing to zero must win over the sprint-scope empty copy ("No matching activity in this sprint." not "No activity in this sprint yet."), or an active filter reads as an empty sprint and the user never thinks to clear it. Density note: the whole filter row (scope, type, time) uses the shared 28px `Chip` (`h-7`); this is an accepted, documented density for the `BoardActivityFilters` bar — if the 44px touch floor (rule 5) is ever enforced here, lift the **entire** row's hit area, never single out one group (uniformity beats a lone compliant control). Reference: `features/board/activity/BoardActivityFilters.tsx`, `BoardActivityPanel.tsx`, `useBoardActivity.ts`.
