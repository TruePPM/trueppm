/**
 * Shared chevron affordance for this feature's native `<select>` controls (the
 * Program picker and the working-calendar picker on the Start sheet, #2728) — a
 * single data-URI so both stay pixel-identical without either importing the other.
 */
export const SELECT_CHEVRON =
  "url(\"data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='11' height='11' viewBox='0 0 16 16'><path d='M4 6l4 4 4-4' stroke='%23667085' stroke-width='2' stroke-linecap='round' fill='none' /></svg>\")";
